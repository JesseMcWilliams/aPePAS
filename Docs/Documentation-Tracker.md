# Documentation Tracker

Tracks the status of all design and reference documents for the CyberArk PAS Scripts project.
Update the Status column as documents are created or revised.

## Status Key

| Symbol | Meaning |
|---|---|
| ✅ | Complete |
| 🔄 | In Progress |
| 📋 | Planned |

---

## Documents

### Architecture and Design

| Document | File | Status | Priority | Description |
|---|---|---|---|---|
| Architecture Overview | [Architecture.md](Architecture.md) | ✅ | High | System components, folder structure, data flow, design decisions, technology stack |
| Interface Definitions | [Interfaces.md](Interfaces.md) | ✅ | High | All shared data contracts: token object, result object, module metadata, profile schema, response object, log format |
| API Module Development Guide | [API-Module-Development-Guide.md](API-Module-Development-Guide.md) | ✅ | High | Step-by-step guide for creating new API modules, metadata contract, entry point signature, complete example, checklist |
| Auth Module Rework Design | [Auth-Module-Rework-Design.md](Auth-Module-Rework-Design.md) | ✅ | High | Design plan for splitting Get-AuthToken.ps1 into CyberArk.Auth.Common/ISPSS/SelfHosted.psm1; public API surfaces, driver changes, refresh improvements, open decisions |
| Documentation Tracker | [Documentation-Tracker.md](Documentation-Tracker.md) | ✅ | High | This file |

### Module References

| Document | File | Status | Priority | Description |
|---|---|---|---|---|
| Shared Comms Module Reference | [SharedComms-Reference.md](SharedComms-Reference.md) | 📋 | High | Complete function reference for `CyberArkComms.psm1`: `Invoke-CyberArkAPI`, query helpers, URL helpers, response object, WhatIf behavior, pagination, rate limiting |
| Logging Module Reference | [Logging-Reference.md](Logging-Reference.md) | 📋 | High | `Write-CyberArkLog` signature, all log levels with examples, log file format spec, startup/shutdown blocks, bare mode, sensitive data masking, log cleanup |
| Driver Script Reference | [Manage-Privilege-Reference.md](Manage-Privilege-Reference.md) | 📋 | Medium | Driver navigation system (B# breadcrumbs), session lifecycle, profile management flow, CSV processing loop, token expiry handling, module discovery |

### Configuration and Schema

| Document | File | Status | Priority | Description |
|---|---|---|---|---|
| Profile Schema Reference | [Profile-Schema.md](Profile-Schema.md) | 📋 | Medium | Both profile files fully documented: JSON field descriptions and validation rules, DPAPI XML field table, field resolution order, example profiles |

### Testing

| Document | File | Status | Priority | Description |
|---|---|---|---|---|
| Testing Plan | [Testing-Plan.md](Testing-Plan.md) | ✅ | High | Full test matrix: unit test IDs (L/C/S/A/D series), integration test procedures, Pester v5 patterns, known limitations |

### Operations

| Document | File | Status | Priority | Description |
|---|---|---|---|---|
| Installation and Prerequisites | [Installation.md](Installation.md) | 📋 | Medium | Requirements (PS version, WebView2, .NET), setup steps, folder layout, first-run walkthrough, module installation |
| Troubleshooting Guide | [Troubleshooting.md](Troubleshooting.md) | 📋 | Low | Common failures with resolution steps: WebView2 not found, certificate not in store, SSL errors, rate limiting, token refresh failure, profile decryption on wrong machine |

### Living Documents (update as modules are added)

| Document | File | Status | Priority | Description |
|---|---|---|---|---|
| API Coverage Matrix | [API-Coverage-Matrix.md](API-Coverage-Matrix.md) | 📋 | Medium | Tracks every planned and implemented API module: Name, Category, SupportedSystems, endpoint, module version, status (Planned / In Progress / Complete) |
| Lessons Learned | [Lessons-Learned-PowerShell-Pester.md](Lessons-Learned-PowerShell-Pester.md) | ✅ | Medium | PS 5.1 compatibility, Pester v6 mock patterns, API module conventions — bugs found during unit test development |

---

## Creation Order

Build documents in this sequence to support the implementation phases:

1. ✅ `Architecture.md` — foundation reference
2. ✅ `Interfaces.md` — contracts needed before writing any code
3. ✅ `API-Module-Development-Guide.md` — module authors need this before first module
4. ✅ `Documentation-Tracker.md` — this file
5. 📋 `Logging-Reference.md` — write alongside `CyberArkLogging.psm1`
6. 📋 `SharedComms-Reference.md` — write alongside `CyberArkComms.psm1`
7. 📋 `Profile-Schema.md` — write alongside Driver profile management code
8. 📋 `Driver-Reference.md` — write alongside Driver action menu and navigation
9. ✅ `Testing-Plan.md` — created alongside first module
10. 📋 `API-Coverage-Matrix.md` — create when first module is built; update continuously
11. 📋 `Installation.md` — write when system is functionally complete
12. 📋 `Troubleshooting.md` — write after first full end-to-end test run

---

## Revision Log

| Date | Document | Change |
|---|---|---|
| 2026-08-14 | Architecture.md | Created |
| 2026-08-14 | Interfaces.md | Created |
| 2026-08-14 | API-Module-Development-Guide.md | Created |
| 2026-08-14 | Documentation-Tracker.md | Created |
| 2026-08-15 | Lessons-Learned-PowerShell-Pester.md | Created — PS 5.1 / Pester v6 patterns |
| 2026-08-15 | Architecture.md | Added Groups category (7 modules) |
| 2026-08-15 | Groups modules | Created 7 APIModules\Groups\Invoke-Groups*.ps1 + 7 test files — 103 new tests, all passing |
| 2026-08-15 | Reports module | Created APIModules\Reports\Invoke-ReportsList.ps1 (SelfHosted only, Priority=70) + 15 unit tests (RL01–RL15) |
| 2026-08-15 | Architecture.md | Added Reports category (Invoke-ReportsList.ps1) to folder structure |
| 2026-08-15 | Lessons-Learned-PowerShell-Pester.md | Added Section 4 — PS 5.1 strict-mode patterns: hashtable dot notation, PSCustomObject optional properties, null-safe Count, Pester Should-Not-Throw scope |
| 2026-08-15 | API-Module-Development-Guide.md | Fixed dot-notation examples to bracket notation; added strict-mode safety checklist items |
| 2026-08-15 | 10+ API modules | Fixed PS 5.1 strict-mode bugs: $InputData bracket notation, PSObject.Properties guards, null-safe Count — all 583 unit tests passing |
| 2026-08-15 | Driver.ps1 | Added SystemType field (Privilege Cloud / Self-Hosted) to profile; branched BaseURL prompt in edit flow; passes SystemType + URL pre-populated to Get-AuthToken |
| 2026-08-15 | Interfaces.md | Updated Driver Profile JSON schema: added SystemType and BaseURL fields, field reference table, and SystemType→BaseURL prompt mapping table |
| 2026-08-15 | Architecture.md | Added Design Decisions rows for SystemType labelling and branched BaseURL collection |
| 2026-08-15 | Driver.ps1 | Added AppName field (default PasswordVault) and AuthMethod field to profile; numbered auth method selection in edit flow; fixed Self-Hosted PVWAUrl to join BaseURL+AppName; Privilege Cloud token.BaseURL patched post-auth; LogFolder/OutputFolder auto-created; URL shown in auth error messages |
| 2026-08-15 | Get-AuthToken.ps1 | Replaced auth method text prompt with numbered list selection; logon URL included in authentication failure error messages |
| 2026-08-15 | Interfaces.md | Updated Driver Profile JSON schema: added AppName and AuthMethod fields; updated Get-AuthToken call mapping to include AppName in PVWAUrl construction |
| 2026-08-15 | Driver.ps1 | Bug fixes: AuthMethod column now falls back to profile JSON when no token exists; Enter defaults to C (Connect) on profile detail screen; username captured from Get-Credential and saved back to profile; module functions re-dot-sourced in Invoke-SessionLoop scope so Invoke-ActionModule can resolve them |
| 2026-08-15 | 27 API modules | Fixed $Defaults.Key dot notation → $Defaults['Key'] bracket notation in all Get-*Input custom input functions — PS 5.1 strict-mode throws PropertyNotFoundException on missing hashtable keys accessed via dot notation |
| 2026-08-15 | Lessons-Learned-PowerShell-Pester.md | Added Section 4.5 ($Defaults bracket notation in custom input functions) and Section 6.1 (function scope lost when dot-source runs inside a returned function) |
| 2026-08-15 | API-Module-Development-Guide.md | Fixed $Defaults example code to use bracket notation; added $Defaults['Key'] checklist item under custom input section |
| 2026-08-15 | Driver.ps1 | Added Get-CsvSavePath (Windows Forms SaveFileDialog + console fallback); Invoke-ActionModule now offers CSV save after any ProducesOutput=true module returns results |
| 2026-08-15 | Driver.ps1 | Changed Get-ProfileTokenPath: token file extension .xml → .cred |
| 2026-08-15 | Get-AuthToken.ps1 | Changed Resolve-ProfilePath (2 paths) and Get-AuthTokenProfiles filter: .xml → .cred |
| 2026-08-15 | Interfaces.md | Updated Auth Token File section and Driver Profile field reference: .xml → .cred |
| 2026-08-16 | Driver.ps1 | Added Invoke-EntitySearch helper; updated 10 Get-*Input functions (Accounts, Users, Platforms, Groups) to allow ID or name/search with numbered pick list |
| 2026-08-16 | Driver.ps1 | Changed CSV save prompt to default N ([y/N]) |
| 2026-08-16 | Invoke-PlatformsList.ps1 | Fixed PropertyNotFoundException: added PSObject.Properties guards for id/name/description/active/platformType |
| 2026-08-16 | Invoke-UsersGet.ps1 | Fixed PropertyNotFoundException: added nested PSObject.Properties guards for personalDetails.email/firstName/lastName |
| 2026-08-16 | Invoke-AccountsGet.ps1 | Fixed PropertyNotFoundException: added nested PSObject.Properties guards for secretManagement sub-fields |
| 2026-08-16 | Invoke-AccountsUpdate.ps1 | Fixed PropertyNotFoundException: added nested PSObject.Properties guards for secretManagement sub-fields in result mapping |
| 2026-08-16 | Invoke-PlatformsGet.ps1 | Fixed PropertyNotFoundException: added PSObject.Properties guards for all platform result fields |
| 2026-08-16 | Accounts modules (11 new) | Added Invoke-AccountsGetActivity, LinkAccount, UnlinkAccount, Unlock, CheckIn, ResumeAutoManagement, CancelCpmTask, Verify, ChangeInVault, ChangeImmediate, Reconcile — each with AcceptsInputFile=true, SupportsWhatIf=true, entity search input, Pester test file |
| 2026-08-16 | Lessons-Learned-PowerShell-Pester.md | Added Section 7 — custom input function cancellation ($null return) and nested PSObject.Properties guard pattern |
| 2026-08-16 | Architecture.md | Added 11 new Account modules to folder structure; updated .xml → .cred in profile files; updated Data Flow to mention ID-to-name lookup |
| 2026-08-16 | Custom modules (4 new) | Added Invoke-CustomExportAll, ExportEntitlements, ExportGroupMembersLocal, ExportGroupMembersLDAP — orchestration modules with recursive group traversal and ADSI support |
| 2026-08-16 | Applications modules (7 new) | Added Invoke-ApplicationsList/Get/Add/Delete, ListAuthMethods, AddAuthMethod, DeleteAuthMethod — SelfHosted only, legacy PIMServices.svc endpoint, nested body wrapper pattern |
| 2026-08-16 | Tests (11 new) | Added Pester test files for all 4 Custom export and 7 Applications modules |
| 2026-08-16 | Architecture.md | Added Custom and Applications categories to folder structure |
| 2026-08-16 | Lessons-Learned-PowerShell-Pester.md | Added Section 8 — orchestration module pattern, ADSI vs AD module, stack-based traversal, Applications API body/response shape |
| 2026-08-16 | 22 test files (Tests\Unit\) | Fixed Pester v6.1 regression: moved BeforeAll to file level (before Describe) in all new-style test files; added Initialize-CyberArkLog calls; added Get-CsvSavePath global stub; fixed mock data for GetActivity, ChangeInVault, LinkAccount, UnlinkAccount; removed invalid WhatIf test from GetActivity — 697 tests passing, 0 failures |
| 2026-08-16 | Lessons-Learned-PowerShell-Pester.md | Added Section 9 — Pester v6 BeforeAll scoping (9.1), Initialize-CyberArkLog requirement (9.2), Driver-scope helper stubs (9.3), success test required field completeness (9.4), WhatIf only suppresses mutating methods (9.5) |
| 2026-08-16 | CyberArkComms.psm1 | Fixed Join-CyberArkUrl: changed Trim('/') → TrimStart('/') to preserve trailing slashes; appended [Method URL] to all HTTP 400/500 error messages |
| 2026-08-16 | Driver.ps1 | Added Logout option [L] to profile detail screen; failed modules shown in red in action menu and show error on invoke; Join-CyberArkUrl trailing slash fix enables Applications List |
| 2026-08-16 | Driver.ps1 | List actions display numbered table and offer line-number drill-down to corresponding Get module after results; Invoke-ActionModule accepts $Defaults for pre-populated input |
| 2026-08-16 | Driver.ps1 | Back [B] is now the default on profile detail, action menu, and module mode selection screens — pressing Enter goes back |
| 2026-08-16 | Driver.ps1 | Continue [C] now shows auth token details (signed-in user, system type, auth method, base URL, expiry) before entering session loop |
| 2026-08-16 | Invoke-PlatformsGet.ps1 | Added root-level fallback for id/name/description/platformType/active — handles CyberArk single-GET responses where fields are at root instead of under general |
| 2026-08-16 | Invoke-PlatformsList.ps1 | Same dual-fallback applied for consistency |
| 2026-08-16 | 9 API modules + Run-Tests.ps1 + CyberArkComms.psm1 | Fixed 62 test regressions from commit 5edf0c8: applied [array] type constraint to 7 list modules (AccountsList, SafesList, PlatformsList, UsersList, SafeMembersList, ReportsList, ExportGroupMembersLocal); added (-not $col) or null guard to ExportEntitlements and ExportGroupMembersLocal empty-list checks; reverted GroupsGetMembers to /Members endpoint + value property; restored memberType in GroupsAddMember body; fixed CyberArkComms Join-CyberArkUrl TrimStart→Trim (was breaking C12); added PassThru=true to Run-Tests.ps1 Pester config — all 697 tests passing |
| 2026-08-16 | Lessons-Learned-PowerShell-Pester.md | Added Section 4.6 — PS 5.1 empty @() in script block outputs nothing (not an empty array), so [array] alone does not prevent null when API returns 0 items; added Section 9.6 — Invoke-Pester requires PassThru=true to return the result object; without it $result is null |
| 2026-08-16 | Invoke-GroupsGetMembers.ps1 + Invoke-CustomExportGroupMembersLocal.ps1 | Fixed runtime HTTP 405: reverted from /Members sub-resource to bare group GET (/API/UserGroups/{id}) with -PageSize 0; added multi-property fallback probe (members/Members/groupMembers/value) for PVWA version compatibility |
| 2026-08-16 | Invoke-GroupsGetMembers.Tests.ps1 + Invoke-CustomExportGroupMembersLocal.Tests.ps1 | Updated mock data from value= to members= property; updated endpoint filters from */Members to bare group path — 697 tests passing |
| 2026-08-16 | Lessons-Learned-PowerShell-Pester.md | Added Section 8.5 — CyberArk Groups /Members sub-resource returns 405 on many PVWA versions; correct pattern is bare group GET with multi-property member extraction |
| 2026-08-16 | Invoke-CustomExportEntitlements.ps1 | Fixed PropertyNotFoundException on $members.Count when a safe returns an empty members array (PS 5.1 @() from if-expression = null; added $memberCount null guard) |
| 2026-08-16 | Driver.ps1 Invoke-EntitySearch | Added -ClientSideFilter switch: fetches all results without server-side search param, then applies case-insensitive contains filter client-side across DisplayProperties |
| 2026-08-16 | 5 Applications input functions | Removed SearchParam='AppID' from Invoke-EntitySearch calls (PIMServices does exact match only); switched to -ClientSideFilter for proper partial search behaviour |
| 2026-08-16 | Get-AuthToken.ps1 | Fixed Privilege Cloud Interactive auth: (1) Username from profile was dropped by local variable overwrite in Interactive dispatch — fixed fallback chain to include $Username param; (2) Resolve-IdentityTenantURL used HTTP redirect-follow which fails when portal uses JavaScript redirects — replaced with direct {subdomain}.id.cyberark.cloud construction |
| 2026-08-17 | Get-AuthToken.ps1 | Replaced static Resolve-IdentityTenantURL with multi-candidate Invoke-WebRequest redirect-discovery approach (3 candidate URLs, up to 8 HTTP redirects, detects *.id.cyberark.cloud in final response host or exception redirect host) |
| 2026-08-17 | Get-AuthToken.ps1 | Fixed StrictMode crash: PSObject.Properties['IdpRedirectShortUrl'] guard added — field absent on non-IdP tenants |
| 2026-08-17 | Get-AuthToken.ps1 | Fixed StrictMode crash: PSObject.Properties['Token'] and ['Auth'] guards added in AdvanceAuthentication result extraction; added root-level $resp.Token/$resp.Auth fallback for OOB LoginSuccess string result |
| 2026-08-17 | Get-AuthToken.ps1 | Improved OOB poll loop: 2s interval (was 3s), in-place elapsed timer, case-insensitive OobPending comparison |
| 2026-08-17 | Invoke-AccountsList.ps1 | Fixed StrictMode crash: PSObject.Properties guards added for all flat account fields (id, name, address, userName, platformId, safeName, secretType) |
| 2026-08-17 | Driver.ps1 | Fixed 401 re-auth: added immediate Test-TokenExpiry + Invoke-TokenRefresh check after each Invoke-ActionModule call in inner action loop — prompt appears immediately, not after user presses [B] |
| 2026-08-17 | Invoke-CustomExportAll.ps1 | Fixed ExcludeFromExportAll filtering: changed $_.Meta.PSObject.Properties['ExcludeFromExportAll'] to $_.Meta['ExcludeFromExportAll'] — PSObject.Properties on a hashtable never finds key-value entries |
| 2026-08-17 | Driver.ps1 | Fixed SaveFileDialog ignoring OutputFolder: added RestoreDirectory = $true to Get-CsvSavePath dialog — required for InitialDirectory to be honoured |
| 2026-08-17 | Invoke-PlatformsGet.ps1 | Fixed Platform GET showing only Active field: added dual-location fallback (general sub-object + root) and alternate field name support (PlatformID/SystemType vs id/platformType); added DEBUG logging of actual response field names |
| 2026-08-17 | Lessons-Learned-PowerShell-Pester.md | Added Section 4.7 — PSObject.Properties on hashtable finds object members not key-value entries; use $ht['Key'] for hashtable, PSObject.Properties only for PSCustomObject |
| 2026-08-17 | Lessons-Learned-PowerShell-Pester.md | Added Section 11 — CyberArk Identity/Privilege Cloud: Identity tenant URL multi-candidate discovery (11.1), AdvanceAuthentication dual-field + root-level token extraction + OOB patterns (11.2), immediate re-auth after 401 (11.3) |
| 2026-08-17 | Lessons-Learned-PowerShell-Pester.md | Added Section 12 — CyberArk Platform GET response field name variations by PVWA version (general sub-object vs root, id/platformType vs PlatformID/SystemType) |
| 2026-08-17 | Lessons-Learned-PowerShell-Pester.md | Added Section 13 — Windows Forms: SaveFileDialog.RestoreDirectory must be $true to honor InitialDirectory |
| 2026-08-17 | Get-AuthToken.ps1 | Fixed ISPSS BaseURL missing /PasswordVault: PCLOUD_BASE_TEMPLATE now includes /PasswordVault so all token paths (fresh auth, refresh, AutoRefresh) produce the correct URL without Driver-side patching |
| 2026-08-17 | Driver.ps1 | Added Driver-side BaseURL safety patches (Test Connection, Connect load-from-disk, Connect fresh-auth) to fix old saved tokens lacking /PasswordVault |
| 2026-08-17 | Driver.ps1 + Get-AuthToken.ps1 | Renamed profile storage folder: %APPDATA%\CyberArkPAS → %APPDATA%\IdiraUnifiedScripts\Profiles (aligns code with README and all documentation) |
| 2026-08-17 | Driver.ps1 | Added three ISPSS-only profile fields: TenantPortal ({sub}.cyberark.com), TenantVault (vault-{sub}.privilegecloud.cyberark.com), TenantAuth (discovered identity URL). Auto-computed at profile edit; TenantAuth passed to Get-AuthToken to skip per-login HTTP probe; written back after each successful login |
| 2026-08-17 | Interfaces.md | Updated Driver Profile JSON schema: added TenantPortal, TenantVault, TenantAuth fields; updated AppName description (ISPSS vs Self-Hosted distinction); corrected BaseURL-to-API-URL note (now baked into PCLOUD_BASE_TEMPLATE); added auto-computed fields table |
| 2026-08-17 | Lessons-Learned-PowerShell-Pester.md | Updated Section 11.1 rule: IdentityHost → TenantAuth field name; added post-login write-back pattern. Added Section 11.4: fix URL constants at the source not in downstream callers. Updated Section 13.1: added known trade-off note that XP-style dialog was rejected and InitialDirectory remains unresolved |
| 2026-08-17 | Get-AuthToken.ps1 | Changed Resolve-IdentityTenantURL MaximumRedirection from 8 to 0 — intentionally causes first redirect to throw so catch block captures redirect target URL via exception.Response.ResponseUri |
| 2026-08-17 | Lessons-Learned-PowerShell-Pester.md | Updated Section 11.1 — corrected code example to MaximumRedirection 0 and explained why 0 is correct (captures redirect URL in exception vs silently following chain to JavaScript portal) |
| 2026-08-17 | Auth-Module-Rework-Design.md | Created — design document for splitting Get-AuthToken.ps1 into three psm1 modules (Common/ISPSS/SelfHosted); covers public API surfaces, driver changes, refresh improvements (proactive refresh, 401 vs 403, transient retry, context clearing), and 7 open decisions |
| 2026-08-17 | Auth\CyberArk.Auth.Common.psm1 | Created — shared auth utilities: New-AuthTokenObject, ConvertTo-PlainText, Get-FilteredClientCertificate, Import-WebView2Assembly, Invoke-WebView2Window, Save-AuthToken, Import-AuthToken (no AutoRefresh), Get-AuthTokenProfiles, Remove-AuthTokenProfile |
| 2026-08-17 | Auth\CyberArk.Auth.ISPSS.psm1 | Created — ISPSS auth module: exports Get-ISPSSAuthToken (7 params, no SystemType routing), Update-ISPSSAuthToken (refresh_token grant + fallback re-auth), Resolve-IdentityTenantURL; all Identity challenge/OOB logic private |
| 2026-08-17 | Auth\CyberArk.Auth.SelfHosted.psm1 | Created — SelfHosted auth module: exports Get-SelfHostedAuthToken (8 params), Update-SelfHostedAuthToken; all PVWA logon helpers private |
| 2026-08-17 | Driver.ps1 | Replaced dot-source of Get-AuthToken.ps1 with Import-Module for three new auth modules; updated Assert-Prerequisites; replaced Get-AuthToken calls in Connect and Test Connection with Get-ISPSSAuthToken/Get-SelfHostedAuthToken; rewrote Invoke-TokenRefresh to use Update-ISPSSAuthToken/Update-SelfHostedAuthToken; added Invoke-ProactiveRefresh (silent CC refresh 10 min before expiry) and Invoke-ClearNonRefreshableContext (removes stored credentials for Interactive/SSO/SAML/OIDC after login); added ProactiveRefreshThresholdMin and PVWA_SESSION_EXPIRY_MIN constants |
| 2026-08-17 | Auth-Module-Rework-Design.md | Updated status to reflect implementation complete; decisions D1-D6 resolved |
| 2026-08-17 | Architecture.md | Updated Build Status table, Component Diagram, Folder Structure, Startup data flow, Component Descriptions, and Design Decisions to reflect three-module auth split (Common/ISPSS/SelfHosted); removed Get-AuthToken.ps1 references; added Auth module isolation, proactive refresh, and credential scrubbing design decisions |
| 2026-08-17 | Interfaces.md | Updated token object source reference; renamed `_RefreshContext` Update-AuthToken note; added Auth Module Public Functions section with full signatures for Get-ISPSSAuthToken, Update-ISPSSAuthToken, Resolve-IdentityTenantURL, Get-SelfHostedAuthToken, Update-SelfHostedAuthToken, and CyberArk.Auth.Common.psm1 function table; updated SystemType call-site mapping; added ProactiveRefreshThresholdMin to config variables table |
| 2026-08-17 | CyberArkLogging.psm1 | Added -OverwriteFile switch to Initialize-CyberArkLog: uses fixed 'startup.log' filename and truncates file at init; timestamped log files unchanged when switch is absent |
| 2026-08-17 | Driver.ps1 | Added -LogLevel and -LogFolder parameters; Initialize-CyberArkLog moved to immediately after Assert-Prerequisites so startup log captures module load sequence; startup log uses -OverwriteFile (overwritten on each launch); added PS version, platform, and per-module DEBUG log lines at startup; Get-AllDriverProfiles now logs profile count and per-profile name/type/token-status/expiry at DEBUG level |
| 2026-08-17 | CyberArk.Auth.ISPSS.psm1 | Added private script:Write-ISPSSLog wrapper (calls Write-CyberArkLog when available, falls back to Write-Verbose); replaced all Write-Verbose in Resolve-IdentityTenantURL with structured log entries (INFO for discovery start/result, DEBUG for per-candidate probe detail, WARN for fallback and no-redirect cases) |
| 2026-08-17 | CyberArk.Auth.ISPSS.psm1 | Fixed three InvalidOperationException sources in Resolve-IdentityTenantURL: (1) Get-WebResponseHost and Get-ExceptionRedirectHost — wrapped .ResponseUri access in try/catch since non-null WebException.Response can be in an invalid state on SSL/TLS failures; (2) catch block — captured $_ as $caughtError immediately (any pipeline or inner try/catch overwrites $_ in PS 5.1); (3) StatusCode access wrapped in try/catch; each Exception property access wrapped individually |
| 2026-08-17 | Lessons-Learned-PowerShell-Pester.md | Updated Section 11.1 — updated Get-WebResponseHost and Get-ExceptionRedirectHost code examples to show try/catch guards; added note explaining why null-check alone is insufficient for invalid-state WebException.Response objects |
| 2026-08-17 | Lessons-Learned-PowerShell-Pester.md | Added Section 6.2 — cross-module logging: private script: wrapper pattern with Get-Command guard; why -FunctionName must be passed explicitly through the wrapper; rule against calling session-level functions directly from .psm1 |
| 2026-08-17 | Lessons-Learned-PowerShell-Pester.md | Added Section 14 — PS 5.1 catch block safety: Section 14.1 ($_ overwritten by any pipeline or inner try/catch — always capture as $caughtError = $_ first); Section 14.2 (WebException response properties throw InvalidOperationException on non-null invalid-state objects — wrap each property access in its own try/catch; includes table of which network conditions produce invalid-state response objects) |
| 2026-08-17 | Driver.ps1 | Fixed token refresh exit bug: removed `Invoke-TokenRefresh | Out-Null` from Warning branch (failures silently discarded caused premature expiry detection); added 'Warning' to Invoke-TokenRefresh early-return guard so valid-but-expiring tokens never trigger re-auth |
| 2026-08-17 | Driver.ps1 | Added Set-SessionToken helper: copies all NoteProperties (including MaxResults) from old token to new before assigning $script:SessionToken; replaced all direct $script:SessionToken assignments in refresh paths |
| 2026-08-17 | Driver.ps1 | Export Entitlements display truncation: generalized existing SafeMembers truncation flag to also cover Custom/ExportEntitlements; both limited to first 10 records on screen |
| 2026-08-17 | Invoke-SafeMembersAdd.ps1 | Added Role/Specified permission mode: 1=Role (named preset: ReadOnly/EndUser/PowerUser/SafeManager); 2=Specified (enter each of 20 permissions as Y/N or supply per-column CSV values from Entitlements report export); $script:PermissionColumns and helper functions scoped with script: prefix |
| 2026-08-17 | Invoke-SafeMembersUpdate.ps1 | Same Role/Specified permission mode as Add; three-tier permission resolution: (1) InputData['Permissions'] hashtable, (2) individual permission CSV columns, (3) named role preset |
| 2026-08-17 | Invoke-CustomExportGroupMembersLDAP.ps1 | Pre-skipped Vault-type groups before AD lookup; broadened LDAP detection regex to include 'directory', 'ldap', 'external', 'activedirectory', 'microsoftad'; improved not-found message to include groupType and directoryType hints |
| 2026-08-18 | Driver.ps1 | Action menu sort: List module now explicitly sorted first within each category using [int]($_.Meta.Action -ne 'List') as primary sort key; secondary sort by Priority unchanged |
| 2026-08-18 | Driver.ps1 | Added Role_Template_Safe and Role_Group_Prefix profile fields: New-BlankProfile, normalization loop, Show-ProfileDetail, Invoke-ProfileEditFlow |
| 2026-08-18 | Driver.ps1 | Added TenantPortal, TenantVault, TenantAuth to profile normalization loop (were missing from Get-AllDriverProfiles string-field normalization) |
| 2026-08-18 | Driver.ps1 | Added DisplayLimit profile field (default 20, 0=show all): stored in profile, editable in edit flow; generalised list truncation from hardcoded 10-row cap (SafeMembers + ExportEntitlements only) to all List actions across all categories plus ExportEntitlements; limit driven by profile setting at render time; hint pointing to Profile Settings shown when truncated |
| 2026-08-18 | Invoke-CustomExportGroupMembersLDAP.ps1 | Removed client-side groupType/directoryType filter entirely (ISPSS now returns all groups with groupType='Vault' regardless of actual directory source); AD lookup is now the sole filter; not-found changed from WARN/Failure to DEBUG/skip (expected for Vault-only groups) |
| 2026-08-18 | Invoke-AccountsList.ps1 | Added By-Safe retrieval mode: fetches safe list first, queries accounts per safe with OData filter, bypasses ~20K API count cap; script:Add-AccountToResult helper eliminates duplicated mapping block; normal mode unchanged |
| 2026-08-18 | Interfaces.md | Updated Driver Profile JSON schema: added Username, Limit, DisplayLimit, Role_Template_Safe, Role_Group_Prefix; removed ParallelThreads (never implemented); updated field reference table accordingly |
| 2026-08-18 | Architecture.md | Updated Build Status table (all components Complete); added four Design Decision rows: List-first sort, DisplayLimit, By-Safe accounts mode, Role profile fields |
| 2026-08-18 | Lessons-Learned-PowerShell-Pester.md | Added Section 15 — session token NoteProperty preservation (15.1 copy-before-replace pattern; 15.2 Warning branch must not call Invoke-TokenRefresh); Section 16 — ISPSS runtime behaviors (16.1 groupType='Vault' on all groups, 16.2 20K account cap and per-safe workaround, 16.3 OData filter quoting); Section 17 — script-scoped helper functions in dot-sourced modules |
| 2026-08-18 | Auth\CyberArk.Auth.SelfHosted.psm1 + CyberArk.Auth.ISPSS.psm1 | Fixed Save-AuthToken CommandNotFoundException: both modules called Import-Module CyberArk.Auth.Common -Force without -Global, reimporting Common as a nested module and removing it from global session state; added -Global so the import stays visible to all callers |
| 2026-08-18 | Invoke-SafeMembersList.ps1 + Invoke-CustomExportEntitlements.ps1 | Fixed all 22 permission columns showing false: API returns camelCase (useAccounts) but lookups used PascalCase (UseAccounts); PSObject.Properties['UseAccounts'] returns null silently — all permissions defaulted to false; fixed all lookups to camelCase; added missing header fields safeUrlId, safeNumber, memberId, isExpiredMembershipEnable, requestsAuthorizationLevel1/2; removed non-existent SearchIn and IsMemberOfSafe fields; bumped both modules to v1.1.0 |
| 2026-08-18 | Invoke-AccountsList.ps1 | Fixed By-Safe mode returning zero accounts: OData filter used single quotes (safeName eq 'Name') which get URL-encoded to %27 by New-CyberArkQuery; server received quotes as literal characters in safe name — no match found; removed single quotes; URL encoding handles safe names with spaces without quoting; bumped to v1.2.0 |
| 2026-08-18 | Driver.ps1 | Extended display-limit truncation from hardcoded ExportEntitlements action to all Custom modules with ProducesOutput=true: covers ExportGroupMembersLDAP and ExportGroupMembersLocal and any future Custom export automatically |
| 2026-08-18 | Driver.ps1 | Fixed relative profile folder path resolution in Get-CsvSavePath, Select-InputFiles, Get-OutputCsvPath: Test-Path on a relative path resolves against Get-Location (cwd), not PSScriptRoot; now uses IsPathRooted to detect relative paths and Join-Path PSScriptRoot to make them fully qualified before testing |
| 2026-08-18 | Lessons-Learned-PowerShell-Pester.md | Fixed Section 16.3 (OData filter quoting was wrong — single quotes get URL-encoded and become part of the literal name, causing zero results); added Section 18 — Import-Module -Force without -Global inside a .psm1 removes module from global scope (nested module scope bug); Section 19 — CyberArk SafeMembers API camelCase permissions and full field reference table; Section 20 — relative profile path resolution must use IsPathRooted + PSScriptRoot |
| 2026-08-18 | Architecture.md | Updated Table display truncation design decision: changed "List and ExportEntitlements" to "List actions and all Custom export operations (ProducesOutput)" |
| 2026-08-18 | Lessons-Learned-PowerShell-Pester.md | Corrected Section 16.3 to use %22 (double quote) encoding example instead of %27; fixed 16.2 workaround URL to show unquoted filter |
| 2026-08-18 | Invoke-SafesList.ps1 + Invoke-SafesGet.ps1 | Added missing fields to both Safe modules (v1.1.0): SafeUrlId, SafeNumber, CreatorId, LastModified (microseconds epoch ÷1000→ms), IsExpiredMember; added PSObject.Properties guards to all field accesses; fixed unguarded $safe.creator access in SafesGet that would throw under strict mode |
| 2026-08-18 | Invoke-PlatformsGet.ps1 | Added RequiredProperties and OptionalProperties columns (v1.1.0) built from platform.properties.required[*].name and optional[*].name arrays |
| 2026-08-18 | Invoke-AccountsAdd.ps1 | Fixed PropertyNotFoundException crash (v1.1.0): $acct.secretManagement.status threw under strict mode when secretManagement existed but lacked a status property; all response field accesses now use PSObject.Properties guards at every level |
| 2026-08-18 | Modules\CyberArkLogging.psm1 | Added -FileOnly switch to Write-CyberArkLog and Write-ToDestinations: suppresses console output while still writing to the log file; used for large or sensitive content that should not appear on screen |
| 2026-08-18 | Modules\CyberArkComms.psm1 | Added POST/PUT/PATCH request body logging at DEBUG/file-only (immediately before the API call; sensitive fields masked by Mask-SensitiveData); added HTTP 4xx/5xx response body logging at DEBUG/file-only for post-failure diagnosis |
| 2026-08-18 | Invoke-AccountsLinkAccount.ps1 | Fixed HTTP 400 on Link Account (v1.1.0): body used PascalCase keys (Name, Safe, Folder, ExtraPasswordIndex) but API requires camelCase (name, safe, folder, extraPasswordIndex); extraPasswordIndex was sent as string but API requires integer; folder now defaults to Root when blank; added Search-LinkedAccount helper that searches the Accounts API and pre-populates Name/Safe/Folder defaults from the selected account; link type changed from raw number prompt to friendly menu (1=Logon, 2=Enable, 3=Reconcile) |
| 2026-08-18 | Lessons-Learned-PowerShell-Pester.md | Added Section 21 — CyberArk API request body field casing is endpoint-specific (PascalCase vs camelCase, with endpoint table); Section 22 — numeric API fields must be typed as integer not string; Section 23 — Safe lastModificationTime is in microseconds (divide by 1000 for milliseconds, not by 1000000); Section 24 — PSObject.Properties guard required at every level of nested property access; Section 25 — request body and error response DEBUG/file-only logging pattern |
| 2026-08-18 | Architecture.md | Added Design Decision row for request body and error logging (DEBUG, file-only) |
| 2026-08-18 | Invoke-SafeMembersAdd.ps1 | Fixed HTTP 400 on Add Safe Member (v1.2.0): body top-level keys changed to camelCase (memberName, searchIn, membershipExpirationDate, permissions); all 22 permission keys in Get-PermissionSet and Get-SpecifiedPermissions changed to camelCase (useAccounts, retrieveAccounts, etc.) using first-letter-lowercase conversion from PascalCase column names; interactive specifiedPerms hashtable now uses camelCase keys; searchIn default changed from 'Vault' to empty string — omitted from body when blank so API applies its own default; mutual exclusivity validation and response mapping updated to camelCase keys |
| 2026-08-18 | Auth\CyberArk.Auth.ISPSS.psm1 + Auth\Get-AuthToken.ps1 | Fixed MFA prompt showing "Sent" instead of "Send": CyberArk Identity API returns "Sent" in PromptMechChosen and PromptSelectMech; applied `-replace '\bSent\b','Send'` at all 4 render sites in each file |
| 2026-08-18 | Invoke-AccountsLinkAccount.ps1 | Redesigned CSV input schema (v1.2.0): replaced AccountID column with AccountName + Safe; added LinkName, LinkSafe, LinkFolder columns (was using AccountID + extraPasswordIndex only); account ID resolved server-side via safe-scoped filter (GET /API/Accounts?filter=safeName eq {Safe}&limit=1000) with local name/userName match; fixed EM DASH in WARN string (PS 5.1 UTF-8-no-BOM reads E2 80 94 as Windows-1252 byte 94 = right double-quote, corrupting token stream); fixed PropertyNotFoundException from $null.PSObject under strict mode — added $null -ne $searchResp.Data.value guard and $_ -and short-circuit in Where-Object |
| 2026-08-18 | Lessons-Learned-PowerShell-Pester.md | Added Section 26 — EM DASH in string literals breaks PS 5.1 parsing on UTF-8-no-BOM files (bytes E2 80 94 → Windows-1252 byte 94 = right double-quote → terminates string token); EM DASH in comments is safe; rule: use ASCII hyphen in all string literals; Section 27 — $null.PSObject returns $null in PS 5.1 (language intrinsic, not ETS lookup) so $null.Properties then throws; must guard with $null -ne check before PSObject.Properties access; $_ -and short-circuit pattern in Where-Object |
| 2026-08-18 | Driver.ps1 | Added IsDefault profile field: New-BlankProfile initializes to $false; normalization loop includes IsDefault; Show-ProfileDetail displays "Default Profile: Yes/No"; profile detail menu toggles [F] Set as Default / [F] Clear Default; setting a new default clears old default by loading all profiles, finding IsDefault=$true entries, setting them to $false and saving; Copy always sets IsDefault=$false on the copy |
| 2026-08-18 | 13 Accounts modules | Updated all CSV-accepting account modules to use AccountName + Safe instead of AccountID (v1.1.0): Invoke-AccountsGet, Delete, Unlock, CheckIn, Verify, Reconcile, GetActivity, CancelCpmTask, ChangeImmediate, ResumeAutoManagement, UnlinkAccount, ChangeInVault, Update; each resolves account ID via GET /API/Accounts?filter=safeName eq {Safe}&limit=1000 then local name/userName match; interactive path retains AccountID passthrough; AccountsUpdate also renames $accountID → $accountId and $encodedID → $encodedId; all dot-notation InputData access converted to bracket notation for strict-mode safety; GetCredential skipped (AcceptsInputFile=$false) |
| 2026-08-18 | Invoke-CustomTestApi.ps1 | Created new Custom module (v1.0.0): interactive raw API tester; method selection (GET/POST/PUT/PATCH/DELETE); API path relative to token BaseURL; optional query params; JSON body prompt for POST/PUT/PATCH (validated and normalized); calls Invoke-WebRequest directly for access to response headers; status code color-coded; JSON response body pretty-printed (40-line cap with log fallback); verbose toggle ([V]) shows masked request headers + request body + response headers per-request; session loop with [A] Another / [S] Save JSON / [V] Verbose / [B] Back; [S] saves all runs to timestamped JSON in OutputFolder with auth headers masked; driver CSV summary on exit (Time/Method/Path/QueryParams/StatusCode/Success per run); SSL bypass inherits IgnoreSSL from active profile |
| 2026-08-18 | Invoke-SafeMembersUpdate.ps1 | Fixed and aligned with Add module (v1.1.0): added RequestsAuthorizationLevel1/2 to InputSchema; changed all permission keys to camelCase in Get-PermissionSet (was PascalCase — would cause HTTP 400); added requestsAuthorizationLevel1/2 to permission base and presets; fixed ReadOnly preset (added viewSafeMembers); fixed SafeManager preset (added initiateCPMAccountManagementOperations); Get-SpecifiedPermissions now returns camelCase keys and handles auth level columns; Test-HasSpecifiedColumns checks auth level columns; interactive Specified mode gains dual-control 0/1/2 prompt; Invoke-SafeMembersUpdate gains mutual exclusivity validation; body keys changed to camelCase (membershipExpirationDate, permissions); Get-PermissionSet made script:-scoped; result mapping converted to PSObject.Properties guards |
| 2026-08-18 | Invoke-CustomTestApi.ps1 | Added [R] Retry option: reuses existing method, API path, and query params; re-prompts for request body only; $retryRequest flag wraps method/path/query prompt block with else branch showing reused values; flag reset to $false after conditional so it does not persist to next iteration |
| 2026-08-18 | Invoke-SafeMembersAdd.ps1 | Fixed missing memberType in request body (v1.2.1): MemberType was read from InputData in WhatIf and result mapping but never added to the body hashtable sent to the API; extracted $memberType alongside $searchIn and added conditionally to body (omit when blank so API default applies) |
| 2026-08-18 | Modules\CyberArkComms.psm1 + Invoke-CustomTestApi.ps1 | Added trailing slash when last URL path segment contains a dot: Join-CyberArkUrl checks $result.Split('/')[-1] -match '\.' after joining all segments and appends '/' if true; Invoke-CustomTestApi applies the same check to $cleanPath before appending query params; prevents proxies/servers from misinterpreting dotted segments as file extensions |
| 2026-08-18 | Lessons-Learned-PowerShell-Pester.md | Added Section 26 -- EM DASH in string literals breaks PS 5.1 parsing on UTF-8-no-BOM files (bytes E2 80 94 become Windows-1252 right double-quote, terminating the string token); Section 27 -- $null.PSObject returns $null in PS 5.1 so chaining .Properties throws; guard with $null -ne before PSObject.Properties; $_ -and short-circuit in Where-Object; @($null) one-element array origin; Section 28 -- trailing slash required when last URL path segment contains a dot; Section 29 -- PowerShell switch parameters: -SwitchParam $true silently fills next positional parameter with 'True' string; use bare flag name instead |
| 2026-08-18 | Invoke-CustomTestApi.ps1 | Fixed API Path prompt showing [default: True]: Show-FieldPrompt has -Required as [switch]; passing -Required $true caused $true to bind to the $Default positional parameter (coerced to string "True"), displayed as [default: True]; fixed by changing to -Required (bare switch syntax) |
| 2026-08-19 | Invoke-CustomExportGroupMembersLDAP.ps1 | Filter to '@'-containing groups only: groups without '@' in the CyberArkGroupName are excluded at list-build time — Vault-only groups are never sent to ADSI; progress counter reflects only groups being queried |
| 2026-08-19 | Invoke-CustomExportGroupMembersLDAP.ps1 | Strip UPN suffix before ADSI lookup: added second regex to remove '@domain.com' after the existing 'DOMAIN\' prefix strip, so sAMAccountName matching works for UPN-format group names (GroupName@domain.com -> GroupName) |
| 2026-08-19 | Invoke-CustomExportGroupMembersLDAP.ps1 | Log LDAP filter at DEBUG level: LDAP query string (&(objectClass=group)(sAMAccountName=...)) is written to log via Write-CyberArkLog -Level DEBUG before each ADSI FindOne call |
| 2026-08-19 | README.md | Updated Features (proactive refresh, safe-scoped lookup, Test API, display limit, default profile, DEBUG body logging); Project Structure Auth/ section (three auth modules); Custom/ comment; Configuration table (added AppName, Username, Limit, DisplayLimit, IsDefault, Role_Template_Safe, Role_Group_Prefix, TenantPortal, TenantVault, TenantAuth) |
| 2026-08-19 | Architecture.md | Added Invoke-CustomTestApi.ps1 to Custom\ folder structure; updated LDAP export comment to note '@-groups only' |
| 2026-08-19 | Lessons-Learned-PowerShell-Pester.md | Added Section 16.4 — CyberArk ISPSS UPN-format group names: groups appear as GroupName@domain.com; sAMAccountName for ADSI lookup is the portion before '@'; only '@'-containing group names represent directory-backed groups worth querying |
| 2026-08-20 | Add-Safe-From-Template-Design.md | Created — design doc for Safes/AddFromTemplate; Decisions D1-D4 resolved (settings copied from template except SafeName/Description; role-group exclusion by name prefix across all memberTypes; membershipExpirationDate never copied; Role_Template_Safe and Role_Group_Prefix both required) |
| 2026-08-20 | Invoke-SafesAddFromTemplate.ps1 | Created new Safes module (v1.0.0, Priority=15): reads template safe settings (GET /API/Safes/{Role_Template_Safe}) and members (GET /API/Safes/{Role_Template_Safe}/Members), excludes members whose name starts with Role_Group_Prefix, creates the new safe (POST /API/Safes) with template settings + fresh SafeName/Description, then copies each retained member (POST /API/Safes/{NewSafe}/Members) with membershipExpirationDate always null; first real consumer of the previously-unused Role_Template_Safe / Role_Group_Prefix profile fields |
| 2026-08-20 | Invoke-SafesAddFromTemplate.Tests.ps1 | Created 24 unit tests (AFT01-AFT24): module metadata, success/role-group-filtering/WhatIf/validation/error branches, all mocking Invoke-CyberArkAPI; all passing |
| 2026-08-20 | Testing-Plan.md | Added Component Test Matrix row for Invoke-SafesAddFromTemplate.ps1; added Invoke-SafesAddFromTemplate.ps1 — Test Cases section (AFT01-AFT24) |
| 2026-08-20 | Architecture.md | Added two Design Decisions rows for Add Safe From Template (role-group exclusion rule; settings-vs-identity field split); noted Role_Template_Safe/Role_Group_Prefix are now consumed by Safes/AddFromTemplate |
| 2026-08-20 | README.md | Corrected Role_Template_Safe / Role_Group_Prefix Configuration-table descriptions (previously described an unbuilt "role-based entitlement export" use case) to match Safes > Add Safe From Template; updated Safes/ project-structure line to list AddFromTemplate |
| 2026-08-20 | Invoke-SafesAddFromTemplate.ps1, Invoke-SafesAdd.ps1, Invoke-SafesUpdate.ps1 | Fixed per user correction: OLACEnabled is not a supported field for the CyberArk Safes API and must never be read, prompted for, or sent - removed entirely from all three modules' InputSchema, Get-*Input prompts, request bodies, and Results objects; NumberOfVersionsRetention and NumberOfDaysRetention are mutually exclusive on this API - only one may be sent (NumberOfDaysRetention wins when its merged/template value is greater than 0, otherwise NumberOfVersionsRetention is sent); bumped all three modules to v1.1.0 |
| 2026-08-20 | Invoke-SafesAddFromTemplate.Tests.ps1, Invoke-SafesAdd.Tests.ps1, Invoke-SafesUpdate.Tests.ps1 | Added tests for the OLACEnabled removal and retention mutual-exclusivity fix (T11a-T11c / A11a-A11c / U05a-U05c); removed stale OLACEnabled entries from $ValidInput fixtures; full Tests\Unit suite: 724 passing, 6 pre-existing unrelated failures in Accounts/SafeMembersList tests (untouched by this change) |
| 2026-08-20 | Testing-Plan.md | Corrected Invoke-SafesAddFromTemplate.ps1 test-case ID prefix from AFT to T (matching the actual test file, which was never renamed); updated T11 and added T11a-T11c |
| 2026-08-20 | Architecture.md | Added two Design Decisions rows: retention fields are mutually exclusive; OLACEnabled is not a supported field. Updated the earlier Add Safe From Template settings-copy row to drop the OLACEnabled/retention wording it started with |
| 2026-08-20 | Add-Safe-From-Template-Design.md | Added Decisions D5-D6 for the OLACEnabled/retention correction; updated algorithm steps 2 and 5; bumped documented Version to 1.1.0 |
| 2026-08-20 | Driver.ps1 | Added $script:ExcludedTemplateMemberNames = @() constant (empty by default) near the other script-level configuration constants - member names never copied by Safes/AddFromTemplate (or any future Safes/SafeMembers module reusing it), exact match, case-insensitive, all memberTypes |
| 2026-08-20 | Invoke-SafesAddFromTemplate.ps1 | Added global exclusion filter (v1.2.0): member list filter now also excludes any member whose name exactly matches (case-insensitive) an entry in $script:ExcludedTemplateMemberNames, in addition to the existing Role_Group_Prefix filter |
| 2026-08-20 | Invoke-SafesAddFromTemplate.Tests.ps1 | Added T09a-T09b covering the global exclusion list (excludes regardless of memberType; exact-match not prefix-match); Reset-MockActiveProfile now also resets $script:ExcludedTemplateMemberNames to @() each test, since tests dot-source only the module and not Driver.ps1 |
| 2026-08-20 | Interfaces.md | Added $script:ExcludedTemplateMemberNames row to the Script-Level Configuration Variables table |
| 2026-08-20 | Architecture.md | Added Design Decisions row explaining why the exclusion list is a Driver.ps1 constant rather than a profile field |
| 2026-08-20 | Testing-Plan.md | Added T09a-T09b to the Invoke-SafesAddFromTemplate.ps1 Test Cases section |
| 2026-08-20 | Add-Safe-From-Template-Design.md | Added Decision D7 for the global exclusion list; updated Section 2 (Driver.ps1 now has one addition) and algorithm step 4; bumped documented Version to 1.2.0 |
| 2026-08-20 | Modules\CyberArkComms.psm1 | Added HTTP 504 (Gateway Timeout) retry handling to Invoke-CyberArkAPI, mirroring the existing 429 retry loop but with a fixed delay instead of exponential backoff: $script:MaxGatewayTimeoutRetries=2 (default), $script:GatewayTimeoutDelaySec=5, both new script-scoped variables; if pagination is in use for the call, the effective page size (limit) is reduced by 25% (floor 1) before each retry since the same offset is retried; added '504' = 'Gateway Timeout' to Get-StatusMessage's status map (was previously unmapped) |
| 2026-08-20 | Interfaces.md | Added $script:MaxGatewayTimeoutRetries and $script:GatewayTimeoutDelaySec rows to the Script-Level Configuration Variables table |
| 2026-08-20 | API-Module-Development-Guide.md | Added a "Gateway timeouts (504)" callout alongside the existing "Rate limiting" callout, documenting the new retry/page-size-reduction behavior and that a persisted 504 is treated as a normal non-fatal failure response |
| 2026-08-20 | Architecture.md | Added Design Decisions row explaining the fixed-delay + page-size-reduction choice for 504 vs. the exponential backoff used for 429 |
| 2026-08-20 | Testing-Plan.md | Noted the new 504 retry loop is not covered by an automated unit test (same HttpWebResponse instantiation limitation as 429) and recommended manual verification |
| 2026-08-20 | Invoke-SafeMembersAdd.ps1 | Added interactive SearchIn picker (v1.3.0): new script:Get-SafeMembersSearchInOptions helper calls GET /API/Configuration/LDAP/Directories (GetDirectoryServices) and returns Vault plus one entry per directory returned; Get-SafeMembersAddInput now shows a numbered menu instead of a free-text prompt for SearchIn. CSV/bulk input (InputSchema) and the core Invoke-SafeMembersAdd body-building logic are unchanged - this only affects the interactive single-item flow. Response field names for GetDirectoryServices are unconfirmed against a live system - the helper defensively probes id/domainName/directoryName/name and falls back to Vault-only on any failure (never blocks the Add Safe Member flow); also fixed: this file was missing its required UTF-8 BOM (pre-existing, unrelated to this change but fixed while editing) |
| 2026-08-20 | Invoke-SafeMembersAdd.Tests.ps1 | Added MA23-MA28 covering Get-SafeMembersSearchInOptions: Vault always first, GetDirectoryServices failure/exception falls back to Vault-only, bare-array and value-wrapped response shapes, and an item missing all probed ID fields is skipped rather than throwing |
| 2026-08-20 | Lessons-Learned-PowerShell-Pester.md | Added Section 12.2 flagging that GetDirectoryServices' response shape is unverified against a live system, documenting the defensive probe applied and the follow-up needed once confirmed |
| 2026-08-20 | Architecture.md | Added Design Decisions row for the SearchIn picker (Vault-first, directories from GetDirectoryServices, fallback-on-failure, unconfirmed field names) |
| 2026-08-20 | (repo-wide, not fixed) | Discovered 16 pre-existing .ps1 files missing their required UTF-8 BOM while auditing after fixing Invoke-SafeMembersAdd.ps1's encoding: 15 in APIModules\Accounts\ and Invoke-CustomTestApi.ps1. Flagged to the user; not fixed as part of this change since it's unrelated in scope - listed here so it isn't lost |
| 2026-08-20 | Invoke-SafeMembersAdd.ps1 | Fixed [FATAL] PropertyNotFoundException on 'Count' at Get-SafeMembersAddInput (reported from a live Driver.ps1 run): $searchInOptions = script:Get-SafeMembersSearchInOptions ... was not wrapped in @(), so the Vault-only fallback (single-item array) unwrapped to a bare PSCustomObject under PS 5.1's array-unwrap behavior, and .Count threw under strict mode. Fixed with [array]$searchInOptions = @(script:Get-SafeMembersSearchInOptions -Token $Token). Not caught by the MA23-MA28 unit tests added for this feature because they call the helper directly under pwsh (this session's tooling), which has a synthetic Count property on scalars that PS 5.1 lacks - see Lessons-Learned Section 4.3 for the takeaway |
| 2026-08-20 | Lessons-Learned-PowerShell-Pester.md | Added a "Real-world instance" callout to Section 4.3 documenting this exact incident and the pwsh-vs-PS5.1 testing gap that let it through |
| 2026-08-20 | Invoke-SafeMembersAddFromTemplateRole.ps1 | Created new SafeMembers module (v1.0.0, Priority=24): interactive/CSV input matches Invoke-SafeMembersAdd.ps1 (SafeName, MemberName, SearchIn, MemberType, ExpirationDate) except individual permission columns/PermissionRole are replaced by a required RoleName; permissions are resolved by reading the profile's Role_Template_Safe members (GET /API/Safes/{template}/Members), filtering to members whose name starts with Role_Group_Prefix (the opposite filter from Safes/AddFromTemplate), and matching RoleName exactly (case-insensitive) among those; the matched role's permissions object is copied verbatim into the POST /API/Safes/{safe}/Members body. Interactive mode shows role options as a numbered Index/Name menu (script:Get-TemplateRoleOptions), falling back to free-text entry if no roles are found. Duplicates script:Get-SafeMembersSearchInOptions from Invoke-SafeMembersAdd.ps1 rather than sharing it, consistent with this codebase's existing per-file duplication of script:Get-PermissionSet |
| 2026-08-20 | Invoke-SafeMembersUpdateFromTemplateRole.ps1 | Created new SafeMembers module (v1.0.0, Priority=25): same template-role resolution as the Add variant above, but mirrors Invoke-SafeMembersUpdate.ps1's shape (SafeName, MemberName, RoleName, ExpirationDate only - no SearchIn/MemberType, matching Update's existing scope) and PUTs to /API/Safes/{safe}/Members/{member} with { membershipExpirationDate; permissions } |
| 2026-08-20 | Invoke-SafeMembersAddFromTemplateRole.Tests.ps1, Invoke-SafeMembersUpdateFromTemplateRole.Tests.ps1 | Created 44 unit tests (ATR01-ATR23, UTR01-UTR21): module metadata (confirms no individual-permission InputSchema columns), success/permission-copy/WhatIf/validation/error branches including role-not-found and role-exists-but-not-role-prefixed cases; all passing. Full suite: 782 total, 776 passing, same 6 pre-existing unrelated failures |
| 2026-08-20 | README.md | Updated SafeMembers/ project-structure line to list AddFromTemplateRole, UpdateFromTemplateRole |
| 2026-08-20 | Architecture.md | Added Design Decisions row for the two template-role actions, noting $script:ExcludedTemplateMemberNames does NOT apply to role resolution (different concern from member-copy exclusion) |
| 2026-08-20 | Testing-Plan.md | Added both new modules to the Component Test Matrix |
| 2026-08-20 | Driver.ps1 | Fixed 401 handling: both places that called Invoke-TokenInvalidate on IsFatal (CSV-loop and single-item paths) previously gated the call on regex-matching the error message text for "401"/"Unauthorized" - a genuine 401 worded differently would silently skip invalidation, leaving a rejected token in place. Now invalidates unconditionally on IsFatal (which every module already restricts to 401 or network-error StatusCode 0, per the IsFatal table in API-Module-Development-Guide.md) |
| 2026-08-20 | Driver.ps1, Auth\CyberArk.Auth.Common.psm1 | Added logon-phase token age check: New-AuthTokenObject gained an optional Created (UTC) parameter defaulting to now (every fresh-auth/refresh call site is unaffected, since none pass it explicitly); Import-AuthToken now passes -Created from the persisted SavedAt field (falls back to now if absent, e.g. pre-existing .cred files) instead of dropping it. Added $script:LogonTokenMaxAgeMin=15 to Driver.ps1; in the profile Connect ('C') flow's 'TokenStatus -eq Valid' branch, a loaded token whose Created is more than 15 minutes old is now refreshed (Update-ISPSSAuthToken/Update-SelfHostedAuthToken) before the session starts, rather than being used as-is just because it isn't expired yet |
| 2026-08-20 | Interfaces.md | Added Created field to the Token Object shape; added $script:LogonTokenMaxAgeMin to the Script-Level Configuration Variables table |
| 2026-08-20 | Architecture.md | Added two Design Decisions rows: unconditional 401 invalidation (and why the network-error side effect is acceptable); logon-phase token age check rationale |
| 2026-08-20 | Testing-Plan.md | Added A13 and D19-D22 manual integration test procedures for both changes (no unit test coverage - Driver.ps1 session/connect flow and the Auth modules are interactive/UI, per existing testing boundaries) |
| 2026-08-20 | README.md | Updated the "Proactive token refresh" Features bullet to mention the new logon-phase 15-minute age check, alongside the existing near-expiry refresh description |
| 2026-08-21 | Driver.ps1 -> Manage-Privilege.ps1 | Renamed the entry-point script (git mv, history preserved). Fixed two self-referential strings inside the file (prerequisite-check message, top-level FATAL error message). Renamed Tests\Unit\Driver.Tests.ps1 -> Manage-Privilege.Tests.ps1 to match, updated its dot-source path and Describe block titles, and fixed a pre-existing filename mismatch in Testing-Plan.md's Component Test Matrix (it referenced a nonexistent Driver.Profile.Tests.ps1). Bulk-updated every "Driver.ps1" reference across README.md, Architecture.md, Interfaces.md, Testing-Plan.md, API-Module-Development-Guide.md, Lessons-Learned-PowerShell-Pester.md, Add-Safe-From-Template-Design.md, and 32 test files' boilerplate comments, plus Invoke-SafesAddFromTemplate.ps1's one in-code comment. Historical dated entries in this Revision Log are left as-is (they describe what was true at the time); only this entry and the "current state" rows above (e.g. Module References) were updated |
| 2026-08-21 | Invoke-AccountsResumeAutoManagement.ps1 | Fixed per user correction (v1.2.0): the API endpoint is `/API/Accounts/{id}/Resume`, not `/API/Accounts/{id}/ResumeAutoManagement`, and it does not accept or use a request body - removed the Reason field entirely (InputSchema, interactive prompt, and the now-dead body construction) and dropped `-Body` from the POST call. Also fixed: this file was missing its required UTF-8 BOM (pre-existing, one of the 16 flagged 2026-08-20; fixed while editing) |
| 2026-08-21 | Invoke-AccountsResumeAutoManagement.Tests.ps1 | Added a test pinning the exact endpoint (`/API/Accounts/{id}/Resume`) and asserting no `Body` parameter is passed to `Invoke-CyberArkAPI`, so this can't regress silently |
| 2026-08-21 | Invoke-AccountsResumeAutoManagement.ps1 | Superseded the same-day `/Resume` no-body fix per further user correction (v1.3.0): the correct call is `PATCH /API/Accounts/{id}/` with a JSON Patch (RFC 6902) body - `[{"op":"replace","path":"/secretManagement/automaticManagementEnabled","value":"true"},{"op":"replace","path":"/secretManagement/manualManagementReason","value":""}]` - built as an ordered array of hashtables (array order matters for JSON Patch; object key order does not). `value: "true"` is sent as the literal string given, not a boolean, matching the exact payload specified |
| 2026-08-21 | Invoke-AccountsResumeAutoManagement.Tests.ps1 | Replaced the `/Resume` no-body test with one asserting `Method=PATCH`, `Endpoint=/API/Accounts/{id}/`, and the two-element JSON Patch body content/order |
| 2026-08-25 | Invoke-SafesAssignCPM.ps1, Invoke-SafesUnassignCPM.ps1 (new) | Added Assign/Unassign CPM actions to the Safes category. Both use the same GET-then-PUT pattern as Invoke-SafesUpdate.ps1 (the Safes PUT is a full replace, so every other field is carried forward from the GET response unchanged) and set only `ManagingCPM` - Assign to the input value, Unassign to `''`. Assign's interactive input adds a CPM picker: queries `GET /API/Users?userType=CPM&componentUser=true` - confirmed against the bundled `Swagger\CyberArk_PasswordVault_Swagger_14.6.v1.json`, which documents both as server-side query parameters on `/api/Users` and lists "CPM" as one of the userType values considered a component user. Falls back to free-text entry if the query returns nothing or the API call fails. CSV/bulk input bypasses the picker entirely (ManagingCPM is a direct required column on Assign) |
| 2026-08-25 | Invoke-SafesAssignCPM.Tests.ps1, Invoke-SafesUnassignCPM.Tests.ps1 (new) | Unit tests for both new modules (GET/PUT sequencing, body construction, WhatIf, validation, GET/PUT-phase error handling) plus dedicated coverage of the CPM query filter and username extraction (`script:Get-SafesCPMOptions`) in the Assign test file |
| 2026-08-25 | README.md | Updated the Safes/ project-structure line to list AssignCPM, UnassignCPM |
| 2026-08-25 | Invoke-AccountsGet.ps1 (v1.2.0) | Flattened the nested AccountModel fields that Get Account was previously dropping, confirmed against the bundled Swagger spec's AccountModel/AutomaticSecretManagement/RemoteMachinesAccess definitions: secretManagement gained LastCPMModified/LastReconciled/LastVerified (lastModifiedTime/lastReconciledTime/lastVerifiedTime, alongside the already-mapped AutoManaged/CPMStatus/ManualReason); remoteMachinesAccess is now mapped to RemoteMachines/RemoteAccessRestricted; categoryModificationTime/deletionTime are now mapped to CategoryModified/Deleted; platformAccountProperties (a free-form per-platform object, e.g. LogonDomain/Port) is flattened onto dynamic `Platform_<Key>` columns. `secret` is deliberately never surfaced - the live API doesn't return it from this endpoint anyway, and writing credentials into a plaintext CSV export by default would be a security hazard regardless. Also fixed while in here: `id`/`name`/`address`/`userName`/`secretType` are only `platformId`/`safeName`-required per the Swagger schema, but were being accessed unguarded - a real account missing one would have thrown under the driver's `Set-StrictMode -Version Latest`. Discovered via new test fixtures that omitted `address`, so guarded these the same way the nested fields already were. Also fixed: this file was missing its required UTF-8 BOM (pre-existing; fixed while editing) |
| 2026-08-25 | Invoke-AccountsGet.Tests.ps1 | Extended the sample account fixture with categoryModificationTime, deletionTime, remoteMachinesAccess, and platformAccountProperties; added tests for every new flattened column, for `secret` never appearing in the result, and for missing-optional-field safety |
| 2026-08-25 | Invoke-AccountsList.ps1 (v1.3.0) | Applied the same AccountModel flattening as Invoke-AccountsGet.ps1's v1.2.0 change to `script:Add-AccountToResult` (the shared per-account mapper used by both the Normal and By-Safe retrieval paths): ManualReason/LastCPMModified/LastReconciled/LastVerified from secretManagement, RemoteMachines/RemoteAccessRestricted from remoteMachinesAccess, CategoryModified/Deleted from categoryModificationTime/deletionTime, and platformAccountProperties flattened onto dynamic `Platform_<Key>` columns (secret still never surfaced). Also added `script:Add-PlatformColumnParity`, called once at the end of each retrieval path before returning: since platformAccountProperties keys vary per platform and Export-Csv in PS 5.1 only reads column headers from the first row, a platform key that only appeared on a later account would otherwise silently vanish from the CSV for every row. It backfills every row with an empty-string value for any `Platform_*` column present on at least one other row, so the full column union is present starting from row 1 |
| 2026-08-25 | Invoke-AccountsList.Tests.ps1 | Extended the sample account fixture the same way as Invoke-AccountsGet.Tests.ps1; added tests for every new flattened column, for `secret` never appearing in the result, and a dedicated test asserting two accounts with non-overlapping platform properties both end up with the full column union (`script:Add-PlatformColumnParity`) |
| 2026-08-25 | Invoke-AccountsGetCredential.ps1 (v1.1.0) | Added the same AccountID-or-search-by-AccountName+Safe fallback already used by Invoke-AccountsGet.ps1, Invoke-AccountsUnlock.ps1, and most other single-account Accounts actions: InputSchema changed from AccountID (Required) to AccountName+Safe (Required, matching that convention) with Reason unchanged; the interactive input gained the same "blank ID -> Invoke-EntitySearch picker" flow as Invoke-AccountsUnlockInput. `Invoke-AccountsGetCredential` now resolves AccountID from AccountName+Safe via the same `GET /API/Accounts?filter=safeName eq {Safe}` lookup-and-locally-match pattern as the other modules when AccountID is blank. Also fixed while in here: `$InputData.Reason` (dot notation) threw `PropertyNotFoundException` under the driver's `Set-StrictMode` whenever the optional Reason column was absent from InputData - a real, previously-untested case now exercised by the new AccountName+Safe tests, none of which pass a Reason key. Changed to `$InputData['Reason']` (bracket notation is safe on hashtables for missing keys even under strict mode) |
| 2026-08-25 | Invoke-AccountsGetCredential.Tests.ps1 | Added a `New-AccountLookupResponse` factory and a new "resolve by AccountName/Safe" Describe block (AC19-AC24) covering: successful two-call resolve-then-retrieve, all-fields-blank validation, AccountName-without-Safe validation, lookup-phase 401 (IsFatal, no retrieve attempted), no-match-found, and multiple-matches-uses-first. Updated the file's Test IDs range comment from AC01-AC18 to AC01-AC24 |
| 2026-08-25 | Invoke-CustomExportAll.ps1 | Fixed per user report: a relative profile `OutputFolder` (e.g. `Output`) resolved against `$PSScriptRoot`, which inside this file is `APIModules\Custom` (where PowerShell lexically defines it) - NOT the project root, even though Manage-Privilege.ps1 dot-sources this file into its own scope. Files were landing in `APIModules\Custom\Output` instead of the project-root `Output` the profile intended. Now resolves via `$script:APIModulesPath`'s parent (set by Manage-Privilege.ps1 before any module runs), the same project root every other save-to-CSV path already uses, with a `(Get-Location).Path` fallback if that variable isn't set (e.g. an isolated test context). The other three Custom export modules (ExportEntitlements, ExportGroupMembersLocal, ExportGroupMembersLDAP) don't self-manage a save path at all - they return `Results` and rely on the driver's own correctly-rooted "Save to CSV?" prompt - so only this file had the bug. `Invoke-CustomTestApi.ps1` resolves relative paths against `(Get-Location).Path` instead, a different (pre-existing, untouched) approach not exhibiting this symptom |
| 2026-08-25 | Invoke-CustomExportAll.Tests.ps1 | Added a "Relative OutputFolder resolution" Context: points `$script:APIModulesPath` at a temp directory, sets a relative `OutputFolder` on `$script:ActiveProfile`, and asserts the saved CSV lands under `<TempRoot>\Output\...`, not anywhere containing `APIModules`. Noted in passing: the pre-existing "List module returns results" test has no path mocking and writes a real `Export_DataCategoryList.csv` into the repo root on every run (via the `(Get-Location).Path` fallback, since `$script:ActiveProfile` is null there) - left as-is since it predates and is unrelated to this fix, but the stray file it produces was deleted from the working tree after each local test run |
| 2026-08-25 | Manage-Privilege.ps1 (new `Invoke-FileWriteWithRetry`), Invoke-CustomExportAll.ps1, Invoke-CustomTestApi.ps1 | Per user request: added a retry prompt to every report/export file write, so a file locked in another program (most commonly Excel) no longer silently discards data the user would otherwise have to regenerate by re-running the whole report. New driver helper `Invoke-FileWriteWithRetry` (placed beside `Get-CsvSavePath`, `Confirm-Action`, `Show-FieldPrompt` - the existing "driver helpers available because this module is dot-sourced into scope" group) wraps a write in `while ($true) { try { & $Action; return $true } catch { ...; if (-not (Confirm-Action ...)) { return $false } } }`. Wired into all 4 report-output write sites: `Invoke-CsvProcessing`'s CSV-bulk-loop output (Manage-Privilege.ps1), the interactive single-run "Save results to CSV?" path (Manage-Privilege.ps1), Export All's per-module CSV save (Invoke-CustomExportAll.ps1), and Test API's JSON save (Invoke-CustomTestApi.ps1). Deliberately scoped to these 4 - not the profile-save (`Set-Content`) or log-truncation (`CyberArkLogging.psm1`) writes, which aren't "a report" in the sense the user meant and carry a different risk profile (profile save is cheap to retry manually; a blocking `Read-Host` prompt buried in logging internals could hang a non-interactive code path) |
| 2026-08-25 | Tests/Unit/Invoke-CustomExportAll.Tests.ps1 | Added a `global:Invoke-FileWriteWithRetry` stub (runs Action once, returns success/failure) alongside the existing `Get-CsvSavePath` stub, and a test confirming a `SaveFailed` (not `Saved`) row when it reports failure |
| 2026-08-25 | Tests/Unit/Manage-Privilege.Tests.ps1 | `Invoke-FileWriteWithRetry` is deliberately NOT unit-tested. Per this project's established testing boundary (Testing-Plan.md), `Read-Host`-driven interactive helpers (via `Confirm-Action`) aren't unit tested. Beyond that: empirically confirmed that appending any Describe block calling it after the existing DP01-DP17 tests in this specific file reproducibly hangs under Pester v6.1 - even the non-throwing, no-retry-needed case - while the exact same function, dot-sourced and called directly from a plain pwsh session with no Pester involved, returns correctly with no hang (verified via a background job with a timeout). This is a Pester/file interaction issue in the spirit of Pester issue #2669 (which this file's `BeforeAll`-placement already works around once, per "Pester v6 Test File Structure" in Lessons-Learned-PowerShell-Pester.md), not a defect in the function itself. A comment documenting this (and the direct-call verification) was left in the test file in place of a test block |
| 2026-08-25 | Manage-Privilege.ps1 | Added a new profile field `CPM_List` (comma-separated CPM usernames, per user request) with the same 4 touchpoints as `Role_Template_Safe`/`Role_Group_Prefix`: `New-BlankProfile`, the `Get-AllDriverProfiles` normalization array (backfills `''` on older saved profiles missing it), `Show-ProfileDetail`, and `Invoke-ProfileEditFlow`. Gives pages that ask for a CPM a picker sourced from a manually-maintained list instead of requiring the user to remember or type CPM usernames |
| 2026-08-25 | Invoke-SafesAddFromTemplate.ps1 (v1.3.0) | Two additions to Add Safe From Template, both interactive-mode prompts plus full CSV/bulk column support (see `Docs\Add-Safe-From-Template-Design.md` Decisions D8-D10 for the full rationale): (1) `ManagingCPM` is no longer copied from the template safe - interactive mode shows a numbered picker built from the profile's new `CPM_List` (entry 1 = "(none)", the default), falling back to free-text entry if the list is empty; CSV/bulk mode gets a plain optional `ManagingCPM` column, blank = none. (2) A new "additional members" feature: interactive mode loops a Type (User/Group) + Name + Role prompt ("add another member?") until declined; CSV/bulk mode gets an optional `ExtraMembers` column, semicolon-separated `Type:Name:RoleName` triples. The Role picker/resolution reuses the exact mechanism from `SafeMembers/AddFromTemplateRole.ps1` (`script:Get-TemplateRoleOptions`, duplicated per this codebase's established per-file-standalone convention) - members of `Role_Template_Safe` whose name starts with `Role_Group_Prefix`, permissions copied verbatim from the matched role member. Extra members reuse `$templateMembers` already fetched for the template-copy step rather than a second API call. Malformed `ExtraMembers` entries and unresolvable `RoleName`s are recorded as individual non-fatal errors and skipped - they never block safe creation or the other members. Added a `RoleName` column to every `Results` row (blank except for extra members) to keep the row schema consistent across `ItemType`s, matching this file's existing full-schema-per-row convention |
| 2026-08-25 | Invoke-SafesAddFromTemplate.Tests.ps1 | Updated T11 (ManagingCPM default changed from "copied from template" to blank); added T06a/T06b (new InputSchema columns), T11d/T11e (ManagingCPM from InputData), a new "ExtraMembers" Describe block (T25-T29: valid entry, multiple entries, malformed entry, unresolvable RoleName, blank), and T15a (WhatIf preview includes extra members). Updated the shared `Invoke-DefaultRouting` mock's `POST /API/Safes` branch to echo back the submitted `ManagingCPM` (previously a fixed fixture value regardless of input) so tests can actually assert on it |
| 2026-08-25 | Manage-Privilege.Tests.ps1 | Added DP04a (`CPM_List` defaults to `''`) and DP11a (`Get-AllDriverProfiles` backfills `CPM_List` on an older profile saved without it) to the existing, already-safe `New-BlankProfile`/persistence Describe blocks - not a new Describe block, per the Pester hang risk documented above |
| 2026-08-25 | Docs\Interfaces.md, Docs\Architecture.md, Docs\Add-Safe-From-Template-Design.md, README.md | Documented `CPM_List` (Interfaces.md field reference + example JSON, README.md Configuration table); added 3 new Architecture.md Design Decisions rows (profile CPM list, CPM prompt replacing auto-copy, additional-members feature) and updated the existing "settings vs. identity fields" row to remove `ManagingCPM`; added Decisions D8-D10, updated the algorithm/ModuleMeta/Profile-Field-Consumption sections, and added a revision-log entry to Add-Safe-From-Template-Design.md |
| 2026-08-25 | Manage-Privilege.ps1 ("Generate Template" option), Invoke-SafesAddFromTemplate.ps1 (v1.3.1) | Per user request: the CSV template generator (`Invoke-ActionModule`'s `[3] Generate Template` menu option) now writes a second row of example values beneath the header, when the module's `InputSchema` defines any - blank for columns that don't. Purely additive: `InputSchema` entries are hashtables, so presence is checked with `.ContainsKey('Example')` (dot-notation would throw under strict mode for the many entries across other modules that don't define it), and modules with no `Example` values anywhere in their schema get the exact same single-header-row template as before (verified directly against `Invoke-SafesAdd.ps1`, which defines none). Added `Example` values to all 4 of `Invoke-SafesAddFromTemplate.ps1`'s `InputSchema` columns - `ExtraMembers`'s Example (`User:jdoe:Role_Viewer;Group:AdminsGroup:Role_Admin`) is the one that actually motivated this: the `Type:Name:RoleName;...` syntax is far easier to grasp from a worked example than from the column's prose description alone |
| 2026-08-25 | Invoke-SafesAddFromTemplate.Tests.ps1 | Added T06c asserting the `ExtraMembers` Example value matches the documented `Type:Name:RoleName` syntax. The template-generation code itself lives in `Invoke-ActionModule` (`Manage-Privilege.ps1`'s interactive session loop) and was not unit-tested for the same reason as `Invoke-FileWriteWithRetry` in the previous entry - verified directly instead, via a backgrounded job with a timeout: confirmed the header+example-row output for `Invoke-SafesAddFromTemplate.ps1` (2 lines, example row showing the real `ExtraMembers` syntax) and the header-only output for `Invoke-SafesAdd.ps1` (1 line, unaffected) |
| 2026-08-25 | Invoke-SafesAddFromTemplate.ps1 (v1.3.2) | Fixed a real production crash reported directly by the user: `[FATAL] PropertyNotFoundException: The property 'Count' cannot be found on this object` at `Get-SafesAddFromTemplateInput` line 123 (the new CPM picker). Root cause: `[array]$cpmList = if (cond) {@(...)} else {@()}` - PowerShell unrolls a script block's output, so the empty `@()` in the `else` branch emits zero objects, which collapses to `$null` on capture despite the `[array]` type constraint; `$null.Count` then throws under `Set-StrictMode`, always active in real usage via `Manage-Privilege.ps1`. Full writeup in `Lessons-Learned-PowerShell-Pester.md` sections 9.8-9.9. Extracted the CPM-list logic into a new `script:Get-ProfileCPMOptions` helper (testable in isolation, matching the existing `script:Get-TemplateRoleOptions` pattern) with the fix (`@(if (cond) {...})` - wrap the *whole* expression, not just a branch). Auditing the same file for the identical pattern (an Explore-agent sweep of the whole codebase found no other unguarded instances) turned up two more real, unguarded instances - `$templateMembers` and `$excludedNames` in `Invoke-SafesAddFromTemplate` itself, both `.Count`-accessed a few lines later - fixed the same way. Also fixed while auditing the same result-row-construction block: `$safeBody.NumberOfVersionsRetention`/`$safeBody.NumberOfDaysRetention` accessed via dot notation on a hashtable where only one of the two keys is ever present (they're mutually exclusive) - this made **WhatIf mode crash unconditionally, every time**, in real usage (the WhatIf preview row had no guard at all); and `$createdSafe.autoPurgeEnabled` accessed without the `PSObject.Properties[...]` guard every sibling field in the same object literal already had. None of these were caught by any existing test, because (see 9.9) this test file never runs under `Set-StrictMode` unless a test opts in explicitly |
| 2026-08-25 | Invoke-SafesAddFromTemplate.Tests.ps1 | Added a new "array-collapse regression (strict mode)" Describe block (T30-T35): each test calls `Set-StrictMode -Version Latest` itself (scoped to that `It`, confirmed not to leak to siblings) since the file's `BeforeAll` doesn't dot-source `Manage-Privilege.ps1`. T30/T31 exercise the two `Invoke-SafesAddFromTemplate`-internal fixes; T32-T35 test `script:Get-ProfileCPMOptions` directly. Added the same `Set-StrictMode -Version Latest` to the pre-existing WhatIf Describe's `BeforeEach` (the block that hid the unconditional retention-field crash) - T13-T15a now genuinely verify strict-mode safety instead of silently passing regardless. Debugging T31 itself hit the identical bug a third time, this time in the *test's own assertion*: `($r.Results | Where-Object {...}).Count` with zero matches - fixed by wrapping with `@(...)`, same as everywhere else |
| 2026-08-25 | Docs\Lessons-Learned-PowerShell-Pester.md | Added sections 9.8 (`[array]$x = if(){@()}else{@()}` collapses to `$null`, not an empty array - wrong/correct code, plus the inline-pipeline variant of the same bug) and 9.9 (unit tests for individual API modules don't run under `Set-StrictMode`, which is why 9.8 shipped and passed every test; flags that the "N pre-existing unrelated failures, confirmed via `git stash`" reasoning used throughout prior sessions correctly identified those failures as pre-existing but likely mischaracterized *why* - fixing this file's instances of the bug made every one of its full-suite-only failures disappear, suggesting the same bug class may be the real cause in `Invoke-SafesAdd.ps1`, `Invoke-SafesUpdate.ps1`, `Invoke-AccountsList.ps1`, and others still showing that signature. Not yet verified file-by-file - flagged as a hypothesis for follow-up, not a completed audit) |
| 2026-08-25 | Invoke-SafesAdd.ps1 (v1.1.1), Invoke-SafesUpdate.ps1 (v1.1.1) | Follow-up audit (user approved) of the 9.9 hypothesis above, one file per parallel investigation. Both confirmed **real production bugs**, both Pattern C - the identical `$body.NumberOfVersionsRetention`/`$body.NumberOfDaysRetention` mutually-exclusive-hashtable-key dot-notation shape already fixed in `Invoke-SafesAddFromTemplate.ps1`. In `Invoke-SafesAdd.ps1` this hit both the `WhatIf` branch and the success-result-mapping branch (2 instances); in `Invoke-SafesUpdate.ps1`, the `WhatIf` branch only (1 instance). Both meant **WhatIf mode crashed unconditionally, every time**, in real usage - the same severity as the original AddFromTemplate finding. Fixed with the same `ContainsKey`/bracket-notation pattern, comments referencing Pattern C and `Invoke-SafesAddFromTemplate.ps1`. Verified via Pester (isolated, strict mode added to `BeforeAll`/`BeforeEach`) reproducing the exact reported-style exception at the exact production line before the fix, 100% passing after; and independently via a `Start-Job`-based direct invocation dot-sourcing the real `Manage-Privilege.ps1` (genuine strict mode, no Pester involved) before and after |
| 2026-08-25 | Invoke-SafesAdd.Tests.ps1, Invoke-SafesUpdate.Tests.ps1 | Added `Set-StrictMode -Version Latest` to every `BeforeAll`/`BeforeEach` block so these files' tests actually exercise strict-mode safety going forward, matching the pattern already applied to `Invoke-SafesAddFromTemplate.Tests.ps1` |
| 2026-08-25 | Invoke-SafeMembersList.ps1 (v1.1.1) | Same follow-up audit; found a **different real production bug**, not the 9.8/9.9 array-collapse class: a genuine logic/bookkeeping gap introduced by an earlier refactor (commit `0c1bef5`) - the per-safe API-failure branch inside the "list all safes" loop never recorded the failure on `$result` (no `Errors.Add`/`Failures++`/`ItemsProcessed++`) the way the equivalent top-level all-safes-lookup-failure branch a few lines above it already did, so a per-safe 403 silently vanished and a per-safe 401 returned `IsFatal=$true` with zero `Errors` explaining why. Fixed by mirroring the existing top-level branch exactly. Also hardened one latent (not-yet-crashing, since only consumed via `foreach`) Pattern A instance (`$members`) found while in there, matching the established fix |
| 2026-08-25 | Invoke-SafeMembersList.Tests.ps1 | Two of the four full-suite-only-failing tests (`ML16`/`ML17`) turned out to be stale, not strict-mode-related: they asserted a pre-refactor "SafeName is required" validation the module intentionally no longer has (confirmed against `$ModuleMeta.InputSchema` and `Get-SafeMembersListInput`'s own prompt text, both documenting "leave blank for all safes"). Rewrote both to exercise the real, current "blank SafeName -> GET /API/Safes -> members per safe" flow, adding a `New-SafesListApiResponse` mock factory. Added `Set-StrictMode -Version Latest` to all `BeforeEach` blocks |
| 2026-08-25 | Invoke-AccountsList.Tests.ps1 | Same follow-up audit; found **no production bug** - `Invoke-AccountsList.ps1`'s `$ignoreSSL = if ($script:ActiveProfile) {...}` line is legitimate production code (`Manage-Privilege.ps1` always initializes `$script:ActiveProfile` before any module runs), but the test file's `BeforeAll`/`BeforeEach` never assigned that script-scoped variable at all, so under strict mode it threw `RuntimeException: variable has not been set` on literally the first line of the function - explaining why ~19 of the file's ~25 tests failed together (everything that calls the function) while `ModuleMeta` (which doesn't) was unaffected. Fixed test-only: added a `Reset-MockActiveProfile`-style helper (matching `Invoke-SafesAddFromTemplate.Tests.ps1`'s existing pattern) setting `$script:ActiveProfile = [PSCustomObject]@{ IgnoreSSL = $false }` in `BeforeEach`, plus `Set-StrictMode -Version Latest`. `Invoke-AccountsList.ps1` itself was not touched - confirmed via `git diff --stat` |
| 2026-08-25 | Invoke-AccountsLinkAccount.ps1 (v1.2.1), Invoke-AccountsLinkAccount.Tests.ps1 | Same follow-up audit; found **no strict-mode bug at all** for the one failing test - it used the wrong `InputData` hashtable keys (`Name`/`Safe` instead of the `InputSchema`-documented `LinkName`/`LinkSafe`), so `$InputData['LinkName']` correctly returned `$null` (bracket access on an absent key never throws) and production correctly rejected the call with "LinkName is required" - the test was exercising the validation-failure path while asserting success-path expectations. Reproduced identically with or without strict mode added, confirming it predates and is unrelated to 9.8/9.9. Fixed the test's `InputData` keys. While investigating, found and hardened two latent (currently-safe, not yet crashing) Pattern A instances in the production file (`Search-LinkedAccount`'s `$accounts`, and the equivalent block in `Invoke-AccountsLinkAccount` itself) for consistency with the rest of the audited codebase; also fixed a missing UTF-8 BOM on the production file (pre-existing gap, confirmed via `git show HEAD` to predate this session) |
| 2026-08-25 | Docs\Lessons-Learned-PowerShell-Pester.md | Closed out section 9.9's "flagged as a hypothesis for follow-up" note with the audit results above: the full suite went from 44 failing to 1 (the pre-existing, separately-diagnosed `AG06` InputSchema mismatch in `Invoke-AccountsGetCredential.Tests.ps1`, unrelated to any of this). Confirms "fails only in the full suite" is a real signal worth investigating with strict mode, but explicitly does NOT confirm every such failure is the same 9.8 array-collapse bug - two of the five files had a different real bug, and two had no production bug at all (test-only gaps) |
| 2026-08-25 | Invoke-SafesAddFromTemplate.ps1 (v1.4.0) | Per user request, two changes to the interactive additional-members flow (Decisions D11-D12 in Add-Safe-From-Template-Design.md): (1) Collapsed the two-gate loop ("Add additional members? [y/N]" before, "Add another member? [y/N]" after each one) into a single recurring "Additional Member" name prompt defaulting to blank - accepting the blank default is both "no (more) members" and the loop's exit condition, since re-prompting for the next name already implicitly asks "want to add another." `MemberName` moved to be asked first each iteration (was Type previously), since it's now also the exit check. (2) `script:Get-TemplateRoleOptions` now also queries `GET /API/UserGroups` (the same endpoint/response shape as `Invoke-GroupsList.ps1` - `.value` array with `groupName`/`description`) and matches each role's name to a group's `description`, shown in the picker as `"1 = RoleName - Description"`. `search=Role_Group_Prefix` narrows the query server-side as a payload hint only; matching is always done client-side by exact `groupName` (case-insensitive), same defensive pattern as `RoleName` resolution elsewhere. Best-effort and non-blocking throughout: the group lookup is skipped entirely when there are zero roles, and any failure (API error, no matching group) just leaves `Description` blank rather than affecting role usability |
| 2026-08-25 | Invoke-SafesAddFromTemplate.Tests.ps1 | Added a new `script:Get-TemplateRoleOptions - role descriptions` Describe block (T36-T40, all with `Set-StrictMode -Version Latest`): description populated on match, blank when the matched group has no description, blank when no group matches, blank (no throw) when the `GET /API/UserGroups` call fails, and confirming that call is skipped entirely when there are zero role options. The loop restructuring itself lives in `Get-SafesAddFromTemplateInput`, not unit-tested for the established `Show-FieldPrompt` dependency reason - verified directly via a `Start-Job` invocation under real strict mode confirming `script:Get-TemplateRoleOptions`'s description lookup behaves correctly end to end |
| 2026-08-25 | Invoke-SafesAddFromTemplate.ps1 (v1.4.1) | Per user request: the template-role picker's description moved from an inline suffix (`"1 = RoleName - Description"`) to its own indented line(s) beneath the role name. Added a new `script:Get-DescriptionDisplayLines` helper that splits a description on any embedded `\r\n`/`\n`/`\r`, trims each resulting line, and drops blank/whitespace-only lines - so a multi-line group description (CyberArk descriptions are free text and can contain real line breaks) gets every line indented under the role instead of only the first line lining up with `Write-Host`'s single-shot indent. Returns an empty array (never `$null`, never throws) for a blank/whitespace-only/missing description |
| 2026-08-25 | Invoke-SafesAddFromTemplate.Tests.ps1 | Added a new `script:Get-DescriptionDisplayLines` Describe block (T41-T44, all with `Set-StrictMode -Version Latest`): splits CRLF/LF/mixed line breaks into separate trimmed lines, drops blank lines (including whitespace-only) without leaving gaps, returns an empty array for blank/whitespace-only/null input, and returns a single trimmed line for a single-line description. Full suite re-verified at 55/55 in isolation; the display loop itself remains unit-tested only indirectly via this helper, per the established `Get-SafesAddFromTemplateInput`/`Show-FieldPrompt` exemption |
| 2026-08-26 | Invoke-AccountsUpdate.ps1 (v1.2.0) | Per user request: switched Update Account from the old GET-current-account + full-body PUT merge to a JSON Patch (RFC 6902) PATCH call, matching the current CyberArk `/API/Accounts/{id}` contract (PATCH-only - PUT full-replace is no longer offered). The GET-before-update step is gone entirely; instead, a `$patchOps` array of `{ op = 'replace'; path = ...; value = ... }` hashtables is built containing only the fields actually supplied and non-blank (Name->`/name`, Address->`/address`, UserName->`/userName`, PlatformID->`/platformId`, SafeName->`/safeName`, AutoManaged->`/secretManagement/automaticManagementEnabled`, with the boolean sent as the string `'true'`/`'false'`, matching the existing convention already established in `Invoke-AccountsResumeAutoManagement.ps1`). If no optional field is supplied at all, the module now fails fast with a clear validation error instead of silently PATCHing with an empty array. `AccountName`+`Safe` remain the two always-required CSV columns (unchanged - the account-resolution logic that locates `AccountID` from them was not touched), all other columns remain optional. On success, the result row is built from the PATCH response body, which CyberArk returns as the full updated `AccountModel` (same shape the old PUT response had). WhatIf mode no longer performs any GET, so its preview row shows only the field(s) actually being patched - fields not included in the update are left blank rather than displaying stale/guessed values. Restored a missing UTF-8 BOM on the production file (pre-existing gap, unrelated to this change) |
| 2026-08-26 | Invoke-AccountsUpdate.Tests.ps1 | Rewrote the success/WhatIf/error Describe blocks for the PATCH-only flow: removed the `$script:CurrentAccount` GET fixture and the "GET phase errors" Describe block entirely (there is no GET call anymore); "PUT phase errors" renamed to "PATCH phase errors" (AU19-AU20). Added AU06a (patch body contains only the supplied field's op), AU13/AU13a (blank fields omitted entirely rather than falling back to a fetched current value; multiple non-blank fields each produce their own op), AU15a (WhatIf preview reflects only the supplied field), and AU18 (no optional fields supplied at all -> Failures=1, no API call). 23/23 passing in isolation; full suite re-verified at 863 passing (net +3 over the prior 860), same 1 known pre-existing unrelated failure (`AG06`). Also independently verified via a `Start-Job` invocation under real strict mode confirming the PATCH body and the empty-patch validation error both behave correctly end to end |
| 2026-08-26 | Modules\CyberArkComms.psm1 | Fixed a real production bug reported by the user immediately after the v1.2.0 Update Account release: a single-field CSV update sent an HTTP 400, because `Invoke-CyberArkAPI` built the request body via `$Body \| ConvertTo-Json` - piping a single-element array into `ConvertTo-Json` unrolls it to its lone element first, so the body was serialized as a bare `{...}` object instead of a `[...]` array, which CyberArk's JSON Patch endpoint rejects. Fixed by calling `ConvertTo-Json -InputObject $Body` instead of piping - this was a latent bug in the shared comms layer affecting any single-element array body, not specific to Accounts Update. Full root-cause writeup in `Lessons-Learned-PowerShell-Pester.md` section 30 |
| 2026-08-26 | Tests\Unit\CyberArkComms.Tests.ps1 | Added C25a: sends a one-op JSON Patch body through the real `Invoke-CyberArkAPI` (only `Invoke-WebRequest` mocked) and asserts the captured request body parses back as a one-element array (not a bare object) - reproduces the exact regression above. Asserts structurally via `ConvertFrom-Json`, not a literal string match, since PS 5.1 hashtable key order isn't guaranteed (confirmed by the user's own log line showing keys reordered from insertion order) |
| 2026-09-02 | Modules\CyberArkComms.psm1 | Per user request for a full-project Self-Hosted test-plan/review pass: re-fixed the `Join-CyberArkUrl` trailing-slash loss for `PIMServices.svc`/Applications endpoints (originally added 2026-08-16, reverted same day to fix C12, never re-fixed). Fixed this time at the `Invoke-CyberArkAPI` call site, restoring the slash based on the caller's own `-Endpoint` string, rather than changing `Join-CyberArkUrl`'s generic (always-trim) contract again - see Lessons-Learned section 31.2 for why the call-site fix was chosen over another helper-level change |
| 2026-09-02 | Tests\Unit\CyberArkComms.Tests.ps1 | Added C25b/C25c covering the re-fix: a trailing slash on `-Endpoint` is preserved through to the final URI; no trailing slash is added when `-Endpoint` doesn't end with one |
| 2026-09-02 | Modules\CyberArkLogging.psm1 | Generalized `$script:SensitivePatterns` from a fixed OAuth field-name list (`access_token`/`refresh_token`/`id_token`) to also mask any quoted JSON key containing `password`/`secret`/`token`/`credential` (case-insensitive). Confirmed a real gap this closes: `Invoke-AccountsChangeInVault.ps1`'s `NewCredentials` field (the literal new vault password) was being logged in cleartext at DEBUG/`-FileOnly` via the shared request-body logging in `CyberArkComms.psm1`. See Lessons-Learned section 31.3 |
| 2026-09-02 | Tests\Unit\CyberArkLogging.Tests.ps1 | Added L22a/L22b/L22c covering quoted-JSON masking of `password`, `NewCredentials`, and `ClientSecret` |
| 2026-09-02 | Invoke-ApplicationsAdd.ps1 | Replaced unguarded `[int]$AccessFrom`/`[int]$AccessTo` casts (would throw and crash the whole batch on a non-numeric CSV cell) with `[int]::TryParse`, returning a normal non-fatal `Failures` entry instead. Also fixed the `Disabled` field, previously cast `[bool]` directly from a CSV string - see the CSV-boolean class fix below |
| 2026-09-02 | Tests\Unit\Invoke-ApplicationsAdd.Tests.ps1 | Added tests under a new "AccessPermittedFrom / AccessPermittedTo validation" Context for the TryParse fix |
| 2026-09-02 | APIModules\Applications\Invoke-ApplicationsAdd.ps1, Invoke-ApplicationsAddAuthMethod.ps1, Invoke-ApplicationsList.ps1, APIModules\Platforms\Invoke-PlatformsList.ps1, APIModules\Safes\Invoke-SafesList.ps1 | New bug class found and fixed across 5 modules: `[bool]$row.SomeField` on a CSV-sourced string is always `$true` regardless of the string's content (`[bool]"false"` is `$true` in .NET) - `Disabled`, `IsFolder`/`AllowInternalScripts`, `IncludeSublocations`, `ActiveOnly`, and `ExtendedDetails` respectively were all affected. Fixed by matching against `-match '(?i)^(true\|yes\|y\|1)$'` instead of casting. Full writeup in `Lessons-Learned-PowerShell-Pester.md` section 31.1 |
| 2026-09-02 | Tests\Unit\Invoke-ApplicationsAdd.Tests.ps1, Invoke-ApplicationsAddAuthMethod.Tests.ps1, Invoke-ApplicationsList.Tests.ps1, Invoke-PlatformsList.Tests.ps1, Invoke-SafesList.Tests.ps1 | Added a CSV-string `"false"` regression test to each of the 5 modules fixed above, confirming the literal text is no longer treated as `$true` |
| 2026-09-02 | APIModules\Reports\Invoke-ReportsList.ps1 | Added `PSObject.Properties[...]` existence guards to all 6 mapped result fields (`ReportID`, `ReportName`, `Description`, `ReportType`, `RunDate`, `Aggregated`) - unguarded dot-notation would throw `PropertyNotFoundException` under the driver's always-active `Set-StrictMode -Version Latest` if a live PVWA response ever omitted one |
| 2026-09-02 | Tests\Unit\Invoke-ReportsList.Tests.ps1 | Added RL08a: `Set-StrictMode -Version Latest` plus a sparse report object (only `reportId`/`reportName` present) confirming no exception |
| 2026-09-02 | APIModules\Platforms\Invoke-PlatformsList.ps1 | Field-mapping only handled one response shape; `Invoke-PlatformsGet.ps1` already had a 4-variant fallback chain for PVWA-version differences (`id`/`PlatformID`, `platformType`/`SystemType`, nested under `general` or at root). Rewrote `PlatformsList`'s mapping to mirror `PlatformsGet`'s full fallback chain so both modules behave consistently across PVWA versions |
| 2026-09-02 | Tests\Unit\Invoke-PlatformsList.Tests.ps1 | Added PL12a: an alternate-shape platform object (root-level `PlatformID`/`SystemType`) confirming both map correctly |
| 2026-09-02 | APIModules\Custom\Invoke-CustomExportGroupMembersLocal.ps1 | ISPSS returns `groupType='Vault'` for every group including LDAP-backed ones; the sibling `Invoke-CustomExportGroupMembersLDAP.ps1` already worked around this with an `@`-in-name heuristic, but this module didn't, so on ISPSS it would misclassify LDAP groups as local. Added the same `-or ($gname -and $gname -match '@')` condition |
| 2026-09-02 | Tests\Unit\Invoke-CustomExportGroupMembersLocal.Tests.ps1 | Added an "ISPSS groupType quirk" Context: a group with `groupType='Vault'` but an `@`-containing name is correctly excluded from local-group processing |
| 2026-09-02 | Manage-Privilege.ps1 | Two Self-Hosted-relevant driver fixes found during this review, neither unit-tested for the documented `Manage-Privilege.Tests.ps1` Pester-hang risk (2026-08-25 entry above) - verify manually via new D23/D24 (Testing-Plan.md): (1) `Invoke-SelfHostedKeepalive` extended the session's server-side expiry but never persisted the new expiry to the on-disk `.cred` file via `Save-AuthToken` - a crash shortly after a keepalive would understate the token's actual remaining lifetime on next load. (2) The inner category/action-loop (inside `Invoke-SessionLoop`) never ran the inactivity-timeout / proactive-refresh / token-expiry check block that the outer category-selection loop already ran on every iteration - a user staying inside one category for several actions in a row got no keepalive, no proactive refresh, and no inactivity timeout until returning to the category menu. Fixed by duplicating the outer loop's check block into the inner loop |
| 2026-09-02 | Auth\Get-AuthToken.ps1 | Deleted. Confirmed dead code (zero real call sites; only reference was an unrelated same-named Pester mock stub in `Manage-Privilege.Tests.ps1`) and matches `Auth-Module-Rework-Design.md`'s own documented, never-executed final step ("Delete `Get-AuthToken.ps1` shim once all call sites are updated") |
| 2026-09-02 | README.md, Docs\API-Module-Development-Guide.md | Removed stale `Get-AuthToken.ps1` references from project-structure trees following the deletion above |
| 2026-09-02 | Docs\Interfaces.md | Corrected the `Invoke-WebView2Window` reference row to its actual parameters (`-NavigateUrl`, `-CookieName`, `-TargetHost`, `-Title`) and return shape (`{Token, TokenType}`), noting the fixed `$script:WEBVIEW2_TIMEOUT_SEC` timeout is not a parameter; corrected `Get-SelfHostedAuthToken`'s code sample to remove false `[Parameter(Mandatory)]` attributes on `AuthMethod`/`PVWAUrl` (both actually fall back to interactive `Read-Host` prompts when omitted, per Auth-Module-Rework-Design.md) |
| 2026-09-02 | Docs\Testing-Plan.md | Full rewrite for this review's stated goal (a Self-Hosted-focused test plan): added the Self-Hosted vs. ISPSS scope/caution section flagging all dual-use (SelfHosted+ISPSS) modules as ISPSS-unverified; replaced the stale Component Test Matrix (which only listed ~6 modules) with a complete matrix of every module in the project; added a Findings and Fixes section (F01-F11) cross-referencing every fix above; added a Known Issues / Risk Register (K01-K07) for gaps found but not fixed this pass (PS7/pwsh WebException typing, process-global IgnoreSSL not reset across profile switches, WebView2 IgnoreSSL no-op for SAML/OIDC, stale PVWAUrl reuse from an old token, GroupsList GroupType ISPSS UX gap, Update-SelfHostedAuthToken silent-prompt fallback, missing Username fallback in SelfHosted token refresh); replaced the stale Get-AuthToken.ps1-referencing auth test section with a Self-Hosted Auth section (A01-A18) covering all 8 auth methods with SAML/OIDC/PKI/PKIPN flagged highest-risk; added D23-D25 driver test cases for the two Manage-Privilege.ps1 fixes above; added a Self-Hosted Full Functional Checklist enumerating all ~55 module actions for the live test pass against the lab PVWA host |
| 2026-09-02 | Docs\Lessons-Learned-PowerShell-Pester.md | Added section 31: the CSV-string-to-`[bool]` cast bug class (31.1, five instances fixed above), the `Join-CyberArkUrl`/`Invoke-CyberArkAPI` trailing-slash regression cycle and the rule of checking this tracker before considering a shared-helper contract change complete (31.2), and the fixed-field-name-list weakness in secret-masking regexes vs. a name-pattern match (31.3) |
| 2026-08-26 | Invoke-AccountsUpdate.ps1 (v1.2.1) | While fixing the above, also hardened the success-path result-mapping block: `$acct.address`/`.userName`/`.platformId`/`.safeName`/`.secretType`/`.createdTime`/`.id`/`.name` were read directly with no `PSObject.Properties` guard, inconsistent with the `secretManagement` sub-fields two lines below them which already had one (the same Bug Pattern D gap documented in Lessons-Learned 9.8/9.9's audit). Not yet observed to throw with a real CyberArk response, but nothing in the `AccountModel` schema guarantees these fields are always present, so guarded them the same way. Added AU21, a regression test using a two-field-only mock response, confirming no throw under `Set-StrictMode -Version Latest`. Also added `Set-StrictMode -Version Latest` to all four `BeforeEach` blocks in this test file - it previously had none, meaning the isolated test suite ran without strict mode at all and could not have caught either of these two bugs; this closes that gap the same way it was closed for other Accounts/Safes test files in the 2026-08-25 follow-up audit |
| 2026-08-26 | Invoke-CustomTestApi.ps1 (v1.0.1) | Fixed a real bug reported by the user: a 401 from Custom/Test API's own inline retry logic never actually re-authenticated. Root cause was in the shared `Invoke-TokenRefresh` (`Manage-Privilege.ps1`): it opens with `if ((Test-TokenExpiry) -in @('Valid','Warning')) { return $true }` - a short-circuit meant for its normal *proactive* callers (checked before a request, based purely on the token's locally-tracked expiry timestamp). Test API's 401 handler called `Invoke-TokenRefresh` *reactively*, straight after the server had already rejected the token - but the local expiry clock hadn't caught up, so `Test-TokenExpiry` still reported `Valid`/`Warning`, the function returned `$true` without refreshing anything, and the retry silently reused the same rejected token. Every other module avoids this by setting `IsFatal=$true` on 401, which the driver's `IsFatal` handler responds to by calling `Invoke-TokenInvalidate` (force-expires the token, deletes the saved `.cred` file) *before* the next `Test-TokenExpiry`/`Invoke-TokenRefresh` cycle - Test API's inline retry skipped that invalidate step entirely. Fixed by calling `Invoke-TokenInvalidate` immediately before `Invoke-TokenRefresh` in the 401 branch, matching the pattern already used at the two other `Invoke-TokenInvalidate` call sites in `Manage-Privilege.ps1`. Verified by direct code reading rather than a new Pester test - `Get-TokenRemainingMinutes`/`Test-TokenExpiry`/`Invoke-TokenInvalidate` are pure functions over `$script:SessionToken.Expiry` with no other side effects, and this module (like other `Read-Host`/`Show-FieldPrompt`-driven interactive modules in this codebase) has no existing unit test harness to extend |
| 2026-08-26 | Invoke-AccountsUpdate.ps1 (v1.2.2) | Per user request: removed the `Name` and `SafeName` columns from `InputSchema`, so the "Generate Template" CSV for Update Account no longer includes them (`Manage-Privilege.ps1`'s template generator builds its header directly off `$meta.InputSchema`, with no per-module override). Both were already optional and un-required, and `Test-CsvSchema` only enforces `Required` columns, so this only affects the generated template - a hand-edited CSV that still includes a `Name` or `SafeName` column continues to work exactly as before, since `Invoke-CsvProcessing` passes every column present in a row through to `InputData` regardless of schema, and the module's own field-mapping logic keys off `$InputData.ContainsKey(...)`, not the schema list. Interactive mode (`Get-AccountsUpdateInput`) is unaffected - it prompts for its own fixed set independent of `InputSchema`. Verified directly: dot-sourcing the module and reading `$ModuleMeta.InputSchema` now yields `AccountName,Safe,Address,UserName,PlatformID,AutoManaged`. Full suite re-verified at 865 passing, same 1 known pre-existing unrelated failure (`AG06`) |
| 2026-08-26 | Manage-Privilege.ps1 | Per user request: a reactive 401 mid-CSV (a module returns `IsFatal=$true`) now retries the failed row once after a successful re-authentication, instead of unconditionally aborting the whole batch. `Invoke-CsvProcessing`'s per-row loop now, on `IsFatal`: calls `Invoke-TokenInvalidate` (force-expires the token so `Invoke-TokenRefresh` doesn't short-circuit as "already valid" - see the `Invoke-CustomTestApi.ps1` v1.0.1 entry above for why that step is required), then `Invoke-TokenRefresh`; if refresh succeeds, re-invokes the module for the SAME row and uses that result (success or otherwise) for the row's output/counts, and the loop continues to the next row rather than breaking. If refresh fails/is cancelled, or the retried call is ALSO fatal (e.g. a real connectivity outage, not just an expired token), the batch still aborts exactly as before - only ONE retry is attempted per row, there is no retry loop that could spin. Verified with an isolated, non-Pester harness (this file's own test suite has a documented reproducible hang when `Invoke-FileWriteWithRetry` - which `Invoke-CsvProcessing` calls - runs under Pester after the existing DP01-DP17 tests; see the note at the end of `Manage-Privilege.Tests.ps1`) that copies the exact new control-flow block verbatim with mocked `Invoke-TokenInvalidate`/`Invoke-TokenRefresh`/module-call functions: confirmed (1) reauth succeeding retries the failed row and the next row still processes, with no abort; (2) reauth failing aborts immediately, next row never processed; (3) the retry itself staying fatal aborts after invalidating the token a second time. Full suite re-verified at 865 passing, same 1 known pre-existing unrelated failure (`AG06`) |
| 2026-08-28 | Count-AccountsPerPlatform.ps1 (new file) | Per user request: a standalone script (no dependency on `Manage-Privilege.ps1`, `APIModules`, or any CyberArk connection) that reads the already-exported `Export_PlatformsList.csv` and `Export_AccountsList.csv` (the shape produced by `Invoke-PlatformsList.ps1`/`Invoke-AccountsList.ps1`, e.g. via "Export All") and writes a new CSV joining them on `PlatformID`: one row per platform with a count of accounts assigned to it. Platforms with zero matching accounts are still listed (`AccountCount = 0`), rather than only listing platforms that happen to have accounts. Two edge cases are surfaced as explicit placeholder rows instead of being silently dropped or miscounted: an account whose `PlatformID` doesn't match any row in the platforms CSV (deleted/renamed platform) becomes a `"(Not Found)"` row, and an account with a blank `PlatformID` becomes a `"(No Platform Assigned)"` row - so the sum of `AccountCount` across every output row always equals the total account count. Output is sorted by `AccountCount` descending, then `PlatformName` ascending. All three paths (`-PlatformsCsvPath`, `-AccountsCsvPath`, `-OutputCsvPath`) are optional; missing input files fail fast with a clear error rather than an exception dump. Verified against this project's own real `Output\Export_PlatformsList.csv`/`Export_AccountsList.csv` (38 platforms, 17 accounts - correctly split 9 platforms in use / 4 accounts with no platform assigned) and against synthetic fixtures covering the orphaned-PlatformID and blank-PlatformID cases. Not unit tested - it is an offline, top-level CLI utility outside the `APIModules`/Pester harness, matching this project's own convention of relying on direct verification for scripts in that category |
| 2026-08-28 | Count-AccountsPerPlatform.ps1 | Per user request: any of the three paths not supplied on the command line now launches a dialog instead of falling back to a hardcoded default - `Select-CsvOpenDialog` (`OpenFileDialog`, single-select, CSV filter) for `PlatformsCsvPath`/`AccountsCsvPath`, `Select-CsvSaveDialog` (`SaveFileDialog`, pre-filled with `Count-AccountsPerPlatform_<yyyy-MM-dd>.csv` in the platforms CSV's folder) for `OutputCsvPath`. The accounts-CSV dialog opens in the same folder the platforms CSV was picked from, so the user isn't renavigating for the second file. Both dialog helpers mirror the existing `Select-InputFiles`/`Get-CsvSavePath` pattern in `Manage-Privilege.ps1`: `System.Windows.Forms` wrapped in try/catch, falling back to a `Read-Host` prompt in a headless/non-desktop session (blank cancels the open dialog's fallback; for the save dialog's fallback, blank accepts the suggested default and `X` cancels, matching the "Enter to continue, X to exit" convention already used elsewhere in this codebase). Cancelling any dialog exits 0 with a plain message, distinct from the exit-1 path used for actual errors like a missing/unreadable file. Verified: explicit `-PlatformsCsvPath`/`-AccountsCsvPath`/`-OutputCsvPath` still bypasses all dialogs and produces identical output to before against the same real export data; the `Read-Host` fallback branch of both helpers was verified directly (blank-cancels-open, blank-accepts-default-on-save, `X`-cancels-save) since `System.Windows.Forms` dialogs themselves can't be driven headlessly |
| 2026-09-02 | Invoke-AccountsCancelCpmTask.ps1 (v1.2.0) | Phase 1 item from aPePAS-Improvement-Plan-2026-09-02.md: user confirmed `/API/Accounts/{id}/Cancel/` as the correct endpoint (matching psPAS's `Stop-PASCPMTask.ps1`). Replaced the previous `/API/Accounts/{id}/StopImmediateAutoMgmtOperations` path |
| 2026-09-02 | Invoke-ApplicationsAdd.ps1 (v1.1.0) | Phase 1 items: per user direction, `Location` changed from optional to mandatory in `InputSchema` and enforced with a new validation check (mirroring the existing `AppID`-required pattern), and `SupportedSystems` expanded from `@('SelfHosted')` to `@('ISPSS', 'SelfHosted')` per user confirmation this module works on both platforms |
| 2026-09-02 | Tests\Unit\Invoke-ApplicationsAdd.Tests.ps1 | Added a "Missing Location" Context and a ModuleMeta Context (SupportedSystems includes both platforms; InputSchema marks Location Required) for the changes above. Added `Location` to every pre-existing test's `InputData` that previously omitted it, since it's now a required field |
| 2026-09-02 | Invoke-ReportsList.ps1 (v1.1.0) | Phase 1 item: per user confirmation, expanded `SupportedSystems` from `@('SelfHosted')` to `@('ISPSS', 'SelfHosted')` |
| 2026-09-02 | Tests\Unit\Invoke-ReportsList.Tests.ps1 | Updated RL03 (previously asserting SelfHosted-only) to assert both `SelfHosted` and `ISPSS` are present in `SupportedSystems` |
| 2026-09-02 | Tests\Unit\Invoke-ApplicationsAdd.Tests.ps1 | Traced the root cause of the two pre-existing "AccessPermittedFrom / AccessPermittedTo validation" full-suite-only failures (previously filed as unexplained pre-existing failures): `{ $result = Invoke-ApplicationsAdd ... } \| Should -Not -Throw` never assigns `$result` in the `It` block's own scope - the scriptblock piped to `Should` runs in a child scope. Silent under normal execution (`$null.Failures` is `$null`, so the assertion just fails with a mismatch), but throws `PropertyNotFoundException` under `Set-StrictMode -Version Latest` (only active via `Tests\Run-Tests.ps1`, never in an individual file run directly) - exactly the "full suite only" signature Section 9.9 describes for a different cause. Fixed both tests, plus a third ("Missing Location") added in this same session that was written with the identical pattern before this was diagnosed, by assigning directly instead of wrapping in `Should -Not -Throw`. Full writeup in `Lessons-Learned-PowerShell-Pester.md` Section 32, which flags `Invoke-CustomExportGroupMembersLocal.Tests.ps1`'s ISPSS groupType test and `Invoke-ReportsList.Tests.ps1`'s RL08a (same exception signature) as not yet checked for the same root cause |
| 2026-09-02 | Invoke-GroupsAddMember.ps1 (v1.1.0) | Phase 1 item: per user confirmation, `MemberType`'s valid values differ by platform for the identical `POST /API/UserGroups/{id}/Members` endpoint - Self-Hosted uses `Domain`/`Vault` (default `Vault`), ISPSS uses `EPVUser`/`Group` (default `EPVUser`, unchanged from the prior single-platform default). Added platform-aware default selection (`$Token.SystemType`) and validation rejecting a `MemberType` not valid for the current platform as a normal non-fatal failure, matching the file's existing `GroupID`/`MemberID`-required validation pattern. `Invoke-GroupsRemoveMember.ps1` doesn't reference `MemberType` and needed no change |
| 2026-09-02 | Tests\Unit\Invoke-GroupsAddMember.Tests.ps1 | Fixed `$script:ValidInput`'s `MemberType` from `'EPVUser'` to `'Vault'` - the shared `$script:MockToken` is `SystemType='SelfHosted'`, and `EPVUser` was never actually a valid value for that platform even before this session's fix; the existing tests just didn't know. Renamed/re-asserted GAM09 to `Vault` accordingly. Added a new "MemberType by platform" Describe block (GAM16-GAM21) covering both platforms' defaults and valid/invalid values, including a second mock token (`$script:MockTokenISPSS`) |
| 2026-09-02 | Invoke-AccountsResumeAutoManagement.ps1 (v1.4.0) | Phase 1 item: per user confirmation against a live Self-Hosted tenant, the endpoint is `POST /API/Accounts/{id}/Resume/` with no body, matching psPAS's `Resume-PASCPMAutoManagement.ps1` - not the pre-existing `PATCH /API/Accounts/{id}/` JSON Patch call. ISPSS is unconfirmed and keeps the pre-existing PATCH behavior; branches on `$Token.SystemType` (guarded with `.PSObject.Properties['SystemType']` first, since the property isn't guaranteed present on every token fixture/shape and this file's existing tests construct minimal token stubs without it - would otherwise throw under `Set-StrictMode` the same way Section 27/32's guarded-access pattern exists to prevent) |
| 2026-09-02 | APIModules\Groups\Invoke-GroupsAddMember.ps1 | Retroactively guarded the `$Token.SystemType` accesses added in this session's earlier MemberType fix with the same `.PSObject.Properties['SystemType']` check, for the same reason as the ResumeAutoManagement fix above - consistency, and defensive robustness against a token object that doesn't carry `SystemType` |
| 2026-09-02 | Tests\Unit\Invoke-AccountsResumeAutoManagement.Tests.ps1 | Added a new "endpoint by platform" Describe block: Self-Hosted token asserts `POST .../Resume/` with no `Body` key sent at all (not merely an empty body); ISPSS token asserts the pre-existing `PATCH .../` JSON Patch body unchanged; a token with no `SystemType` property at all asserts it falls back to the ISPSS/PATCH path without throwing, covering the pre-existing tests' minimal token fixtures |
| 2026-09-02 | Invoke-GroupsGetMembers.ps1 (v1.1.0) | Phase 1 item: per user confirmation, `includeMembers` is optional and defaults to `false` server-side, so the pre-existing behavior of never sending it was not the silent-empty-results risk originally suspected from reading psPAS's source alone. Added an opt-in `IncludeMembers` field (CSV-string-to-bool matched, never cast directly per Lessons-Learned 31.1) that sends `includeMembers=true` as a query param when set; omitted (not sent as `false`) when not requested, preserving the exact pre-existing request shape by default |
| 2026-09-02 | Tests\Unit\Invoke-GroupsGetMembers.Tests.ps1 | Added a new "IncludeMembers" Describe block (GM14-GM16): omitted sends no `includeMembers` query param, `'true'` sends `includeMembers=true`, and an explicit `'false'` string also sends no param (not `includeMembers=false`) |
| 2026-09-02 | APIModules\Platforms\Invoke-PlatformsEnable.ps1, Invoke-PlatformsDisable.ps1, Invoke-PlatformsCopy.ps1, Invoke-PlatformsRename.ps1, Invoke-PlatformsRemove.ps1, Invoke-PlatformsImport.ps1, Invoke-PlatformsSetPSMConfig.ps1 (new) | Phase 2: closed most of the Platforms scope gap from aPePAS-Improvement-Plan-2026-09-02.md. Endpoints, methods, and payload schemas confirmed against two real references pointed to by the user rather than guessed or fetched from docs.cyberark.com (which blocks automated fetching entirely - every URL 404s via WebFetch, even the homepage): `C:\Code\References\pspas.8.0.20` (psPAS 8.0.20 source, the actual shipped implementation of `Enable-`/`Disable-`/`Copy-`/`Rename-`/`Remove-`/`Import-`/`Export-PASPlatform.ps1` and `Set-PASPlatformPSMConfig.ps1`) and `C:\Code\References\CyberArk Self-Hosted Swagger\CyberArk_PasswordVault_Swagger_14.6.v1.json` (the official OpenAPI spec, used to confirm exact schemas/casing/validation ranges psPAS's own PowerShell param names don't directly reveal). Key finding driving the design: Enable/Disable/Copy/Rename/Remove/SetPSMConfig all key off a **numeric internal platform ID** (confirmed via the Swagger's `TargetPlatform` schema, which returns `ID` (int64) alongside the string `PlatformID` used by the pre-existing Get/List modules) - each new module resolves the numeric ID from the user-facing string `PlatformID` via `GET /API/Platforms/Targets?search=...` before calling the actual endpoint, duplicated inline per module rather than shared, matching this codebase's established convention (see `Invoke-AccountsCancelCpmTask.ps1`/`Invoke-AccountsResumeAutoManagement.ps1`'s duplicated AccountName->AccountID resolution). Scoped to **Target platforms only** for this pass (not Groups/RotationalGroups/Dependents, which Enable/Disable/Copy/Remove could in principle also support) - a deliberate scope-narrowing decision to avoid guessing at listing/search endpoint availability for those less-common platform types, and because it matches Rename/SetPSMConfig's own psPAS-confirmed Target-only scope, keeping all six new modules consistent. `Invoke-PlatformsRename.ps1` is Self-Hosted only (psPAS explicitly asserts `-SelfHosted` and PVWA 15.0+, and the endpoint is absent from the 14.6 Swagger, confirming it's a newer Self-Hosted-specific addition, not an oversight). `Invoke-PlatformsSetPSMConfig.ps1` does a GET-then-merge before its PUT (no PATCH alternative exists for this endpoint per the Swagger) to avoid wiping the platform's existing `PSMConnectors` array when only `PSMServerID` is being changed - mirrors psPAS's own `Format-PutRequestObject` merge for the identical reason; managing `PSMConnectors` itself is out of scope for this pass (CyberArk's schema needs an array of `{PSMConnectorID, Enabled}` objects, awkward to expose as a single CSV row - a deliberate simplification, not an oversight). `Invoke-PlatformsImport.ps1` sends the ZIP's bytes as a raw JSON array of integers (via `ConvertTo-Json` on a `[byte[]]`), matching psPAS's actual, shipped, working request shape - even though the Swagger's `"format: byte"` annotation on that field would normally imply a base64 string is expected; psPAS's real behavior was trusted over the schema's declarative annotation since it's a proven, in-production implementation, but this specific detail is flagged as unverified against a live tenant in the module's own code comment. `Invoke-PlatformsExport.ps1` (ZIP download) was **not implemented this pass** - `Invoke-CyberArkAPI` reads `Invoke-WebRequest`'s `.Content` as a string always, which would silently corrupt binary ZIP response bytes; fixing this needs a real design change to the shared comms layer (content-type-aware binary response handling) used by every other module in the project, judged too risky to bundle into this same pass without dedicated design attention |
| 2026-09-02 | Tests\Unit\Invoke-PlatformsEnable.Tests.ps1, Invoke-PlatformsDisable.Tests.ps1, Invoke-PlatformsCopy.Tests.ps1, Invoke-PlatformsRename.Tests.ps1, Invoke-PlatformsRemove.Tests.ps1, Invoke-PlatformsImport.Tests.ps1, Invoke-PlatformsSetPSMConfig.Tests.ps1 (new) | New test files for the seven modules above (PE/PD/PC/PRN/PR/PI/PPSM-prefixed), covering: ModuleMeta shape, numeric-ID resolution success/not-found/lookup-failure, the actual mutating call's endpoint/method/body, non-fatal vs. `IsFatal` failure classification, and WhatIf mode (confirming the numeric-ID resolution GET still runs in WhatIf - only the mutating call is skipped, matching `Invoke-AccountsCancelCpmTask.ps1`'s existing WhatIf-gating convention). 89 new tests total across this Phase 2 batch; full suite at 980/983, same 3 pre-existing unrelated failures |
| 2026-09-02 | APIModules\Platforms\Invoke-PlatformsList.ps1 (v1.1.0) | Phase 2: added `SystemType` filtering, confirmed as a direct `systemType` query parameter on `GET /API/Platforms` via the CyberArk 14.6 Swagger spec (simpler than `/API/Platforms/Targets`'s `filter=` expression string, which the existing `PlatformsGet`/`PlatformsList` modules don't use) |
| 2026-09-02 | Tests\Unit\Invoke-PlatformsList.Tests.ps1 | Added PL18/PL19 covering the `SystemType` filter: a supplied value is sent as `QueryParams['SystemType']`; an empty value sends no `SystemType` key, matching the existing `Search`/`Active` field convention |
| 2026-09-02 | APIModules\Policies\Invoke-PoliciesGetMasterPolicy.ps1, Invoke-PoliciesSetMasterPolicy.ps1 (new category) | Phase 2: new `Policies` category (auto-discovered by `Manage-Privilege.ps1`'s `Import-APIModules` via `$ModuleMeta.Category` - no registration needed anywhere else), matching psPAS's `Get-`/`Set-PASMasterPolicy.ps1`. Self-Hosted only, confirmed via psPAS's explicit `Assert-VersionRequirement -SelfHosted` (PVWA 14.6+). All 15 field names, types, and range limits (`ConfirmersNumber` 1-64; `PasswordChangeDays`/`PasswordVerificationDays` 1-3650; `RetentionPeriod` 0-3650) confirmed against the Swagger's `EffectivePolicy`/`UpdatePolicyRequest`/`Rule[Boolean]`/`Rule[UInt32]` schemas for `GET`/`PUT /API/Policies/{policyId}` (default `PolicyId`=1, the Master Policy) - the GET response wraps every field as `{ Value: ... }`, flattened by a small `Get-FlatPolicyValue` helper duplicated in both files rather than shared (same established convention as the Platforms modules above). `SetMasterPolicy` does the same GET-then-merge-then-PUT pattern as `SetPSMConfig` (no PATCH alternative exists for this endpoint either), validating each supplied field's range client-side before touching the API |
| 2026-09-02 | Tests\Unit\Invoke-PoliciesGetMasterPolicy.Tests.ps1, Invoke-PoliciesSetMasterPolicy.Tests.ps1 (new) | New test files (PGMP/PSMP-prefixed) covering: field flattening from the wrapped GET shape, a missing field not throwing under explicit `Set-StrictMode -Version Latest` (written carefully to assign the function's return value directly rather than via `{ $result = ... } \| Should -Not -Throw`, per Section 32 below - this file's own PGMP05 test was caught and fixed for the identical scoping bug before being committed), the GET-then-merge behavior (changing one field carries every other current value through unchanged in the PUT body), every validation range boundary, and WhatIf |
| 2026-09-02 | README.md | Updated the `APIModules/` project-structure tree: `Platforms/` now lists all eight actions instead of just Get/List; added the new `Policies/` category; corrected `Applications/` and `Reports/` (both stale from before this session's Phase 1 `SupportedSystems` changes - no longer Self-Hosted-only) |
| 2026-09-02 | Manage-Privilege.ps1 | Per user request: added `ModuleMeta.AutoSaveCsv` - when a module sets it, `Invoke-ActionModule`'s post-run CSV step skips the `'Save results to CSV? [y/N]'` prompt entirely and saves straight to the default path (module name + date, in the profile's `OutputFolder` or script root). Implemented by adding an `-AutoSave` switch to `Get-CsvSavePath` that returns the computed default path directly instead of showing the `SaveFileDialog`/`Show-FieldPrompt` fallback. No behavior change for any module that doesn't set the flag - `[bool]$meta.AutoSaveCsv` is `$false` for a hashtable with no such key, same as every other `$meta.<Flag>` read already used in this function |
| 2026-09-02 | APIModules\Custom\Invoke-CustomExportGroupMembersLocal.ps1 (v1.1.0), Invoke-CustomExportGroupMembersLDAP.ps1 (v1.1.0), Invoke-CustomExportEntitlements.ps1 (v1.2.0) | Per user request: set `AutoSaveCsv = $true` - these three bulk-export tools now save their CSV automatically with no prompt, since producing a CSV is their entire purpose. `Invoke-CustomExportAll.ps1` and `Invoke-CustomTestApi.ps1` were not changed - not requested, and `TestApi`'s existing `[S]` save-details action is a distinct, already-manual feature (saves full JSON request/response history, not a CSV of `$result.Results`) |
| 2026-09-02 | Tests\Unit\Invoke-CustomExportGroupMembersLocal.Tests.ps1, Invoke-CustomExportGroupMembersLDAP.Tests.ps1, Invoke-CustomExportEntitlements.Tests.ps1 | Added a "ModuleMeta" Context to each asserting `AutoSaveCsv` is `$true` |
| 2026-09-02 | APIModules\Custom\Invoke-CustomTestApi.ps1 (v1.1.0) | Per user request: strips the trailing `/PasswordVault` segment from `$Token.BaseURL` before building request URIs, widening what Test API can reach - a lot of a CyberArk host's API surface (e.g. ISPSS Identity-adjacent endpoints, PVWA endpoints outside the PasswordVault app) lives outside that path, which every other module needs but this ad-hoc testing tool doesn't. `-Endpoint`/API Path input still supplies the full path from root, so this only widens reachability - no other module's request-building was touched. No existing test file for this module (an interactive request/response loop, untested for the same `Show-FieldPrompt`-dependency reason as other interactive-only functions in this codebase) |
| 2026-09-02 | Tests\Unit\Invoke-AccountsGet.Tests.ps1, Invoke-CustomExportGroupMembersLocal.Tests.ps1, Invoke-ReportsList.Tests.ps1 | Per user request, investigated and fixed all 3 remaining pre-existing full-suite failures - each turned out to be a different bug despite two sharing the same `PropertyNotFoundException` signature (see `Lessons-Learned-PowerShell-Pester.md` Section 32's follow-up for the full comparison): (1) `AG06` was a genuinely stale assertion checking `InputSchema` for an `AccountID` column that hasn't existed since `Invoke-AccountsGet.ps1` was refactored to the `AccountName`+`Safe` resolution pattern (`AccountID` remains a supported optional direct-lookup override, just not a required CSV column) - fixed by asserting on the columns the module actually declares. (2) `Invoke-CustomExportGroupMembersLocal.Tests.ps1`'s ISPSS groupType test was the Section 9.8/9.9 array-collapse bug (`($list \| Where-Object {...}).Count` with zero matches collapses to `$null`, not an empty array) - fixed by wrapping in `@(...)`. (3) `Invoke-ReportsList.Tests.ps1`'s RL08a was the Section 32 scriptblock-scope bug (`{ $r = ... } \| Should -Not -Throw`) - fixed by direct assignment. Full suite now at 986/986 passing, zero known failures |
| 2026-09-02 | Docs\Lessons-Learned-PowerShell-Pester.md | Added a "Follow-up" to Section 32 documenting the investigation above and its rule: don't assume every failure sharing an exception signature has the same root cause - read each one's own code |
| 2026-09-02 | Docs\E2E-Automation-Design.md (new) | Per user request, created a design/tracking document for a proposed (not yet built) end-to-end automation layer that would exercise the project against a real tenant with only credential entry requiring a human, following a conversation covering feasibility and level-of-effort. Covers: which of the 11 auth methods across both platforms need a human every run vs. can be scripted; a design sketch for a harness (`Tests\E2E\`, kept out of `Run-Tests.ps1`'s automatic invocation since it hits a live tenant); a fixture/sandbox strategy for mutating actions; a phased rollout (read-only first, then low-risk mutating, then high-risk/tenant-wide deferred to a disposable tenant or left manual); a per-action Progress Tracker covering all 64 current module actions (all "Not started," since nothing is built yet) with a Risk classification (RO/Low/High); and open decisions that need answering before Phase 1 starts (credential storage, shared-vs-disposable tenant for high-risk actions, async-completion handling for Reconcile/Verify) |
| 2026-09-02 | Docs\Testing-Plan.md | Corrected two now-stale claims found while cross-referencing the new automation-tracking doc against the current codebase: the "Self-Hosted vs. ISPSS" caution section still said `Applications`/`Reports` were Self-Hosted-only (Phase 1, earlier this session, confirmed both work on ISPSS and expanded `SupportedSystems` - this doc was never updated to match) and the dual-use module count was stale. Added the 7 new Phase 2 Platforms modules and both new Policies modules to the Component Test Matrix and the Self-Hosted Full Functional Checklist (previously only listed `Platforms/Get`+`List`, with no `Policies` section at all since the category didn't exist when this doc was last revised); updated the Accounts/Groups/Custom checklist entries to call out Phase 1's confirmed endpoint/field changes; added a cross-reference at the top to `E2E-Automation-Design.md` |
| 2026-09-02 | APIModules\Custom\Invoke-CustomTestConnectivity.ps1 (new) | Per user request: a new Custom module testing DNS resolution, TCP port connectivity, and authentication to a Windows or Linux server, with a vault-password fallback when the CSV/interactive input doesn't supply one. Several implementation choices were clarified with the user first rather than guessed: DNS resolution uses .NET's `GetHostEntry` (handles both the IP-given-reverse-lookup and name-given-forward-lookup cases in one call, with "DNSMatch" meaning simply that resolution succeeded, per the user); the Windows auth test maps `\\<Address>\IPC$` via `New-SmbMapping` with the supplied credential (SMB on port 445, labeled `RPC` in the output per the user's spec even though that's not literally the RPC endpoint-mapper protocol on port 135); the Linux auth test cascades PowerShell 7's `-SSHTransport` (if `pwsh` is found) then `plink.exe` (if not) then reports `Plink or PS7 needed for auth test` (if neither is), per the user's exact instruction - the module's own code comments flag that native OpenSSH (which PS7's SSH transport relies on) does not support non-interactive password authentication by design, so that path reliably validates reachability/key-based auth but not password validation, unlike plink's `-pw` flag; the vault password lookup searches `/API/Accounts?search=<Address>` then filters client-side for an exact `address`+`userName` match (mirroring `Invoke-AccountsCancelCpmTask.ps1`'s established "search broadly, match exactly client-side" pattern, since `address`/`userName` are not confirmed as valid server-side `filter` fields) and retrieves via `POST /API/Accounts/{id}/Password/Retrieve` (identical to `Invoke-AccountsGetCredential.ps1`, including never logging the retrieved value). `AutoSaveCsv = $true`, matching the other three Custom export tools. Port-checking uses a raw `TcpClient` connect with a timeout instead of `Test-NetConnection`, which also does an ICMP ping and DNS lookup by default and would be noticeably slower across 4 ports x N servers in a bulk CSV run. Each OS/network primitive (DNS, TCP port check, SMB auth, SSH auth, vault lookup) is a separate `script:`-scoped helper function specifically so Pester can mock each one independently when testing the orchestration logic - discovered along the way that a helper called via its explicit `script:Name` prefix is NOT mockable by Pester (the qualified-scope call bypasses Mock's shadowing entirely), only a bare-name call is; every call site was written/fixed to use the bare name accordingly, with the `function script:Name` definition unchanged (needed to make it script-scoped/accessible across the dot-sourced file) |
| 2026-09-02 | Tests\Unit\Invoke-CustomTestConnectivity.Tests.ps1 (new) | 29 tests (TC01-TC29): ModuleMeta shape; `Resolve-ConnectivityTarget` and `Test-TcpPortOpen` exercised for real against `localhost`/a real loopback `TcpListener` (deterministic, no external dependency, unlike the SMB/SSH auth calls which are mocked); `Resolve-VaultPassword`'s exact-match/no-match/multiple-match/API-failure paths (mocking `Invoke-CyberArkAPI`); the orchestration function's validation, DNS-failure short-circuit, Windows/Linux port-gating-before-auth (confirming the auth function is never called when the relevant port is closed), the exact `PortCheck` string format, and the password-blank-falls-back-to-vault path including the "no password available at all" case |
| 2026-09-02 | README.md, Docs\Testing-Plan.md, Docs\E2E-Automation-Design.md | Added the new module to the `APIModules/` project-structure tree (README), the Component Test Matrix and Full Functional Checklist (Testing-Plan, Custom now 6 actions, dual-use count 62 of 65), and the Progress Tracker (E2E-Automation-Design, total module count 64 to 65) |
| 2026-09-02 | Manage-Privilege.ps1 | Live-breaking regression reported directly by the user: `[FATAL] PropertyNotFoundException` on `AutoSaveCsv` at `Invoke-ActionModule` (line 2096), hit via Accounts List - a module with no `AutoSaveCsv` key. `$meta` is a hashtable; `$meta.AutoSaveCsv` (dot notation, added when `AutoSaveCsv` was introduced two sessions ago) throws under `Set-StrictMode` for the ~61 of 65 modules that don't declare this optional key, exactly the bug class this project's own `Lessons-Learned-PowerShell-Pester.md` Section 4.1 already documented in full (with `$InputData` as the original example) - this was simply never checked against when the new field was added. Fixed with bracket notation (`$meta['AutoSaveCsv']`). Audited every other `$meta.<Field>` access in the file and confirmed all of them read a field every module's `ModuleMeta` always defines, so none of the others were at risk - this was an isolated, one-line regression. Not caught by the automated suite, since this exact code path (`Invoke-ActionModule`'s interactive post-run flow) is documented as requiring manual verification - exactly what surfaced it |
| 2026-09-02 | Docs\Lessons-Learned-PowerShell-Pester.md | Added a note to Section 4.1 recording the `ModuleMeta.AutoSaveCsv` recurrence above and generalizing the section's rule explicitly to `$ModuleMeta`/`$meta` hashtables, not just `$InputData` - flagging it as something to check before adding any future optional `ModuleMeta` field |
| 2026-09-02 | 16 Accounts modules (`Invoke-AccountsCheckIn/CancelCpmTask/ChangeInVault/ChangeImmediate/GetActivity/Get/Delete/GetCredential/LinkAccount/Reconcile/UnlinkAccount/Unlock/ResumeAutoManagement/Verify/Update/List.ps1`) | Per user request: confirmed against psPAS's own `Private/ConvertTo-FilterString.ps1` (API 14.6+ auto-quotes any filter value containing whitespace) that a safe name with spaces must be wrapped in literal double quotes inside a `filter=safeName eq ...` expression - which URL-encodes to `%22` once the query string is built, exactly as the user described. 15 of these modules built the filter via raw string interpolation (`"safeName eq $targetSafe"`), which never added that quoting; `Invoke-AccountsList.ps1` had two more instances of the identical bug (the interactive `Safe`-field convenience shortcut, and the per-safe filter in its "By-Safe" iteration mode). All 16 sites now call the existing, already-tested `New-CyberArkSearchFilter -Criteria @{ safeName = $targetSafe }` helper (`CyberArkComms.psm1`) instead of hand-building the string - it already auto-quotes on whitespace and was simply never used by any of these 16 call sites. Verified end-to-end: `New-CyberArkSearchFilter -Criteria @{ safeName = 'My Safe' }` -> `safeName eq "My Safe"` -> (through `New-CyberArkQuery`) -> `...filter=safeName%20eq%20%22My%20Safe%22...` |
| 2026-09-02 | Tests\Unit\Invoke-AccountsCancelCpmTask.Tests.ps1, Invoke-AccountsList.Tests.ps1 | Added one regression test each (representative of the 15 identical `$targetSafe` call sites, and covering `Invoke-AccountsList.ps1`'s own two distinct call sites respectively) confirming a spaced safe name is quoted in the captured `filter` query value. The other 14 files were not given their own copy of this test since they now all delegate identically to the same already-tested `New-CyberArkSearchFilter` helper (`CyberArkComms.psm1` C13-C16) - a per-file bug is no longer possible there, only a regression in the shared helper itself, which those existing tests already guard |
| 2026-09-02 | Docs\API-Module-Development-Guide.md | While fixing the above, found this guide's own "Query Builder Helpers" example was stale in two ways: it showed a `New-CyberArkQuery` call signature (`-Search`/`-Filter`/`-Limit`/`-Offset`/`-Sort`/`-SavedFilter` named parameters) that doesn't match the real function (`-Params <hashtable>`), and it never mentioned `New-CyberArkSearchFilter` at all - modeling exactly the "hand-write the filter string" anti-pattern that caused the 16-file bug above, and a plausible reason none of those 16 call sites used the existing helper in the first place. Corrected the `New-CyberArkQuery` example to its real signature (noting hashtable key order is not guaranteed) and added `New-CyberArkSearchFilter` with the safe-name-quoting example, cross-referenced to this same date's fix |
| 2026-09-02 | Docs\Lessons-Learned-PowerShell-Pester.md | Added Section 33 for the 16-file safe-name-quoting bug above: a correct, already-tested shared helper (`New-CyberArkSearchFilter`) existed and was simply never used by any of the 16 call sites, which each independently hand-rolled the identical, unquoted filter string instead. Frames the rule as the mirror case of Section 31.2 (which is about a shared helper's *contract* changing without checking every caller) - here the contract was already correct, it just wasn't adopted everywhere it should have been; recommends grepping for the hand-built pattern across the whole `APIModules\` tree before considering a single call site's fix complete |
| 2026-09-02 | Docs\Testing-Plan.md | Added a note to the Accounts checklist flagging the safe-name-quoting fix as needing live verification against a safe name containing a space, across every `AccountName`+`Safe`-resolving action and `List`'s By-Safe iteration mode - none of the 16 fixed call sites have been exercised against a real tenant yet |
| 2026-09-02 | Docs\Testing-Plan.md | Per user report, marked every `Action = 'List'` module (8 modules across Accounts, Safes, SafeMembers, Platforms, Users, Groups, Applications, Reports) as confirmed against a real Self-Hosted host in the Component Test Matrix and Full Functional Checklist. `Applications/ListAuthMethods` and `Custom/ExportAll` explicitly excluded from that confirmation, both for different reasons - see the fix below |
| 2026-09-02 | APIModules\Applications\Invoke-ApplicationsListAuthMethods.ps1 (v1.1.0) | Per user request: `AppID` changed from required to optional - when blank, the module now fetches the full application list (`GET /WebServices/PIMServices.svc/Applications/`, the same endpoint `Invoke-ApplicationsList.ps1` uses) and retrieves auth methods for every application found, instead of failing with "AppID is required". Per-application error handling matches the established convention elsewhere in this codebase: a 401/network error (`StatusCode` 0) on any one application's call is fatal and stops the whole run immediately; any other error is recorded as a non-fatal failure and the loop continues to the next application. The interactive input function's blank-then-blank (App ID and Search both empty) path now falls through to "every application" instead of cancelling; a search that's attempted but finds nothing still cancels, rather than silently expanding to every application |
| 2026-09-02 | APIModules\Custom\Invoke-CustomExportAll.ps1 (v1.1.0) | Per user request: the module-discovery filter now matches `Action -in @('List', 'ListAuthMethods')` instead of only `'List'`, so Export All automatically discovers and runs `Invoke-ApplicationsListAuthMethods.ps1` alongside every other List module. No special-case `InputData` override was needed (unlike Accounts/List's existing `IterateBySafe` override) - the default empty `InputData` already triggers the new "list every application" behavior added above |
| 2026-09-02 | Tests\Unit\Invoke-ApplicationsListAuthMethods.Tests.ps1 | Rewrote the "Missing AppID" test (blank AppID is no longer a failure) and added 8 new tests (AM01-AM11, renumbered from the prior AM01-AM04... naming): blank-AppID application-list retrieval and per-app aggregation, omitted-InputData equivalence, application-list-call failure, zero-applications empty result, a 401 on one application stopping the whole run immediately, and a non-fatal error on one application continuing to the next |
| 2026-09-02 | APIModules\Reports\Invoke-ReportsList.ps1 (v1.2.0) | Per user report from live ISPSS/Privilege Cloud testing: this endpoint (`GET /API/Reports`) returns HTTP 404 on ISPSS, reversing Phase 1's dual-use expansion (which had relied on psPAS's comparison-review claim of ISPSS support from v14.6+ - that claim did not hold up against a real tenant). `SupportedSystems` reverted to `@('SelfHosted')`; `Description` updated to state Self-Hosted only |
| 2026-09-02 | Tests\Unit\Invoke-ReportsList.Tests.ps1 | Reverted test `RL03` to assert `SupportedSystems` is Self-Hosted-only again (single-element array) |
| 2026-09-02 | APIModules\Applications\Invoke-ApplicationsAddAuthMethod.ps1, Invoke-ApplicationsDelete.ps1, Invoke-ApplicationsDeleteAuthMethod.ps1, Invoke-ApplicationsGet.ps1, Invoke-ApplicationsList.ps1, Invoke-ApplicationsListAuthMethods.ps1 | Per user report from the same live ISPSS test: only `Invoke-ApplicationsAdd.ps1` (already dual-use) appeared on the ISPSS Applications menu, confirming these 6 modules had been left `SupportedSystems = @('SelfHosted')` in error. Expanded all 6 to `@('ISPSS', 'SelfHosted')` and removed "Self-Hosted only" from each `Description`. Only ISPSS menu visibility has been confirmed for these 6 - their actual ISPSS request/response behavior is unverified |
| 2026-09-02 | Tests\Unit\Invoke-ApplicationsAddAuthMethod.Tests.ps1, Invoke-ApplicationsDelete.Tests.ps1, Invoke-ApplicationsDeleteAuthMethod.Tests.ps1, Invoke-ApplicationsGet.Tests.ps1, Invoke-ApplicationsList.Tests.ps1 | Added a `Context 'ModuleMeta'` block to each asserting `SupportedSystems` contains both `SelfHosted` and `ISPSS` (2-element array) |
| 2026-09-02 | Tests\Unit\Invoke-ApplicationsListAuthMethods.Tests.ps1 | Added test `AM01a` to the existing `Describe 'ModuleMeta'` block, asserting `SupportedSystems` contains both `SelfHosted` and `ISPSS` |
| 2026-09-02 | Docs\Testing-Plan.md | Updated the Self-Hosted vs. ISPSS caution section for both changes above: SelfHosted-only count 3 -> 4 (added Reports/List back), dual-use count 62 -> 61 of 65 (Reports category no longer has a dual-use member); updated the Component Test Matrix rows for Reports/List (back to Self-Hosted-only, 404 confirmed on ISPSS) and all 7 Applications modules (Add's menu-visibility confirmation, the other 6's expansion with menu-visibility-only caveat); updated the Full Functional Checklist's Applications ("dual-use, see caution section") and Reports ("Self-Hosted only, see caution section") headings and notes; added a Revision Log entry |
| 2026-09-02 | Tests\Unit\Invoke-CustomExportAll.Tests.ps1 | Added a test confirming a `ListAuthMethods`-action module (not just `List`) is discovered and run by the updated filter |
| 2026-09-02 | APIModules\Accounts\Invoke-AccountsChangeInVault.ps1 (v1.2.0) | Per user report: fixed a wrong endpoint - the module called `POST /API/Accounts/{id}/SetNextPassword`, which per the local Swagger spec "gives the ability to set the account's credentials for the next CPM change" (a queued operation), when it should have called `POST /API/Accounts/{id}/Password/Update`, which the same spec describes as setting the account's credentials and changing them in the vault immediately without affecting the target device - exactly matching this module's name and description. Cross-checked against psPAS's `Invoke-PASCPMOperation.ps1`, which treats `Password/Update` and `SetNextPassword` as two distinct parameter sets built from the same `AccountID`+`ParameterSetName` URI pattern. The request body was already correct (`NewCredentials`) - both endpoints share that field name per the Swagger `ChangeInVaultProperties`/`SetNextCredentialsProperties` schemas, so only the endpoint path and its log messages changed |
| 2026-09-02 | Tests\Unit\Invoke-AccountsChangeInVault.Tests.ps1 | Added a regression test capturing the mocked `Invoke-CyberArkAPI` call's `Endpoint` parameter and asserting it is `/API/Accounts/acc123/Password/Update`, not `SetNextPassword` |
| 2026-09-02 | Docs\Testing-Plan.md | Added Finding F12 documenting the wrong-endpoint bug and its fix; updated the Accounts checklist and the Component Test Matrix's `Accounts / ChangeInVault` row to flag the corrected endpoint as needing live verification; added a Revision Log entry |
| 2026-09-02 | Manage-Privilege.ps1 | Live-breaking regression reported directly by the user: `[FATAL] PropertyNotFoundException` on `.Count` when a `List` action (`Accounts / List`) returned exactly one row. `Invoke-ActionModule`'s `$tableData`/`$displayData` were each assigned from an `if/else` without wrapping the whole expression in `@(...)` - a one-item branch result auto-unrolled onto the pipeline as a bare scalar object instead of a one-element array, so `.Count` on the next line threw under `Set-StrictMode`. This is the single-item counterpart of the already-documented Lessons-Learned Section 9.8 bug (which covered the empty-collapses-to-`$null` case). Reproduced and verified the fix directly against real `powershell.exe` (Windows PowerShell 5.1) - confirmed `pwsh`/PowerShell 7 does not reproduce the exception at all, since PS7 adds a synthetic `Count` property to scalar objects, masking the underlying type defect. Fixed by wrapping both entire `if/else` expressions in an outer `@(...)`. No automated test added - `Invoke-ActionModule` is outside this project's established unit-testing boundary for interactive/`Read-Host`-driven driver functions, and `Manage-Privilege.Tests.ps1` has a documented Pester v6.1 hang risk for new `Describe` blocks in this area |
| 2026-09-02 | Docs\Lessons-Learned-PowerShell-Pester.md | Added an addendum to Section 9.8 documenting the single-item collapse-to-scalar manifestation above (as opposed to the originally-documented empty-collapses-to-`$null` case), and the rule that `pwsh`/PowerShell 7 must not be used to verify this class of bug in this codebase, since it silently masks the exact failure that occurs under the project's actual PowerShell 5.1 runtime |
| 2026-09-02 | Docs\Testing-Plan.md | Added Finding F13 documenting the `Invoke-ActionModule` fix above; added a driver-level checklist item to confirm a real single-row `List` result renders correctly; added a Revision Log entry |
| 2026-09-02 | Manage-Privilege.ps1 | Per user request: within each category's action menu, the remaining (non-`List`) actions are now sorted alphabetically by `Meta.Name` instead of by the numeric `Meta.Priority` field, which had drifted into an arbitrary order over many sessions of module additions with no specific top/bottom-placement intent behind most of the values. Checked `Docs\Architecture.md`'s existing Design Decisions table first for any documented exception before changing anything: found and preserved the one legitimate, explicitly-requested exception (`List` always sorted first within its category - Design Decision row, 2026-08-18) and found no other documented per-module ordering request. Categories themselves were already sorted alphabetically and were not changed. The `Priority` field is left in place on every module's `ModuleMeta` (unused by this sort now) as a place to anchor a future explicit top/bottom-of-list request, rather than removing it from all ~65 modules for a change not requested |
| 2026-09-02 | Docs\Architecture.md | Updated the "Action menu ordering" Design Decision row to describe the new alphabetical-by-name secondary sort, replacing the old `Priority`-based description, and noted the change's date and reasoning |
| 2026-09-02 | APIModules\Accounts\Invoke-AccountsCancelCpmTask.ps1 (v1.3.0), Invoke-AccountsResumeAutoManagement.ps1 (v1.5.0) | Per user report and confirmed design direction: `/API/Accounts/{id}/Cancel/` and `/API/Accounts/{id}/Resume/` both require a minimum PVWA version (15.2 and 15.0-15.2 depending on source - psPAS's own `Stop-PASCPMTask.ps1`/`Resume-PASCPMAutoManagement.ps1` assert `RequiredVersion 15.2` for both) that neither this project nor psPAS can query ahead of time. Both modules now try the newer endpoint first and, only on an HTTP 404, retry against a version-agnostic fallback: `CancelCpmTask` falls back to the pre-Phase-1 `/StopImmediateAutoMgmtOperations` endpoint (recovered from git history at commit `1f06d6e`'s parent); `ResumeAutoManagement` (Self-Hosted only - ISPSS already used this shape as its primary path) falls back to the same PATCH `automaticManagementEnabled` JSON Patch body. Any other failure (401/403/500/network) is not reinterpreted as a version issue and fails normally. A `WARN`-level log entry records when a fallback occurs |
| 2026-09-02 | Tests\Unit\Invoke-AccountsCancelCpmTask.Tests.ps1, Invoke-AccountsResumeAutoManagement.Tests.ps1 | Added 3 tests to each module: fallback triggers and succeeds on a 404 from the primary endpoint; a non-404 failure (403) does not trigger the fallback (only one API call is made); the overall result is a failure when the fallback also fails |
| 2026-09-02 | Modules\CyberArkComms.psm1, APIModules\Custom\Invoke-CustomTestApi.ps1 (v1.2.0) | Per user report: the whole script process silently closed with no error shown when the session token expired while using Custom Test API. Found `Invoke-CustomTestApi.ps1` was the only module in the codebase enabling the `IgnoreSSL` bypass via `[System.Net.ServicePointManager]::ServerCertificateValidationCallback = { $true }` - a raw PowerShell scriptblock assigned directly to a .NET delegate - instead of the compiled `ICertificatePolicy`-based `Disable-SSLValidation` helper every other module uses internally through `Invoke-CyberArkAPI`. A scriptblock assigned to that delegate is a known hazard: if .NET's TLS stack invokes it off the runspace's own thread (plausible for a fresh handshake, such as one triggered by a mid-session re-authentication request), it can crash the whole process silently with no catchable PowerShell exception - matching the reported symptom. Exported `Disable-SSLValidation` from `CyberArkComms.psm1` (previously private, used only internally) and switched `Invoke-CustomTestApi.ps1` to call it. Not confirmed as the definite root cause, since no error was available to inspect - flagged in `Testing-Plan.md` as needing live verification with `IgnoreSSL` enabled and a token allowed to expire mid-session |
| 2026-09-02 | Docs\Testing-Plan.md | Added Findings F14 (Cancel/Resume version fallback) and F15 (Test API SSL callback crash); added checklist notes for both under Accounts and Custom; added Revision Log entries |
| 2026-09-02 | Docs\Architecture.md | Added two Design Decision rows: the version-gated CPM endpoint fallback pattern (F14), and Test API reusing `Disable-SSLValidation` instead of its own `ServerCertificateValidationCallback` scriptblock (F15) |
| 2026-09-02 | Manage-Privilege.ps1 | Per user request: renamed `$script:AppName` from "Idira Unified Scripts - CyberArk PAS Driver" to "aPePAS - CyberArk PAS Driver" (shown in the `Show-Header` banner and the startup log line - both already read from this one variable, so no other change was needed to satisfy "pulled from a variable updated each release"; added a comment on `$script:Version` noting it should be bumped per release). Added a new `Get-SignedInUsername` helper and wired it into `Show-Header` to print a `User: <name>` line under the title once a session exists - it prefers the token's `_RefreshContext.Credential.UserName` (the credential actually used to authenticate) over `$script:ActiveProfile.Username`, and is blank (no line shown) before login. Deliberately left `%APPDATA%\IdiraUnifiedScripts\Profiles` (`Get-ProfileDir` in `CyberArk.Auth.Common.psm1`) unrenamed - it's the on-disk profile/token storage location, not the banner, and renaming it would orphan existing users' saved profiles |
| 2026-09-02 | Docs\Architecture.md | Added a Design Decision row for the banner rename, version-variable clarification, and new signed-in-user line, including why the on-disk profile folder name was deliberately left unchanged |
| 2026-09-03 | APIModules\Custom\Invoke-CustomTestApi.ps1 (v1.3.0) | Found while using Test API to diagnose a separate live 400 error on `Invoke-AccountsChangeInVault.ps1`: a genuine CyberArk 400 response (`Content-Length: 80`, `Content-Type: application/json`) came back through Test API as `ResponseBody = null`. Root cause: Windows PowerShell 5.1's `Invoke-WebRequest` already reads the error response stream once internally to populate `$_.ErrorDetails.Message` - by the time this module's `catch [System.Net.WebException]` block tried to read `$webResp.GetResponseStream()` a second time, the stream was already consumed and returned empty, silently discarding the server's actual error body on every 4xx/5xx response this module has ever captured. Fixed by reading `$_.ErrorDetails.Message` first (falling back to a manual stream read only if that's unexpectedly empty) |
| 2026-09-03 | Docs\Lessons-Learned-PowerShell-Pester.md | Added Section 14.3 (alongside the existing WebException/`catch`-block-safety material in Section 14) documenting the stream-already-consumed bug above: `$_.ErrorDetails.Message` must be read first for a `WebException`'s response body, since PowerShell has already exhausted the stream reading it once internally - a second manual read silently returns empty rather than throwing |
| 2026-09-03 | Docs\Testing-Plan.md | Added Finding F16 for the Test API response-body fix; noted the fix is unverified against a real 400 response as of this writing |
| 2026-09-03 | Modules\CyberArkComms.psm1 | Per user request, following up on the F16 investigation: the live 400 on `Invoke-AccountsChangeInVault.ps1` turned out to be a legitimate CyberArk error (`PASWS001W: The account is locked by: [ca_jesse].`) once F16's fix let Test API surface it - confirming the `Password/Update` endpoint fix from earlier this session is correct and needs no further change. Separately, since every module builds its displayed/logged error text from `Invoke-CyberArkAPI`'s `ErrorMessage`, which only ever surfaced the bare `ErrorMessage` field and dropped `ErrorCode`, the user asked for the `ErrorCode` to be included for all requests on any 4xx. Changed both of `Invoke-CyberArkAPI`'s error-response-building sites to format `ErrorMessage` as `"<ErrorCode>: <ErrorMessage>"` when a code is present. Also fixed a latent bug found in the process: `Parse-CyberArkError` used unguarded dot access on the parsed JSON, so a body with only one of `ErrorCode`/`ErrorMessage` present threw `PropertyNotFoundException` under this module's own `Set-StrictMode` and silently discarded both fields (caught by an outer try/catch, but downgrading to a bare `"HTTP <code>"` fallback that lost a message that WAS present) - now uses `PSObject.Properties[...]` guards for both fields |
| 2026-09-03 | Tests\Unit\CyberArkComms.Tests.ps1 | Added a new `Describe` block (C29-C32) covering the `ErrorCode`-prefixing fix: a 4xx and a 5xx body each combine `ErrorCode`+`ErrorMessage` correctly, a body with no `ErrorCode` falls back to the bare message, and a non-JSON body falls back to `"HTTP <code>"` without throwing. These mock `Invoke-WebRequest` to return a non-2xx status directly rather than throwing, since `$isSuccess` is derived purely from the status code - this file's own header comment documents why the thrown-`WebException` path (401, 429, etc.) can't be tested the same way (no public constructor for a real `HttpWebResponse`) |
| 2026-09-03 | APIModules\Custom\Invoke-CustomTestApi.ps1 (v1.4.0) | Per user request: the "Query Params" prompt is now skipped (forced to an empty string) for any HTTP method other than `GET`, since query parameters are conventionally meaningless for POST/PUT/PATCH/DELETE bodies |
| 2026-09-03 | Docs\Testing-Plan.md | Added Findings F17 (`ErrorCode` prefixing + `Parse-CyberArkError` guard fix) and F18 (Test API Query Params GET-only); added a Revision Log entry covering F16-F18 |
| 2026-09-03 | Modules\CyberArkComms.psm1 | Per user request: "Error messages for failed requests should include the body information unless it is blank or null." `Format-CyberArkErrorMessage` (added for F17) gained a third preference tier - the raw response body, whenever non-blank - before falling back to a generic `"HTTP <code>"` message, covering error shapes (an IIS HTML page, non-CyberArk JSON) that the structured `ErrorCode`/`ErrorMessage` envelope doesn't capture |
| 2026-09-03 | Tests\Unit\CyberArkComms.Tests.ps1 | Updated C32 (a non-JSON 4xx body now expects that body's content in `ErrorMessage`) and added C33 (a genuinely blank body still falls back to the bare `"HTTP <code>"` message) for the fix above |
| 2026-09-03 | Manage-Privilege.ps1 | Added `Get-CpmOptions`, a new shared driver-scope helper (alongside `Invoke-EntitySearch`, `Show-FieldPrompt`) for every "pick a CPM" prompt. Per user request: queries live via `GET /API/Users?userType=CPM&componentUser=true`; uses that result whenever the call succeeds, even if empty (a real environment state, not a failure); falls back to the active profile's `CPM_List` only when the call fails or throws. This supersedes the prior split recorded in `Architecture.md` where `Safes/AddFromTemplate` used only `CPM_List` and `Safes/AssignCPM` used only the live query "per explicit direction, not an oversight" - that direction was explicitly superseded by this request. Manually verified all four cases (live success with/without results, live failure, live exception) via a standalone `powershell.exe` repro, since this class of helper has no unit test coverage in this codebase (same reason as `Invoke-EntitySearch`) and `Manage-Privilege.Tests.ps1` has a documented Pester v6.1 hang risk for new `Describe` blocks |
| 2026-09-03 | APIModules\Safes\Invoke-SafesAddFromTemplate.ps1 (v1.5.0), Invoke-SafesAssignCPM.ps1 (v1.1.0) | Deleted each module's own per-module CPM-source function (`script:Get-ProfileCPMOptions`, `script:Get-SafesCPMOptions` respectively) and switched their CPM pickers to call the new shared `Get-CpmOptions` instead - same picker UI in both cases, only the underlying data source changed. Fixed a stale comment in `Invoke-SafesAddFromTemplate.ps1` that cross-referenced the now-deleted function's doc-comment for an unrelated array-collapse explanation, pointing it at `Lessons-Learned-PowerShell-Pester.md` Section 9.8 instead |
| 2026-09-03 | APIModules\Safes\Invoke-SafesAdd.ps1 (v1.2.0) | Per user request, two changes to the interactive input flow: (1) `Location` is no longer prompted for - every safe is created at the default `\` unless overridden via CSV/bulk input, which is unchanged; (2) the `ManagingCPM` prompt changed from free text to the same numbered `Get-CpmOptions`-sourced picker used by Add Safe From Template (`(none)` as entry 1 and the default) |
| 2026-09-03 | Tests\Unit\Invoke-SafesAddFromTemplate.Tests.ps1, Invoke-SafesAssignCPM.Tests.ps1 | Removed T32-T35 and A15-A17, which tested the two now-deleted per-module CPM-source functions directly - that logic now lives in `Get-CpmOptions` (`Manage-Privilege.ps1`), which per the note above is not unit-tested |
| 2026-09-03 | Docs\Architecture.md | Marked the "Profile CPM list" Design Decision row as superseded; added new rows for `Get-CpmOptions`, the raw-response-body error-message fallback, and Add Safe's two interactive-flow changes; updated the "Add Safe From Template — CPM prompt" row to reference `Get-CpmOptions` instead of `CPM_List` directly |
| 2026-09-03 | Docs\Testing-Plan.md | Added Findings F19 (raw-body error fallback) and F20 (CPM source unification + Add Safe changes); updated the Safes checklist for Add/AddFromTemplate/AssignCPM; added a Revision Log entry |
| 2026-09-03 | Modules\CyberArkComms.psm1 | Per user report, live: a `?search=` value containing a period fails to match on the CyberArk API - `[Uri]::EscapeDataString` treats `.` as unreserved (RFC 3986) and leaves it as a literal period, but these endpoints require it percent-encoded as `%2E`. Affects every module that searches by a value that can contain a period - UPN-style usernames, addresses/IPs, email-style account names - via `Invoke-EntitySearch`'s picker, `Invoke-AccountsLinkAccount.ps1`, `Invoke-CustomTestConnectivity.ps1`'s vault lookup, several Platforms modules' by-ID search, and `Invoke-SafesAddFromTemplate.ps1`'s role-prefix group lookup. Fixed once in `New-CyberArkQuery`: replaces `.` with `%2E` in a value's already-encoded form specifically when the query key is `search`, matched case-insensitively since `Invoke-EntitySearch`'s Platforms callers use `-SearchParam 'Search'` while most direct callers use lowercase `search`. Applied in the one shared query-builder every module already routes through, so no per-module changes were needed |
| 2026-09-03 | Tests\Unit\CyberArkComms.Tests.ps1 | Added C34-C36: a period in a lowercase `search` value is encoded as `%2E`, a period in a capitalized `Search` value is also encoded (case-insensitive key match), and a period in a non-search value (e.g. `filter`) is left alone |
| 2026-09-03 | Docs\Lessons-Learned-PowerShell-Pester.md | Added Section 34 documenting the `?search=` period-encoding quirk above and the fix pattern |
| 2026-09-03 | Docs\Testing-Plan.md | Added Finding F21; added a driver-level checklist item to confirm the fix against a real search term containing a period; added a Revision Log entry |
| 2026-09-03 | APIModules\Custom\Invoke-CustomTestConnectivity.ps1 (v1.1.0) | Per user request: added `Safe`, `Username`, and `PasswordSource` columns to the output. `Safe`/`Username` identify the specific vaulted account (its `safeName`/`userName`) that was matched and used to retrieve a credential - populated only when a vault lookup actually succeeded, blank otherwise (a directly-supplied password, or a failed/no-match lookup). `PasswordSource` is `Provided` or `Vault`, set as soon as the password's origin is known regardless of whether a subsequent vault lookup succeeds. `Resolve-VaultPassword` now returns `SafeName` and `Username` alongside `Password` to support this |
| 2026-09-03 | Tests\Unit\Invoke-CustomTestConnectivity.Tests.ps1 | Updated TC11 (asserts the new `SafeName`/`Username` return fields on a successful match) and TC12 (asserts both are blank on no match); updated TC20 (DNS failure - all three new columns blank), TC21 (direct password - `PasswordSource=Provided`, `Safe`/`Username` blank), TC27 (same, via the orchestration path), TC28 (vault success - `Safe`/`Username`/`PasswordSource` populated from the mocked lookup), and TC29 (vault failure - `PasswordSource=Vault` even though nothing was found, `Safe`/`Username` blank) |
| 2026-09-03 | APIModules\Custom\Invoke-CustomTestConnectivity.ps1 (v1.2.0) | Per user report, full stack trace: `[FATAL] PropertyNotFoundException` on `ProcessStartInfo.ArgumentList`, crashing every Linux SSH connectivity test. `ArgumentList` (added in .NET Framework 4.6.1) proved unsafe to rely on under PS 5.1's `Set-StrictMode` - reproduced independently on a second machine (.NET Framework 4.8) with a different symptom (silently `$null` without strict mode; the identical `PropertyNotFoundException` once strict mode was turned on to match production), and neither the .NET Framework version nor `.GetType().GetProperty('ArgumentList')` reflection reliably predicted the failure. Added a runtime probe in `Invoke-ExternalProcessWithTimeout` (a disposable `ProcessStartInfo`, never the real one, wrapped in `try/catch` with a null-check) that falls back to the single-string `.Arguments` property with each argument manually quoted per real Win32 command-line rules (new `ConvertTo-Win32QuotedArgument` helper) whenever `ArgumentList` isn't safely usable. Verified the manual-quoting fallback against a real child process receiving arguments with embedded spaces, a quote, and a trailing backslash - all round-tripped byte-for-byte correctly |
| 2026-09-03 | Tests\Unit\Invoke-CustomTestConnectivity.Tests.ps1 | Added a `ConvertTo-Win32QuotedArgument` `Describe` block (TC30-TC34): a plain argument is unchanged, one with a space is quoted, an embedded quote is escaped, a trailing backslash with no surrounding spaces needs no quoting, and an empty string quotes to `""`. A real-child-process round-trip test was written but removed - this dev machine's PowerShell execution policy blocks running a `.ps1` file from `%TEMP%`, an environment quirk unrelated to the fix itself (the actual production code path never runs a script file this way, only an inline command string to `plink`/`pwsh`) - and was verified manually instead |
| 2026-09-03 | Docs\Lessons-Learned-PowerShell-Pester.md | Added Section 35 documenting the `ProcessStartInfo.ArgumentList` strict-mode unreliability and the disposable-probe fix pattern |
| 2026-09-03 | APIModules\Custom\Invoke-CustomTestConnectivity.ps1 | Per user request: the interactive Server Type prompt's description ("[1] Windows [2] Linux (or type the name)") invited typing a name; changed to "Enter 1 for Windows or 2 for Linux," and the displayed default is now translated back to its number even when a prior name (e.g. from `$Defaults['ServerType']`) is carried forward |
| 2026-09-03 | Docs\Testing-Plan.md | Added Findings F23 (`ArgumentList` crash fix) and F24 (Server Type prompt wording); updated the Custom checklist and Component Test Matrix row for TestConnectivity; added a Revision Log entry |
| 2026-09-03 | (branching) | Created `phase3-development-2026-09-03` off `main` for ongoing work, per user request - `origin/develop` was found to have diverged significantly (34 commits behind `main`, 5 ahead) and reconciling it was out of scope for a simple "create a new branch" request |
| 2026-09-03 | APIModules\Custom\Invoke-CustomTestConnectivity.ps1 | Per user report, live: the Linux SSH test timed out, guessed correctly by the user to be a host-key confirmation issue. Reproduced against a real test host (172.21.20.14, provided by the user) with its key removed from `known_hosts`: the PS7 SSH transport path hung for the full timeout, confirmed to be the unanswerable "unknown host key, continue connecting?" question in a non-interactive child process. Fixed by adding `-Options @{StrictHostKeyChecking='no'}` to the `New-PSSession -SSHTransport` call - verified live to reduce the hang to under a second. While verifying, also found and documented a separate, still-open limitation: a *known* host key can still time out on the password prompt itself, since this path has no console for OpenSSH to prompt through - a pre-existing limitation already noted in the module, now also called out in the timeout `ErrorMessage` with a pointer to `plink.exe` as the more reliable option |
| 2026-09-03 | Manage-Privilege.ps1, APIModules\Custom\Invoke-CustomTestConnectivity.ps1 (v1.3.0) | Per user request: added a new optional `ModuleMeta.CsvFilenameField`, naming an `InputData` column whose value (when present and non-blank) is appended to the single-interactive-run auto-saved CSV filename - e.g. `"Test Connectivity - 172.21.20.14 2026-09-03.csv"` instead of the same filename for every run. Implemented generically in `Invoke-ActionModule`'s CSV-save block (bracket notation, most modules don't declare it) rather than hardcoding a check for this one module by name, so any future module can opt in the same way. Only `Invoke-CustomTestConnectivity.ps1` declares it (`= 'Address'`) as of this change - `Invoke-AccountsAdd.ps1`/`Invoke-AccountsUpdate.ps1`, which also have an `Address` column, are unaffected since they don't opt in |
| 2026-09-03 | Docs\Architecture.md | Added three Design Decision rows: the `ProcessStartInfo.ArgumentList` probing pattern, the Linux SSH host-key auto-accept fix (including the still-open password-prompt limitation), and the new `CsvFilenameField` mechanism |
| 2026-09-03 | Docs\Testing-Plan.md | Added Findings F25 (host-key hang fix) and F26 (CsvFilenameField); updated the Custom checklist and Component Test Matrix row for TestConnectivity; added a Revision Log entry |
| 2026-09-04 | APIModules\Custom\Invoke-CustomTestConnectivity.ps1 (v1.4.0) | Per user report and follow-up direction: F25's host-key fix wasn't sufficient - a real Linux SSH auth attempt via the PS7 SSH transport path still timed out, confirming live the pre-existing password-prompt-without-a-console limitation already documented in this module. The user installed `plink.exe` at the project root and asked for it to be found there; mid-fix, also asked for the standard PuTTY install directories (Program Files (x86) before Program Files) to be checked. Added `Find-PlinkExecutable`, checking in that exact order: PATH, project root (`..\..\plink.exe` relative to this file), `%ProgramFiles(x86)%\PuTTY\plink.exe`, `%ProgramFiles%\PuTTY\plink.exe` (environment variables, not hardcoded drive letters). `Test-LinuxSshAuth`'s cascade was reordered to try plink first (previously it was only a fallback for when `pwsh` wasn't found, so a present pwsh always won even though plink is the documented, more reliable option for password validation) - removed the now-dead duplicate plink branch left over from the reorder. Verified live against the real test host with plink's own (registry-based, separate from OpenSSH's known_hosts) host-key cache genuinely empty: plink fails in well under a second with a clear "host key is not cached... Connection abandoned" message instead of hanging - confirmed via `plink --help` that PuTTY has no CLI equivalent to `StrictHostKeyChecking=no` (`-hostkey` requires the exact key already known), so no attempt was made to auto-accept it |
| 2026-09-04 | Tests\Unit\Invoke-CustomTestConnectivity.Tests.ps1 | Added a `Find-PlinkExecutable` `Describe` block (TC36-TC39, mocking `Get-Command`/`Test-Path`/`Resolve-Path`): found on PATH short-circuits before checking anything else, found at the project root, found in Program Files (x86), and not found anywhere returns `$null` |
| 2026-09-04 | .gitignore | Per user request: added `plink.exe` - a third-party tool the user placed in the project root for local testing, not project source |
| 2026-09-04 | Docs\Architecture.md | Split the "Linux SSH test auto-accepts an unrecognized host key" row into two: the existing PS7-path host-key fix, and a new row for trying plink first, including the live-confirmed fail-fast-not-hang behavior and why no auto-accept workaround was attempted for it |
| 2026-09-04 | Docs\Testing-Plan.md | Added Finding F27 (plink-first reorder and `Find-PlinkExecutable`); updated the Custom checklist and Component Test Matrix row; added a Revision Log entry |
| 2026-09-04 | APIModules\Custom\Invoke-CustomTestConnectivity.ps1 (v1.5.0) | Per user-provided real test credentials, ran the first fully live end-to-end test of this module's Linux path against 172.21.20.14 and found two more real bugs. (1) `Resolve-ConnectivityTarget` aborted the entire test before any port/auth check ran, because the target IP has no reverse-DNS (PTR) record - normal for an internal/test host, and unrelated to reachability; `GetHostEntry` throws for exactly this case. Fixed: a literal IP's `GetHostEntry` failure is now treated as a successful resolution with no hostname, not a hard abort - a name that fails to resolve is unchanged (still a hard failure). (2) Even past that, the first connection to any new host still failed outright needing a manual host-key trust step, since plink correctly refuses an unrecognized key rather than hanging (per the prior fix) but offers no way to bypass that. Found that plink's failure message always includes the fingerprint it computed; `Test-LinuxSshAuth` now parses it and retries once with `-hostkey <fingerprint>` automatically - the same trust-on-first-use tradeoff already used for the PS7 path. While implementing this, also found and fixed a real, separate bug: the plink lookup was called as `script:Find-PlinkExecutable` instead of a bare name, silently making it unmockable in tests (the same `script:`-qualified-call Pester gotcha already documented in this codebase) - changed to match the bare-name convention already used for every other helper call in this file. After both fixes, verified live end-to-end starting from a genuinely empty PuTTY host-key cache: the retry succeeded automatically, a real SSH login succeeded (`AuthStatus: Success`), a repeat run (now legitimately cached) succeeded on the first attempt, and a wrong password still failed cleanly in ~3 seconds with an accurate message rather than hanging |
| 2026-09-04 | Tests\Unit\Invoke-CustomTestConnectivity.Tests.ps1 | Added TC40 (a literal IP with no PTR record, using RFC 5737 TEST-NET-1 for a deterministic repro of the live DNS bug) and a new `Test-LinuxSshAuth - plink trust-on-first-use retry` `Describe` block (TC41-TC43: an unrecognized host key is retried once with the parsed fingerprint and the retry's result returned; a non-host-key failure like a wrong password is not retried; a host-key-shaped failure with no parseable fingerprint is not retried either) |
| 2026-09-04 | Docs\Architecture.md | Added two Design Decision rows: plink's host-key trust via a parsed retry (not a blanket bypass, since PuTTY offers none), and treating a literal IP's reverse-DNS failure as non-fatal |
| 2026-09-04 | Docs\Testing-Plan.md | Added Findings F28 (DNS/PTR-record fix) and F29 (plink trust-on-first-use retry); marked F26/F27/F28/F29 as fully live-confirmed in the Custom checklist - this is the first fix in this module confirmed working end-to-end against a real target with real credentials; updated the Component Test Matrix row; added a Revision Log entry |
| 2026-09-04 | README.md | Per user request: rebranded the title from "Idira Unified Scripts" to "aPePAS" and updated the clone-path example accordingly; reviewed the full document against the live codebase for accuracy and corrected several stale/incomplete descriptions - rewrote the Project Structure module counts to the verified totals (65 actions across 10 categories, with a per-category breakdown), added the missing `WhatIfDefault` profile field to the Configuration table, corrected the `Role_Template_Safe`/`Role_Group_Prefix` descriptions to cover their SafeMembers From-Template-Role usage, corrected `CPM_List` to describe it as a fallback behind the live `Get-CpmOptions` query used by three modules (not a single static list), added a note that the `%APPDATA%\IdiraUnifiedScripts\Profiles\` folder name is deliberately unchanged by the rebrand, added a Connectivity Testing features bullet and a Requirements note on `plink.exe`'s lookup order, corrected the WhatIf mode bullet to state it is set via the `-WhatIf` launch switch or a profile's WhatIf Default (not toggled mid-session), and added a top-of-file pointer to the new User-Guide.md |
| 2026-09-04 | Docs\User-Guide.md | Created: a new end-user usage guide (profiles, menu navigation, CSV batch modes, WhatIf mode, understanding results and CSV auto-save, module categories at a glance, session/token management, profile settings reference, special tools, troubleshooting). Two claims were caught and corrected before finalizing by checking directly against `Manage-Privilege.ps1`: back-navigation only ever goes up one level (no multi-level `[B2]`/`[B3]` shortcut exists despite a pre-existing code comment implying otherwise), and WhatIf mode has no in-session toggle - it is fixed for the whole session at launch |
| 2026-09-04 | Tests\Unit\Invoke-SafeMembersAdd.Tests.ps1, Invoke-SafesAddFromTemplate.Tests.ps1 | Fixed 7 pre-existing test failures (`PropertyNotFoundException: 'Count'`) surfaced by the routine pre-commit test run, unrelated to that commit's actual changes - all seven were already on the branch from earlier work. Root cause: `Get-SafeMembersSearchInOptions` returning its single-item Vault-only fallback, and `$r.Results \| Where-Object { ... }` matching exactly one row, both get unwrapped by PowerShell's pipeline from a one-element array to a bare scalar, which has no `.Count` property under strict mode. This was a test-only bug - the real call site (`Invoke-SafeMembersAdd.ps1`) already wraps the same call in `@(...)`, so production behavior was never affected. Fixed by wrapping all 7 affected assertions/assignments in `@(...)`, matching the existing production convention |
| 2026-09-04 | Docs\Lessons-Learned-PowerShell-Pester.md | Added Section 36 - a test asserting `.Count` on a function's return value or a `Where-Object` result must wrap it in `@(...)` first, since a single match unwraps to a scalar under PowerShell's pipeline and only reproduces for that exact cardinality, letting the bug hide until a single-match case is exercised |
| 2026-09-04 | (live testing) | Per user request: performed a full live end-to-end test pass, calling every one of the ~65 `Invoke-<Category><Action>` functions directly against a real Self-Hosted PVWA test/lab tenant (same code path a user's input triggers after the interactive menu), with real writes followed by cleanup. Found and fixed 4 real, previously-undiscovered live bugs (see Testing-Plan.md F31-F34): (1) `$InputData.Key` dot notation throwing `PropertyNotFoundException` when an optional key is entirely absent, across 9 modules (`Invoke-SafesAdd/Update/UnassignCPM/AssignCPM.ps1`, `Invoke-SafeMembersRemove.ps1`, `Invoke-GroupsDelete/Update/Add.ps1`) - the same bug class already fixed once for `$Defaults` in 2026-08-15 but never applied to these modules' own `$InputData` usage; (2) `GET /API/Platforms/Targets` actually wraps its results under a `Platforms` property (not a bare array as assumed) and its `search` query param does not match against `PlatformID` at all - both confirmed live - breaking the numeric-ID resolution shared by `Invoke-PlatformsCopy/Enable/Disable/Remove/Rename/SetPSMConfig.ps1`; (3) `PUT /API/Safes/{name}` requires `SafeName` in the body as well as the URL on this PVWA version (confirmed live via a raw-HTTP isolation test against psPAS's own documented shape, which omits it) - fixed in `Invoke-SafesUpdate/AssignCPM/UnassignCPM.ps1`; (4) `Invoke-SafesAddFromTemplate.ps1` failed with HTTP 403 trying to re-copy the template safe's own creator as an explicit member, since CyberArk already grants that access implicitly on the new safe - now excluded from the copy the same way role-prefixed and globally-excluded names are. Also confirmed 3 live-tenant limitations that are not aPePAS code bugs (Groups/AddMember's `POST /API/UserGroups/{id}/Members` returns an unconditional empty-body 400 even matching psPAS's exact request shape; Accounts LinkAccount/UnlinkAccount's singular and bulk endpoints both 404; a safe can return 409 on DELETE despite its own GET showing zero accounts, likely a CyberArk-side retention delay) |
| 2026-09-04 | Docs\Testing-Plan.md | Added Findings F31-F34 for the four live-confirmed fixes above, plus a note documenting the three live-tenant limitations found that are not addressable via aPePAS code changes |
| 2026-09-04 | Docs\Lessons-Learned-PowerShell-Pester.md | Added Section 37 - `$InputData.Key` dot notation is unsafe whenever a key is optional/caller-controlled, since it throws only when the key is entirely absent (not merely blank), a path unit tests can't catch if every fixture always supplies every key; only a live end-to-end test against a real system caught it here |
| 2026-09-04 | Tests\Unit\Invoke-Platforms{Copy,Enable,Disable,Remove,Rename,SetPSMConfig}.Tests.ps1, Invoke-SafesAddFromTemplate.Tests.ps1 | Added regression tests for the live-confirmed fixes: one test per Platforms module (PC10/PE10/PD10/PR10/PRN10/PPSM10) confirming numeric-ID resolution against the real `{Platforms:[...], Total:N}` response shape, and T09c confirming the template safe's creator is excluded from `Invoke-SafesAddFromTemplate`'s member copy |
| 2026-09-04 | Docs\Testing-Plan.md | Per user follow-up question ("are the current issues documented?"): added K08-K10 to the Known Issues / Risk Register for the 3 live-tenant limitations found during the same test pass (Groups/AddMember's unconditional 400, Accounts LinkAccount/UnlinkAccount's 404 on both endpoint shapes, Safes/Delete's 409 despite zero accounts) - these were previously only narrated in a note under Findings F31-F34, not cross-referenced in the structured Known Issues table this project uses to track genuinely open items |
| 2026-09-04 | APIModules\Safes\Invoke-SafesDelete.ps1 (v1.1.0) | Per user direction on K10 (safe delete blocked by HTTP 409 even with zero accounts, likely Safe History Retention - each account carries the retention setting active on the safe when it was added, so mixed settings across a safe's history can block a full purge): added a rename-instead fallback. On a 409, explains the likely cause and asks whether to rename the safe to `1_DEL_<SafeName>` (truncated to stay at or under CyberArk's 28-character limit) instead of just failing, appending a "Delete requested <date>" note to the description if one already exists. A successful rename now counts as a Success rather than a Failure, since the practical goal (getting the safe out of normal use) was achieved. Live-verified against all 3 real stuck safes from K10: 2 renamed successfully; the third also got a 409 on the rename PUT and remains stuck - K10 updated accordingly |
| 2026-09-04 | Tests\Unit\Invoke-SafesDelete.Tests.ps1 | Added D19-D24 covering the new 409 rename fallback: user accepts (renamed, reported as Success), user declines (original failure unchanged), name truncation for a long SafeName, description append when one exists, description left blank when none exists, and the rename PUT itself failing (reported as Failure with both error messages) |
| 2026-09-04 | Docs\Testing-Plan.md | Added Finding F35 for the Safes/Delete rename fallback; updated K10 to reflect the partial live-confirmed fix (2 of 3 stuck safes resolved, one still stuck on both delete and rename) |
| 2026-09-04 | Docs\Lessons-Learned-PowerShell-Pester.md | Added Section 38 - an `It`/`Describe` test name containing `<Word>` is treated by Pester as a `-ForEach` template placeholder even when no `-ForEach` is used, and can throw "variable cannot be retrieved" only when run as part of the full suite (not in isolation) if no same-named variable happens to be in scope; discovered when a new test's name accidentally included `<SafeName>` as plain descriptive text |
| 2026-09-04 | Auth\CyberArk.Auth.ISPSS.psm1 | Per user request, reviewed `C:\Code\References\CyberArk ISPSS\` to verify the ISPSS/SaaS auth methods are ready for testing - the "API Token" folder's two docs are the only ones there that document authentication itself (the Access API and User Management APIs folders are generated SDKs for post-auth operations). Found and fixed a severe bug: `Invoke-ISPSSClientCredentials` unconditionally read `$resp.refresh_token` after `POST /oauth2/platformtoken`, but CyberArk's own reference documents that response as `{access_token, token_type, expires_in}` only - confirmed via an isolated repro that dot-accessing a genuinely absent JSON property throws under `Set-StrictMode`, meaning every ClientCredentials login would have crashed immediately after a successful token request. Fixed by guarding every field with `PSObject.Properties[]`, in both `Invoke-ISPSSClientCredentials` and the analogous unguarded pattern in `Update-ISPSSAuthToken`'s refresh branch. Also noted (not changed, insufficient documentation) that the refresh branch calls `/oauth2/platformtoken` again rather than the separately-documented `/oauth2/refreshplatformtoken` - moot in practice since the fix means a ClientCredentials token never carries a refresh_token to reach that branch |
| 2026-09-04 | Tests\Unit\CyberArk.Auth.ISPSS.Tests.ps1 | Created (new file) - the first automated test coverage this module has ever had. ISPSS-CC01-CC06 cover `Get-ISPSSAuthToken`/`Update-ISPSSAuthToken`'s ClientCredentials path: no exception against the documented response shape, correct field mapping, the exact endpoint/body called, graceful handling if a refresh_token is present anyway, and a missing expires_in falling back to 3600s. Interactive and SSO remain live-only (MFA challenge loop / WebView2 browser), matching the Self-Hosted SAML/OIDC/PKI/PKIPN precedent |
| 2026-09-04 | Docs\Testing-Plan.md | Added Finding F36 for the ISPSS ClientCredentials crash fix; updated the Component Test Matrix row for `CyberArk.Auth.ISPSS.psm1`; added a new "ISPSS Auth — Manual Integration Test Procedures" section (AI01-AI12), the ISPSS counterpart to the existing Self-Hosted Auth section, covering all 3 methods plus tenant discovery and refresh behavior |
| 2026-09-04 | Docs\Architecture.md | Per user follow-up ("verify all necessary documents have been updated"): found the Design Decisions table hadn't been touched since before the live end-to-end module test pass, even though its own established convention already covers this exact class of entry (e.g. "OLACEnabled is not a supported field", "401 handling always invalidates the session"). Added 6 rows for F31-F36 and the Safe/Delete rename fallback: the Platforms/Targets response-shape and search-param fix, the Safes PUT SafeName requirement, Add Safe From Template's creator exclusion, the Safe delete rename-instead fallback, the $InputData bracket-notation fix across 9 modules, and the ISPSS ClientCredentials response-field guards |
| 2026-09-04 | Docs\User-Guide.md | Added a Troubleshooting bullet explaining the new Safe Delete rename-fallback prompt (HTTP 409, likely Safe History Retention) introduced by F35, so an end user encountering it for the first time understands why they're being asked to rename instead of delete |
| 2026-09-04 | Modules\CyberArkComms.psm1 | Per user request, adding platform download support: `Invoke-CyberArkAPI` decided JSON-vs-binary purely by trying `ConvertFrom-Json` and catching failure, never inspecting the actual `Content-Type`/`Content-Disposition` headers. Confirmed via a local `HttpListener` test under real Windows PowerShell 5.1 that `.Content` is already a correct `byte[]` for a properly-labeled binary response, but becomes a lossily, irreversibly decoded string when `Content-Type` implies a text charset even though the body is binary - no encoding trick recovers the original bytes afterward. `RawContentStream` held the exact original bytes in every case tested. Now reads headers explicitly: uses `.Content` directly when it's already `byte[]`, falls back to `RawContentStream` when a `Content-Disposition` header appears on a non-JSON response (the mislabeled-file case). Added `DataType='File'` (already documented, never implemented, in `Interfaces.md`) and a new `SuggestedFileName` field. Also fixed all 11 pre-existing inline mock response objects in `CyberArkComms.Tests.ps1`, which had no `Headers` property at all - a pre-existing test-realism gap this fix exposed |
| 2026-09-04 | Tests\Unit\CyberArkComms.Tests.ps1 | Added C37-C40 for the new binary/File response handling (correctly-labeled binary, mislabeled `text/html` with `Content-Disposition`, binary with no `Content-Disposition`, and confirming normal JSON responses are unaffected); updated `New-MockWebResponse` to always include `Headers` (and optionally `RawBytes`), matching what a real response always has |
| 2026-09-04 | APIModules\Platforms\Invoke-PlatformsExport.ps1 (new module) | Per user request: added platform download/export, the counterpart to `Platforms/Import`, matching psPAS's `Export-PASPlatform.ps1`. Supports all 4 of its target types (`PlatformID`, `RotationalGroupID`, `DependentID`, `GroupPlatformID`) via exactly-one-of-four `InputData` columns (per user direction on scope); auto-saves to the profile's `OutputFolder` (per user direction on save location) using the filename CyberArk suggests via `Content-Disposition`, or a generated fallback name. Unlike the other Platform write actions, this endpoint takes the target's own string ID directly - no numeric-ID resolution via `/API/Platforms/Targets` needed. Module count: 65 -> 66 |
| 2026-09-04 | Tests\Unit\Invoke-PlatformsExport.Tests.ps1 (new file) | PE01-PE13 covering all 4 endpoint variants, validation, API failure, unexpected non-File success, save failure, and WhatIf. **Live-verified end-to-end for the `PlatformID` case**: downloaded a real platform `.zip` from a live Self-Hosted tenant, confirmed byte-for-byte valid by actually opening it with `System.IO.Compression.ZipFile` and finding the platform's real `Policy-WinServerLocal.xml`/`.ini` files inside |
| 2026-09-04 | Docs\Interfaces.md | Updated the API Response Object contract to match the now-real implementation: added `SuggestedFileName`, corrected `RawResponse`'s description for a `File` response (empty, since `Data` holds the actual bytes), and added a new subsection explaining exactly how `DataType` is determined from response headers |
| 2026-09-04 | Docs\Architecture.md | Added 2 Design Decision rows: header-based binary/JSON detection in `Invoke-CyberArkAPI`, and the new `Platforms/Export` module's scope/endpoint design |
| 2026-09-04 | Docs\Testing-Plan.md | Added Findings F37 (the `Invoke-CyberArkAPI` binary-response fix) and F38 (the new `Platforms/Export` module); added a Component Test Matrix row; updated the module-count references in the Self-Hosted vs. ISPSS caution section (61 of 65 -> 62 of 66; Platforms 8 of 9 -> 9 of 10 dual-use actions) |
| 2026-09-04 | Docs\Lessons-Learned-PowerShell-Pester.md | Added Section 39 - `Invoke-WebRequest`'s `.Content` can irreversibly corrupt binary data when `Content-Type` implies a text charset; `RawContentStream` is the only reliable source, confirmed via a local `HttpListener` test rather than assumed |
| 2026-09-04 | README.md | Updated the Project Structure module counts for the new `Platforms/Export` action: 65 -> 66 total, Platforms 9 -> 10 actions |
| 2026-09-04 | APIModules\Custom\Invoke-CustomExportPlatformDetails.ps1 (new module) | Per user request: a new Custom-category bulk report that downloads every *active* platform (any type) via F38's export endpoint and builds a spreadsheet summarizing each platform's `Policy-<id>.ini`/`.xml` contents, plus a column listing any bundled files that are not the two policy files (a `META-INF` folder, if present, excluded per user direction). Confirmed live that `/API/Platforms` list entries nest fields under a `general` sub-object, not at the root, and that the list's `platformType` field is unrelated to psPAS's `GroupPlatformID` concept - a `platformType="group"` platform exports successfully via the same standard endpoint used for every other platform, so only that one endpoint is needed regardless of type. INI/XML settings are parsed into `Ini.*`/`Xml.*` columns using XPath throughout (never dot-notation); columns are the union of every setting seen across all active platforms, blank-filled where a given platform lacks one. Module count: 66 -> 67 |
| 2026-09-04 | Tests\Unit\Invoke-CustomExportPlatformDetails.Tests.ps1 (new file) | PPD01-PPD11, including a `New-TestPlatformZipBytes` helper that builds real in-memory `.zip` archives via `System.IO.Compression.ZipArchive` so the tests exercise the actual zip-extraction/INI/XML parsing code rather than mocking it away. All 1099 unit tests pass. **Live-verified end-to-end** against the real Self-Hosted tenant: 10 of 15 platforms were active and all 10 processed successfully, producing 104 dynamic columns, with type-specific settings spot-checked via CSV to populate only where applicable. No real example of an extra bundled file or `META-INF` folder existed on this tenant, so the `OtherFiles`/exclusion logic remains unit-tested only, not live-confirmed |
| 2026-09-04 | Docs\Testing-Plan.md | Added Finding F39 (the new `Custom/ExportPlatformDetails` module); added a Component Test Matrix row; updated the Custom checklist (6 -> 7 actions) and the dual-use module-count reference (62 of 66 -> 63 of 67) |
| 2026-09-04 | README.md | Updated the Project Structure module counts for the new `Custom/ExportPlatformDetails` action: 66 -> 67 total, Custom 6 -> 7 actions |
| 2026-09-04 | APIModules\Policies\Invoke-PoliciesGetMasterPolicy.ps1 | Per user request: "Within CyberArk's Self-Hosted and Privilege Cloud have a master policy. Is there a way to pull this information? I would like to include it in the Export All." Researched psPAS's `Get-PASMasterPolicy.ps1` (asserts `-SelfHosted` only, no ISPSS branch) and the local CyberArk ISPSS reference materials (no Privilege Cloud master-policy endpoint documented) - neither source confirms Privilege Cloud actually lacks the endpoint, only that it's undocumented. Per explicit user direction, changed `SupportedSystems` from `@('SelfHosted')` to `@('ISPSS', 'SelfHosted')` and added `IncludeInExportAll = $true`; a failure on ISPSS is handled by this module's pre-existing non-fatal error path (already handled 401/other-error distinctly), not a new code path. `Invoke-PoliciesSetMasterPolicy.ps1` (a write) was deliberately left Self-Hosted-only. |
| 2026-09-04 | APIModules\Custom\Invoke-CustomExportAll.ps1 | Added a new generic `ModuleMeta.IncludeInExportAll` opt-in flag (the read counterpart to the existing `ExcludeFromExportAll` opt-out) so a module with an action other than `List`/`ListAuthMethods` - like Policies' single-row `GetMasterPolicy` snapshot - can still be picked up by Export All's module discovery. Version 1.1.0 -> 1.2.0 |
| 2026-09-04 | Tests\Unit\Invoke-PoliciesGetMasterPolicy.Tests.ps1, Tests\Unit\Invoke-CustomExportAll.Tests.ps1 | Added PGMP08/PGMP09 (IncludeInExportAll is set; a 404 is a non-fatal Failure) and updated PGMP02 for dual-use SupportedSystems; added 2 cases to the ExportAll tests confirming the IncludeInExportAll opt-in is honored and that a module without it is still excluded. All 1103 unit tests pass. **Live-verified end-to-end** against the real Self-Hosted tenant: Export All correctly discovered and ran Policies/GetMasterPolicy, saving a real CSV with real policy values. ISPSS/Privilege Cloud behavior remains unconfirmed - no Privilege Cloud tenant was available to test against |
| 2026-09-04 | Docs\Testing-Plan.md | Added Finding F40 (Master Policy included in Export All); moved the `Policies/GetMasterPolicy` Component Test Matrix row from the Self-Hosted-only table to the dual-use table and updated its verification notes; updated the SelfHosted-only/dual-use module-count references (4/63-of-67 -> 3/64-of-67) and the Policies and Custom functional checklists |
| 2026-09-04 | README.md | Updated the Policies Project Structure line to reflect `GetMasterPolicy` now being dual-use (ISPSS attempted but unconfirmed), distinct from `SetMasterPolicy` which remains Self-Hosted only |
| 2026-09-04 | Docs\Testing-Plan.md, Docs\Architecture.md, README.md, Docs\User-Guide.md | Documentation-only update: the user verified live against a real Privilege Cloud tenant that no Master Policy equivalent endpoint exists on ISPSS at all, resolving F40's earlier "undocumented, not confirmed absent" uncertainty to "confirmed absent." The user judged the current behavior fine as-is (declared dual-use, fails as a clean non-fatal `Failure` on ISPSS via the module's pre-existing error handling), so no code or test changes were made - only the "unconfirmed" language throughout these 4 files was corrected to reflect the confirmed finding |
| 2026-09-04 | Docs\Testing-Plan.md | Documentation-only update, per user report: "LinkAccount is working now." Added Finding F41 documenting that the previously-logged live-tenant limitation (`POST /API/Accounts/{id}/LinkAccount` and its bulk sibling both returning HTTP 404 against a confirmed-existing account) no longer reproduces - the user retested live and confirmed both the single-account `LinkAccount` action and the bulk `Link/Bulk` endpoint now work correctly. No code or test changes were needed (this was always a plain, correct request matching psPAS's documented shape; the 404 was environment-side, not a code bug). Removed the item from the "NOT fixable via aPePAS code changes" list, updated the `Accounts / LinkAccount` and `Accounts / UnlinkAccount` Component Test Matrix rows and the Accounts functional checklist to reflect the live confirmation |
| 2026-09-04 | APIModules\Groups\Invoke-GroupsAddMember.ps1, Invoke-GroupsRemoveMember.ps1 | Per user report: "For group member add. In the body, the memberid is actually the username. So instead of 32 it would be ca_jesse." This resolved the project's previously-logged "unfixable" `POST /API/UserGroups/{id}/Members` HTTP 400 live-tenant limitation into a real, confirmed code bug: `Invoke-GroupsAddMember.ps1` cast `memberId` to `[int]`, but CyberArk's API expects the username string there, confirmed against psPAS's own `Add-PASGroupMember.ps1`/`Remove-PASGroupMember.ps1` (`[string]$memberId`, and `$Member` aliased to `UserName`). Removed the `[int]` cast; corrected both modules' descriptions/prompts/InputSchema from "numeric ID" to "username"; fixed the interactive "search by username" fallback to return `-IdProperty 'username'` instead of `'id'` (it would have looked up the right user but returned the wrong field for use as MemberID) |
| 2026-09-04 | APIModules\Groups\Invoke-GroupsGetMembers.ps1 | Found and fixed while live-verifying the AddMember fix above: the member-mapping loop crashed under strict mode because a real member entry is only `{id, username}` - no `userType`/`componentUser` at all - and the loop dot-accessed both unconditionally, silently turning every member into a `Failure` instead of a `Result` row. Guarded all 4 field accesses with `PSObject.Properties[...]` existence checks, defaulting the two absent fields to `$null` |
| 2026-09-04 | Tests\Unit\Invoke-GroupsAddMember.Tests.ps1, Invoke-GroupsRemoveMember.Tests.ps1, Invoke-GroupsGetMembers.Tests.ps1 | Updated all `MemberID` test fixtures from numeric-looking strings to a realistic username so the tests can't pass merely because a numeric string still parses as `[int]`; added GM17 using a member object shaped exactly like the real live response (`{id, username}` only) under `Set-StrictMode -Version Latest`. All 1104 unit tests pass. **Live-verified end-to-end** against the real Self-Hosted tenant: created a temporary test group, added a real username as a member (HTTP 201, previously always HTTP 400), confirmed membership via the now-fixed `GetMembers` ("Members retrieved: 1" with correct values, previously "0" due to the crash), removed the member (HTTP 204), and deleted the test group to clean up |
| 2026-09-04 | Docs\Testing-Plan.md | Added Findings F42 (Groups/AddMember username fix) and F43 (Groups/GetMembers missing-field crash fix); removed the AddMember item from the "NOT fixable via aPePAS code changes" list (it was a real code bug all along, not an environment limitation); updated the `Groups / AddMember`, `Groups / GetMembers`, and `Groups / RemoveMember` Component Test Matrix rows and the Groups functional checklist |
| 2026-09-09 | APIModules\Safes\Invoke-SafesAdd.ps1 (v1.3.0), Invoke-SafesUpdate.ps1 (v1.2.0) | Started Phase 3 (Validation hardening) of `aPePAS-Improvement-Plan-2026-09-02.md` - reviewed the full plan against the current codebase first and confirmed Phases 0-2 and most of Phase 1/4 were already complete, leaving Phase 3 as the next unstarted item (and the branch's namesake). Both modules now reject a `SafeName` over 28 characters, with leading whitespace, or containing a reserved character (`\ / : * < > " . \|`) before calling the API, matching psPAS's `Add-`/`Set-PASSafe.ps1` `[ValidateLength(0,28)]` plus the Vault's own documented reserved-character rule - checked against the raw (untrimmed) input for the whitespace case, since trimming first would silently accept what the Vault itself rejects. Updated both modules' `InputSchema`/interactive-prompt descriptions to state the rule |
| 2026-09-09 | APIModules\Applications\Invoke-ApplicationsAdd.ps1 (v1.2.0) | Same Phase 3 pass: added a 0-23 range check to `AccessPermittedFrom`/`AccessPermittedTo` (previously only checked as parseable integers) and corrected comments/error text that mislabeled these hour-of-day fields as "epoch seconds"; added length/charset validation matching psPAS's `Add-PASApplication.ps1` - `AppID` (1-127 chars, no `&`), `Description` (<=99), `BusinessOwnerFName` (<=29), `BusinessOwnerPhone` (<=24) - each a non-fatal per-item `Failure`, consistent with the module's existing validation pattern |
| 2026-09-09 | Tests\Unit\Invoke-SafesAdd.Tests.ps1, Invoke-SafesUpdate.Tests.ps1, Invoke-ApplicationsAdd.Tests.ps1 | Added A21-A24/U18-U20 (Safes: over-length, reserved character, leading whitespace, and exactly-28-chars-passes) and 7 new cases (Applications: range violations, AppID length/charset, Description length, BusinessOwnerFName/Phone length). Fixed one pre-existing test that used epoch-seconds-shaped `AccessPermittedFrom`/`To` values now correctly rejected by the new range check. All 1118 unit tests pass. None of this session's changes have been live-verified |
| 2026-09-09 | Docs\Testing-Plan.md | Added Findings F44 (Safes SafeName validation) and F45 (Applications validation hardening); updated the `Safes / Add`, `Safes / Update`, and `Applications / Add` Component Test Matrix rows to flag the new validation as unit-tested-only; added a Revision Log entry |
| 2026-09-09 | Modules\CyberArkComms.psm1 | Per user request ("Generalize pagination") - Phase 4 of `aPePAS-Improvement-Plan-2026-09-02.md`: `Invoke-CyberArkAPI`'s pagination logic previously only recognized a hardcoded list of collection property names (`value`, `Safes`, `Members`, `Accounts`, `Users`, `Platforms`, `Groups`), duplicated at two call sites - an endpoint using any other name would have silently returned only its first page. Added a new private helper, `Find-CyberArkCollectionProperty`: tries the same known-name list first (preserving exact behavior for every endpoint in use today), then falls back to the first array-valued property on the response object - the same layered approach psPAS's `Get-NextLink.ps1` uses (confirmed by reading it directly at `C:\Code\References\pspas.8.0.20\Private\Get-NextLink.ps1`). Both of `Invoke-CyberArkAPI`'s call sites now use this one shared helper instead of duplicating the list |
| 2026-09-09 | Tests\Unit\CyberArkComms.Tests.ps1 | Added C41 (a made-up `Widgets` collection property not in the known list, paginating correctly across 3 pages via the new fallback) and C42 (single-page response with the same unrecognized property, returned correctly). All 1120 unit tests pass, including the pre-existing `value`-keyed pagination tests unchanged, confirming no behavior change for any endpoint already in use |
| 2026-09-09 | Docs\Architecture.md | Added a Design Decision row for the new pagination collection-property detection strategy |
| 2026-09-09 | Docs\Testing-Plan.md | Added Finding F46 for the pagination generalization; added a Revision Log entry |
| 2026-09-09 | APIModules\SafeMembers\Invoke-SafeMembersAdd.ps1 (v1.4.0), Invoke-SafeMembersUpdate.ps1 (v1.2.0) | Per user request ("Update aPePAS ReadOnly to ReadOnlyStrict and document this"), superseding Phase 5 of `aPePAS-Improvement-Plan-2026-09-02.md` (which called for a documentation-only callout) with an actual rename. Renamed the SafeMembers `ReadOnly` permission-role preset to `ReadOnlyStrict` throughout both modules - `InputSchema`/prompt text, default values, numbered menus, and switch cases - to avoid the same name meaning something different than PVWA's/psPAS's own built-in `ReadOnly` role (which grants `retrieveAccounts`; aPePAS's preset never does). `Get-PermissionSet`'s `switch` in both modules never had an explicit case for the old name - both modules always routed it through the same `default` branch as any other unrecognized role - so a CSV or saved profile default still carrying the old literal `ReadOnly` keeps producing the identical permission set with no migration needed |
| 2026-09-09 | Tests\Unit\Invoke-SafeMembersAdd.Tests.ps1 | Renamed MA11's fixture/test name to `ReadOnlyStrict`; added MA11a confirming the legacy literal `ReadOnly` still resolves to the identical permission set. `Invoke-SafeMembersUpdate.Tests.ps1` needed no changes - its fixture already used `EndUser`. All 1121 unit tests pass |
| 2026-09-09 | README.md, Docs\User-Guide.md, Docs\Architecture.md | Added a README Features bullet and a User-Guide Troubleshooting entry explaining the `ReadOnlyStrict` naming rationale; added a Design Decision row to Architecture.md; corrected a stale `ReadOnly` mention in the AddFromTemplateRole/UpdateFromTemplateRole Design Decision row |
| 2026-09-09 | Docs\Testing-Plan.md | Added Finding F47; updated the `SafeMembers / Add` and `SafeMembers / Update` Component Test Matrix rows; added a Revision Log entry |
| 2026-09-11 | Docs\Testing-Plan.md | Per user request ("Create a test plan for testing the Privilege Cloud version. This will need to prompt for interactive authentication."), added a new "Privilege Cloud (ISPSS) Full Functional Checklist" section - the ISPSS counterpart to the existing Self-Hosted Full Functional Checklist. Scoped specifically to the `Interactive`/`SSO` auth methods (the ones that require a human at the console for the whole session), not the silent `ClientCredentials` grant. Excludes the 3 Self-Hosted-only actions and front-loads a "known ISPSS-specific behavior" callout (the `GroupType='Vault'` quirk, `Policies/GetMasterPolicy`'s already-confirmed absence, the `Settings/Timeout` 404 fallback) so a tester running this pass doesn't mistake already-known, expected behavior for a new bug. Documentation-only - no code or tests changed |
| 2026-09-18 | README.md | Per user report, live: `SSO` authentication failed with `Microsoft.Web.WebView2.WinForms.dll not found` during the interactive Privilege Cloud test pass. Confirmed directly on the user's machine that the assembly genuinely didn't exist anywhere (the Edge WebView2 Runtime itself was already installed). Fixed locally by downloading the official `Microsoft.Web.WebView2` NuGet package and placing `Microsoft.Web.WebView2.WinForms.dll`, `Microsoft.Web.WebView2.Core.dll`, and `WebView2Loader.dll` into `Auth\WebView2\`, one of `Import-WebView2Assembly`'s existing candidate search paths - verified live that the assembly now loads. Rewrote the Requirements callout: names the exact files needed and where to get them, covers `SSO` (ISPSS) alongside `SAML`/`OIDC` (Self-Hosted) which it previously omitted, and corrects an overstatement - `-WebView2AssemblyPath` is not actually reachable through `Manage-Privilege.ps1`, despite the error message suggesting it |
| 2026-09-18 | .gitignore | Added `Auth/WebView2/`, matching the existing `plink.exe` precedent - a locally-installed third-party dependency, not project source, not distributed with the repo |
| 2026-09-18 | Docs\Testing-Plan.md | Added Finding F48 (the missing WebView2 assembly, confirmed and fixed live) and K11 (the `-WebView2AssemblyPath` parameter the error message suggests is unreachable through the normal driver - not fixed this session, documented as a known gap); added a Revision Log entry |
| 2026-09-18 | Auth\CyberArk.Auth.Common.psm1 (`Invoke-WebView2Window`) | Per user report, live, immediately after the F48 fix: the WebView2 window opens but is completely blank with no error. Found a real bug reading the code directly: `CoreWebView2InitializationCompleted`'s handler never checked `$e.IsSuccess` before calling `.Navigate()` - an environment-creation failure left `$wv.CoreWebView2` `$null`, and calling `.Navigate()` on it inside that event handler failed silently with no visible surface to report to, matching the exact symptom reported. Added the `IsSuccess` check (captures `$e.InitializationException.Message` into a `$state.Result = @{ Error = ... }` and closes the window on failure instead of proceeding), wrapped the subsequent `.Navigate()` call in `try/catch` for the same reason, and updated `Invoke-WebView2Window`'s outer function to `throw` a captured `Error` key instead of only ever the generic "timed out or cancelled" message. Renamed the handler's `$sender` param to `$wvSender` per a PSScriptAnalyzer automatic-variable-collision warning. All 1121 unit tests still pass (this class of code is not unit-testable, matching the project's established Testing Boundaries) - not yet live-verified |
| 2026-09-18 | Docs\Testing-Plan.md | Added Finding F49; added a Revision Log entry |
| 2026-09-18 | Auth\CyberArk.Auth.Common.psm1 (`Invoke-WebView2Window`) | Per user report, live, immediately after F49: the window closed and a new error surfaced - `Index was out of range... Parameter name: index`. Traced every collection-indexing site in the SAML call chain and found the culprit: `$ps.Streams.Error[0]`, a raw index-0 access into a `PSDataCollection<ErrorRecord>` that throws exactly this exception (matching parameter name and all) when `HadErrors` is `$true` but the collection is actually empty. Replaced with `Select-Object -First 1`, which degrades to a fallback message instead of throwing. Also corrected a stale note in F49: the user confirmed `pvwa.company.com` is their real lab address, not this project's documentation placeholder - a coincidental match, not the cause of anything |
| 2026-09-18 | Auth\CyberArk.Auth.Common.psm1, CyberArk.Auth.SelfHosted.psm1, CyberArk.Auth.ISPSS.psm1 | Per user request ("Is there a way to add some messaging to show what it is doing? What address it is trying to reach?"): added a status `Label` docked to the top of the WebView2 window itself, showing `"Connecting to: <url>"` initially and `"Loading: <url>"` (reading `$wv.CoreWebView2.Source` live) on every ~750ms timer tick thereafter - directly visible inside the GUI, not just a log file. Upgraded `Invoke-SelfHostedSAML`/`Invoke-SelfHostedOIDC`/`Invoke-ISPSSSO`'s single `Write-Verbose` line each (invisible by default) to visible `Write-Host` lines stating the exact URL being opened and the host being watched for the redirect back |
| 2026-09-18 | Docs\Testing-Plan.md | Added Finding F50; corrected F49's `pvwa.company.com` note; added a Revision Log entry |
| 2026-09-18 | Manage-Privilege.ps1 | Per user request ("Add the ability to backup one or more profiles and to restore one or more profiles. Also allow specifying a profile at startup"). Researched the codebase first and asked 4 clarifying questions (DPAPI token portability scope, whether `-StartProfile` should skip the menus, multi-select UX, backup location) before implementing - answers: back up everything and warn on non-portable restore; skip straight to Connect; numbered checklist + `all`; prompt for location each time. Added `Backup-DriverProfiles`/`Restore-DriverProfiles` (core, parameter-driven), `Get-BackupSavePath`/`Get-BackupOpenPath` (WinForms dialog + console fallback, mirroring/extending `Get-CsvSavePath`), `Invoke-ProfileBackupFlow`/`Invoke-ProfileRestoreFlow` (interactive numbered-checklist wrappers), and two new `script:` zip-byte-stream helpers (`Add-ZipEntryFromFile`/`Save-ZipEntryToFile`) - manual `ZipArchive` I/O rather than `ZipFile`/`CreateEntryFromFile`, since those live in a separate assembly this project doesn't otherwise depend on. A backup is one `.zip` per operation, containing each profile's `.json`, `.cred` if present, and a `_manifest.json` recording the creating Windows user+machine; restore compares that to the current user+machine and warns (rather than silently failing) when a `.cred` can't be decrypted there, since DPAPI is user+machine-locked by design. Wired into the profile list as `[K]` Backup / `[X]` Restore |
| 2026-09-18 | Manage-Privilege.ps1 | Same request, startup half: extracted the profile-detail menu's `'C'` (Connect) case (~220 lines: token load/validate/refresh, interactive re-auth, save, session setup) into a standalone `Invoke-ProfileConnect` function - identical behavior, plus a `-NoPause` switch that suppresses interactive `Read-Host` pauses for the new unattended path. Added a `-AutoConnect` launch switch (requires `-StartProfile <name>`, validated at startup with a clear error otherwise) that calls it directly, skipping the profile list/detail menus entirely - applies only to the initial launch (a session `Restart` still returns to the interactive list, unchanged), and falls back to the normal interactive flow on any failure (profile not found, incomplete, or an auth error) rather than exiting |
| 2026-09-18 | Manage-Privilege.ps1 | Note: an intermediate `Get-Content -Raw \| Set-Content` round-trip used while extracting `Invoke-ProfileConnect` stripped the file's required UTF-8 BOM (Windows PowerShell 5.1 needs it - see Lessons-Learned-PowerShell-Pester.md). Caught immediately via a byte-level check and re-written with an explicit `UTF8Encoding($true)` writer before any further edits; confirmed BOM and CRLF line endings both intact afterward |
| 2026-09-18 | Tests\Unit\Manage-Privilege.Tests.ps1 | No new Pester tests added - this file documents a specific, reproduced Pester/file interaction hang tied to `Invoke-FileWriteWithRetry` (a `Confirm-Action`-driven helper), and `Restore-DriverProfiles` also calls `Confirm-Action` on a same-name conflict. Instead verified `Backup-DriverProfiles`/`Restore-DriverProfiles` end-to-end (byte-exact round-trip, manifest correctness, cross-machine portability-warning path via a synthetic foreign manifest, non-existent-profile handling) via a standalone dot-sourced repro script - the same technique this project's own test-file comments already validate as reliable for this file. All checks passed. All 1121 existing unit tests still pass (no regression). `-AutoConnect`'s parameter validation was also spot-checked directly |
| 2026-09-18 | README.md, Docs\User-Guide.md, Docs\Architecture.md | Added 2 README Features bullets (backup/restore, `-StartProfile`/`-AutoConnect`); added "Backing up and restoring profiles" and "Starting at a specific profile" subsections to User-Guide.md's Profiles section; added 3 Architecture.md Design Decision rows (zip format choice, cross-machine/user restore handling, the shared `Invoke-ProfileConnect` extraction) |
| 2026-09-18 | Docs\Testing-Plan.md | Added Finding F51; added 2 Component Test Matrix rows; added D26-D36 manual test procedures; added a Revision Log entry. While here, fixed a malformed Findings-table row left over from the F50 entry (a duplicated trailing fragment had given it 8 columns instead of the table's 5) |
| 2026-09-18 | APIModules\Custom\Invoke-CustomTestConnectivity.ps1 (v1.6.0) | Per user request ("Add the ability to add additional ports to the connectivity test as an option for interactive and through CSV"): added an optional `AdditionalPorts` `InputSchema` column (comma-separated) and a matching interactive prompt. New `ConvertTo-PortList` helper validates each token as an integer 1-65535, de-duplicates, and fails the item with a specific message naming any bad token (not silently dropped) - matching this project's established validation convention. The parsed ports are tested and appended to the existing `PortCheck` output after the built-in per-`ServerType` ports (135/139/445/3389 for Windows, 22 for Linux); a port duplicating a built-in one is skipped so it's never tested or reported twice. Deliberately informational only - never affects `AuthStatus`, which stays governed solely by the primary port (445/22), matching the literal scope of the request |
| 2026-09-18 | Tests\Unit\Invoke-CustomTestConnectivity.Tests.ps1 | Added a `ConvertTo-PortList` `Describe` block (TC45-TC53: blank input, single/multiple ports, whitespace handling, de-duplication, an invalid token, both range boundaries) and integration cases in the existing Windows/Linux flow and validation blocks (TC54-TC58). All 1136 unit tests pass (1121 + 15 new) |
| 2026-09-18 | README.md, Docs\User-Guide.md | Updated the Connectivity testing / Test Connectivity descriptions to mention the new `AdditionalPorts` field |
| 2026-09-18 | Docs\Testing-Plan.md | Added Finding F52; updated the `Custom / TestConnectivity` Component Test Matrix row and the Self-Hosted checklist item; added a Revision Log entry |
| 2026-09-18 | Manage-Privilege.ps1 | Per user request ("The next step is to allow this script to be called by automation... If interaction is required have it exit stating the issue"). Planned first (approved plan at `glistening-exploring-charm.md`) after two research passes plus direct code verification found every auth method's fresh login is interactive with no exception, that module functions dot-sourced inside a normally-called function vanish once it returns, and that several places (mid-run token refresh, CSV auto-save, early startup logging) would still hang or misbehave unattended. Added `-Category`/`-Action` (both require `-StartProfile`, trigger a new `$script:AutomationMode` flag set once in the Configuration region) and `-InputFile`/`-InputJson` (mutually exclusive) launch parameters. New `Invoke-AutomatedAction` entry point: resolves the profile, connects, re-dot-sources loaded modules into its own scope (duplicated from `Invoke-SessionLoop`'s identical loop rather than factored into a shared helper - dot-sourcing inside a function is discarded when it returns), resolves `InputData` from `-InputFile`/`-InputJson`/empty (a module's interactive `Get-<Category><Action>Input` is never called), and maps the result to an exit code via new `Get-AutomationExitCode` (0 success / 2 partial failure / 3 could not run; 1 crash is unchanged, the existing top-level handler) |
| 2026-09-18 | Manage-Privilege.ps1 | Same request: added `$script:AutomationMode` guards at the 4 confirmed interactive touchpoints. `Invoke-ProfileConnect`'s fresh-auth block now logs why and returns `$null` before ever reaching a `Get-Credential`/certificate-picker/WebView2 prompt (automation is therefore only viable on top of an already-established, silently-refreshable saved session - never a cold start). `Invoke-TokenRefresh` now routes straight to the same silent `Update-SelfHostedAuthToken`/`Update-ISPSSAuthToken` path startup already uses, or fails immediately for SAML/OIDC (Self-Hosted)/Interactive/SSO (ISPSS), which have no silent refresh path at all. `Invoke-FileWriteWithRetry` returns `$false` on the first write failure instead of offering a `Confirm-Action` retry. New `Save-ModuleResultCsv` extracts `Invoke-ActionModule`'s inline CSV-auto-save block (forcing `-AutoSave` in automation mode, so its dialog/prompt fallback becomes structurally unreachable) so both the interactive and automation paths share it |
| 2026-09-18 | APIModules\Safes\Invoke-SafesDelete.ps1, APIModules\Custom\Invoke-CustomTestApi.ps1 (v1.5.0) | `Invoke-SafesDelete.ps1`'s HTTP-409 rename offer now skips the `Read-Host` prompt in automation mode, falling through to the existing plain-`Failure` report unchanged. Its guard uses `Get-Variable -Name AutomationMode -Scope Script -ErrorAction SilentlyContinue` rather than a direct `$script:AutomationMode` reference - found via a real test failure that this module is also dot-sourced standalone by its own unit test (without `Manage-Privilege.ps1`'s Configuration region ever running), where a direct reference to the never-set variable throws under strict mode instead of evaluating falsy. `Invoke-CustomTestApi.ps1` declares the new `ModuleMeta.SupportsAutomation = $false` - it has no `InputData`-driven path at all (its entire request is built interactively), so automation mode rejects it at module-resolution time rather than risking a hang |
| 2026-09-18 | Tests\Unit\Manage-Privilege.Automation.Tests.ps1 (new file) | Deliberately separate from `Manage-Privilege.Tests.ps1`, which has a documented Pester/file-interaction hang. Added AM01-AM24: parameter validation (mutual exclusivity, `-StartProfile` requirement), `Invoke-CsvProcessing -FilePaths` against a fixture CSV with a mocked module function, `Invoke-ProfileConnect`'s automation-mode guard across all 4 failure states (no token, unreadable, refresh throws, 401-rejected) asserting the fresh-auth functions are never called, `Invoke-TokenRefresh`'s automation branch (silent refresh succeeds/throws, SAML/Interactive both fail without ever calling `Read-Host`), and `Get-AutomationExitCode`'s full mapping. All 1160 unit tests pass (1136 + 24 new) |
| 2026-09-18 | Docs\API-Module-Development-Guide.md | Added `SupportsAutomation = $false` to the Optional ModuleMeta Fields table, referencing `Custom/TestApi`. Added a new "Automation Mode" section documenting `$script:AutomationMode` for future module authors (read the same way `$script:WhatIfMode` already is), the `Get-Variable -ErrorAction SilentlyContinue` pattern needed for a module dot-sourced standalone by its own test, and `Invoke-SafesDelete.ps1`'s 409 handler as the existing precedent |
| 2026-09-18 | README.md, Docs\User-Guide.md | Added a README Features bullet for automation mode. Added a new "Automation mode (non-interactive)" subsection to User-Guide.md's Profiles section documenting `-Category`/`-Action`/`-InputFile`/`-InputJson`, the "never falls back to an interactive prompt" contract, the operational prerequisite (a profile must already have been authenticated interactively at least once), and the 0/1/2/3 exit-code table |
| 2026-09-18 | Docs\Testing-Plan.md | Added Finding F53; added 3 Component Test Matrix rows; added D37-D40 manual test procedures; updated the Driver-level checklist to reference D26-D40; added a Revision Log entry |
| 2026-09-18 | Docs\Architecture.md | Added 2 Design Decision rows: automation mode is guarded out of a cold start entirely rather than given its own credential-supply mechanism (the fresh-auth guard, reused for `Invoke-TokenRefresh`/`Invoke-FileWriteWithRetry`), and module loading is re-dot-sourced inline in `Invoke-AutomatedAction` rather than factored into a shared helper (dot-sourcing inside a normally-called function is discarded when it returns) |
| 2026-09-18 | Manage-Privilege.ps1 | Per user request ("Add an automation entry to run the Custom Export processes... Allow a new output folder and filename format to be specified"), clarified via 2 rounds of questions (there are actually 5 Custom export modules, not 3, all already automatable since F53; the real ask was generic `-OutputFolder`/`-FilenameFormat` overrides, not a new combined-run entry point). Added both launch parameters. New `Format-AutomationFilename` expands `{ModuleName}`/`{Category}`/`{Action}`/`{Profile}`/`{Date}` placeholders into a filename, appending `.csv` if absent. `Get-CsvSavePath` gained `-FolderOverride`/`-FileNameOverride` params (both optional, backward compatible) - an explicit folder override that doesn't exist is auto-created, unlike the existing `$DefaultFolder`'s long-standing silent-fallback-to-`$PSScriptRoot` behavior, which was left unchanged. `Save-ModuleResultCsv` reads both overrides (gated on `$script:AutomationMode`) and passes them through, so every automation-run `ProducesOutput` module picks them up with no per-module change |
| 2026-09-18 | APIModules\Custom\Invoke-CustomExportAll.ps1 | Same request: since this module writes its own per-sub-report CSVs directly rather than returning `Results` for the driver to save, it separately reads `-OutputFolder` (not `-FilenameFormat` - its output is inherently one file per sub-report, not a single name) to redirect where those land, via the same `Get-Variable -Scope Script -ErrorAction SilentlyContinue` pattern F53 established for `Invoke-SafesDelete.ps1` (this module is also dot-sourced standalone by its own unit test, where the variable was never set) |
| 2026-09-18 | Tests\Unit\Manage-Privilege.Automation.Tests.ps1, Tests\Unit\Invoke-CustomExportAll.Tests.ps1 | Added `Format-AutomationFilename` (AM25-AM29), `Get-CsvSavePath -FolderOverride/-FileNameOverride` (AM30-AM33), and `Save-ModuleResultCsv` wiring (AM34-AM36) test blocks, plus 3 `-OutputFolder` cases in `Invoke-CustomExportAll.Tests.ps1`. While writing AM34-AM36, hit and fixed a second Pester-only scoping artifact (confirmed via a minimal repro outside Pester that it is not a production bug): a bare top-level parameter reassigned via `$script:Name = ...` from inside an `It` block isn't visible to a same-file function's own bare-name read, because the outer `BeforeAll`'s initial no-args dot-source bound that parameter inside `BeforeAll`'s own nested scope rather than the file's real script-scope container that `$script:` addresses - fixed by having `Save-ModuleResultCsv` read `$script:OutputFolder`/`$script:FilenameFormat` explicitly, which is also correct (and unchanged in behavior) for real, single-scope-layer execution. All 1175 unit tests pass (1160 + 15 new) |
| 2026-09-18 | README.md, Docs\User-Guide.md, Docs\API-Module-Development-Guide.md | Added `-OutputFolder`/`-FilenameFormat` to the README automation-mode bullet and User-Guide's automation mode section (including an example command and a paragraph documenting the template placeholders). Extended `API-Module-Development-Guide.md`'s Automation Mode section with a short paragraph on the two new parameters and when a module needs to read them directly (only when it saves its own files, per `Invoke-CustomExportAll.ps1`'s precedent) |
| 2026-09-18 | Docs\Testing-Plan.md | Added Finding F54; updated 2 Component Test Matrix rows; added D41; updated the Driver-level checklist; added a Revision Log entry |
| 2026-09-18 | Manage-Privilege.ps1 | Per user request, following up on "which Custom export modules place a date in the filename?" - "Remove the date from the file name so they all behave the same. Double check the affect on the new automation piece." Added a new opt-in `ModuleMeta.CsvFilenameNoDate = $true` flag and a matching `Get-CsvSavePath -NoDateSuffix` switch (applied only to that function's own default-naming branch, deliberately not a blanket change to the shared default - `Custom/TestConnectivity` also uses `AutoSaveCsv` and keeps its own date suffix, since F26 added it on purpose and removing it wasn't requested). `Save-ModuleResultCsv` reads the new flag via bracket notation and passes it through; confirmed `-FilenameFormat` still fully overrides the filename even when a module also declares `CsvFilenameNoDate`, and that both `Invoke-ActionModule` and `Invoke-AutomatedAction` already call `Save-ModuleResultCsv` identically, so no additional wiring was needed for automation mode |
| 2026-09-18 | APIModules\Custom\Invoke-CustomExportEntitlements.ps1 (v1.3.0), Invoke-CustomExportGroupMembersLDAP.ps1 (v1.2.0), Invoke-CustomExportGroupMembersLocal.ps1 (v1.2.0), Invoke-CustomExportPlatformDetails.ps1 (v1.1.0) | Same request: all 4 now declare `CsvFilenameNoDate = $true`, so their saved filename is a fixed `"<Module Name>.csv"` with no date, matching `Custom/ExportAll`'s own fixed `Export_<Module>.csv` naming - every Custom export tool now behaves the same. Each run overwrites the previous one's file; automation users who want dated snapshots back use `-FilenameFormat` with `{Date}` explicitly |
| 2026-09-18 | Tests\Unit\Manage-Privilege.Automation.Tests.ps1, and the 4 affected modules' own test files | Added AM37/AM38 (`Get-CsvSavePath -NoDateSuffix`, and that `-FileNameOverride` still wins) and AM39/AM40 (`Save-ModuleResultCsv` honors `CsvFilenameNoDate`, and `-FilenameFormat` still overrides it), plus a `CsvFilenameNoDate is true` `ModuleMeta` assertion in each of the 4 modules' own test files. All 1183 unit tests pass (1175 + 8 new). Hit an unrelated Pester v6 gotcha while naming two new tests: an `It` title containing literal `<...>` is parsed as a `-TestCases` placeholder even with none present, throwing a confusing parser error unrelated to the test logic - fixed by rewording both titles to avoid angle brackets |
| 2026-09-18 | Docs\User-Guide.md, Docs\API-Module-Development-Guide.md | Added a `CsvFilenameNoDate` row to the Optional ModuleMeta Fields table. Corrected `-FilenameFormat`'s User-Guide description (the default it overrides is no longer always `"<Module Name> <date>.csv"`) and added a bullet noting every Custom export tool's filename is now fixed/date-free, overwritten on each run |
| 2026-09-18 | Docs\Testing-Plan.md | Added Finding F55; updated 2 Component Test Matrix rows; added D42; updated the Driver-level checklist; added a Revision Log entry |
| 2026-09-21 | Auth\CyberArk.Auth.SelfHosted.psm1 | Per user request, fixing K06 ("Automated runs will not use interactive authentication methods"). `Invoke-SelfHostedPasswordAuth` and `Update-SelfHostedAuthToken` gained a real `[switch]$NoPrompt` parameter - when set and no credential is available, throws a clear error instead of falling back to `Get-Credential`. Threaded as an explicit parameter rather than a `$script:AutomationMode` scope check, since this module is `Import-Module`'d into its own isolated scope, not dot-sourced into the driver's like API modules are |
| 2026-09-21 | Manage-Privilege.ps1 | Same request, the "user can create a credential object" half: added `Get-ProfileCredentialPath`/`Save-ProfileCredential`/`Get-ProfileCredential`/`Remove-ProfileCredential` (a new per-profile `.autocred` file, DPAPI via `Export-Clixml`, same mechanism as the existing `.cred` token file) and `Use-StoredCredentialIfMissing` (injects a stored credential into a token's `_RefreshContext` only when the token's own copy is missing and the auth method actually needs one - CyberArk/LDAP/RADIUS only). All 3 automation-relevant refresh call sites (`Invoke-ProfileConnect`'s logon-time and `Expired`-token refreshes, `Invoke-TokenRefresh`'s automation branch) now call this helper and pass `-NoPrompt` through to `Update-SelfHostedAuthToken`. Added a profile detail menu `[A]` (Automation Credential) action to set/clear the stored credential interactively. `Remove-DriverProfile` now also cleans up a profile's `.autocred` file on delete |
| 2026-09-21 | Tests\Unit\CyberArk.Auth.SelfHosted.Tests.ps1 (new file), Tests\Unit\Manage-Privilege.Automation.Tests.ps1 | Added SH-NP01-SH-NP04 - the first automated coverage `CyberArk.Auth.SelfHosted.psm1` has ever had (mirroring `CyberArk.Auth.ISPSS.Tests.ps1`, Finding F36), covering `-NoPrompt`'s throw-without-prompting behavior and that the existing interactive fallback is unchanged without it. Added AM41-AM50 covering the credential-store CRUD functions and `Use-StoredCredentialIfMissing`'s guard logic. All 1197 unit tests pass (1183 + 14 new) |
| 2026-09-21 | README.md, Docs\User-Guide.md | Added a clause to the README automation-mode bullet. Added a paragraph on the `[A]` action to User-Guide's automation mode section, and a note that a profile's stored automation credential isn't yet included in backup/restore (a documented, not silent, limitation) |
| 2026-09-21 | Docs\Architecture.md | Added a Security Considerations bullet describing the new `.autocred` file's DPAPI/user-machine-lock model. Added a Design Decision row explaining the `-NoPrompt`-as-real-parameter choice (vs. a scope-check, which wouldn't work across the `Import-Module` boundary) and the stored-credential fallback |
| 2026-09-21 | Docs\Testing-Plan.md | Added Finding F56; marked K06 resolved and corrected its description; updated A12's cross-reference; added D43; updated 3 Component Test Matrix rows; added a Revision Log entry |
| 2026-09-21 | Modules\CyberArkComms.psm1 | Per user request ("Work on K02 and K03 next"), fixing K02: `ServicePointManager.CertificatePolicy` (the SSL bypass `IgnoreSSL` enables) is a process-wide .NET Framework static that was never reset, so switching from an `IgnoreSSL=$true` profile to a different one left the bypass silently active for the rest of the session. Added `$script:SSLValidationDisabled` tracking and a new exported `Reset-SSLValidation` function (restores `CertificatePolicy` to `$null`). `Invoke-CyberArkAPI`'s gate now re-asserts the correct state on every call based on its own `-IgnoreSSL` value instead of only ever turning the bypass on. Corrected the module header's "scoped to the call" doc comment, which was inaccurate |
| 2026-09-21 | Auth\CyberArk.Auth.Common.psm1, Auth\CyberArk.Auth.SelfHosted.psm1 | Same request, an adjacent hazard found during the K02 investigation: 3 places in the Auth module independently assigned a raw `ServerCertificateValidationCallback` scriptblock - the exact crash-risk pattern Finding F15 already fixed for `Invoke-CustomTestApi.ps1`. Consolidated all 3 onto the shared `Disable-SSLValidation`/`Reset-SSLValidation`, which required `Auth.Common.psm1` to import `Modules\CyberArkComms.psm1` (no circular dependency). Also fixing K03: `Invoke-WebView2Window` gained an `-IgnoreSSL` parameter wiring `CoreWebView2.ServerCertificateErrorDetected` to `AlwaysAllow` (confirmed via .NET reflection against the shipped WebView2 Core DLL, not guessed) - the WebView2 control has its own separate network stack, entirely unreachable by the `ServicePointManager`-based fix. `Invoke-SelfHostedSAML`/`Invoke-SelfHostedOIDC` now actually forward their own `-IgnoreSSL` into it instead of silently dropping it |
| 2026-09-21 | Docs\Interfaces.md | Corrected the `IgnoreSSL` profile field description, which had falsely implied per-profile scoping ("for all API calls in this profile") - now describes the real process-wide-but-correctly-reset behavior and that it also applies inside WebView2 |
| 2026-09-21 | Tests\Unit\CyberArkComms.Tests.ps1, Tests\Unit\CyberArk.Auth.SelfHosted.Tests.ps1 | Added `Disable-SSLValidation`/`Reset-SSLValidation` and `Invoke-CyberArkAPI`'s per-call reset behavior (C34-C39) - the first unit test coverage either has ever had. Added SH-SSL01-SH-SSL04 confirming SAML/OIDC forward `-IgnoreSSL` to a mocked `Invoke-WebView2Window` correctly. Discovered `ICertificatePolicy` or even reading `ServicePointManager.CertificatePolicy` is unreliable under `pwsh`/.NET Core - confirmed directly against real Windows PowerShell 5.1 that the fix itself is correct there; skipped the 5 affected tests under `pwsh` with the reasoning documented in-file rather than leave them flaky. All 1202 unit tests pass, 5 skipped |
| 2026-09-21 | Docs\User-Guide.md | Expanded the `IgnoreSSL` profile setting bullet to describe the WebView2 coverage and the correct-reset-on-profile-switch behavior |
| 2026-09-21 | Docs\Architecture.md | Expanded the `IgnoreSSL` Security Considerations bullet. Added a Design Decision row covering both the reset-on-every-call change and the `Auth.Common.psm1` → `CyberArkComms.psm1` import added to reuse the shared SSL-bypass mechanism |
| 2026-09-21 | Docs\Testing-Plan.md | Added Findings F57/F58; marked K02/K03 resolved; added A19; updated A07/A08's WebView2 caveat and A12's cross-reference; updated 3 Component Test Matrix rows; added a Testing Boundaries note on the `pwsh`/`ICertificatePolicy` limitation; added a Revision Log entry |
| 2026-09-21 | Manage-Privilege.ps1 | Per user request ("Work on K04 and K07 next"), researched both first (a dedicated agent traced the full `BaseURL`/`Username` lifecycle and flagged K04's fix approach as a security-relevant call - no cryptographic binding exists between a token and the server that issued it, so automatic "same server" detection isn't safely possible). The user chose to force a fresh login on a mismatch rather than ever patch a token's `BaseURL`, across all 3 affected call sites. Added `Get-ExpectedTokenBaseURL` (mirrors a fresh login's exact URL construction - Self-Hosted's AppName-based shape, and separately confirmed ISPSS's own hardcoded `/PasswordVault` template, not assumed to match Self-Hosted) and `Test-TokenBaseURLStale` (case-insensitive, trailing-slash-normalized, never a false positive when the expected URL can't be computed). `Invoke-ProfileConnect` discards a stale token before trusting it; the Test Connection flow gets the same check plus a corrected URL-preference order (previously it *preferred* a saved token's URL over the profile's); `Invoke-TokenRefresh`'s SelfHosted branch now computes its re-auth URL from the current profile first |
| 2026-09-21 | Manage-Privilege.ps1 | Same request, K07: `Invoke-TokenRefresh`'s SelfHosted CyberArk/LDAP/RADIUS branch was missing the `$script:ActiveProfile.Username` fallback tier its ISPSS sibling branch already has, forcing an unnecessary extra `Read-Host` username prompt whenever `_RefreshContext` had no captured credential but the profile's `Username` was already known. Added the one missing `elseif` tier, mirroring the ISPSS branch exactly |
| 2026-09-21 | Tests\Unit\Manage-Privilege.Automation.Tests.ps1 | Added AM52-AM65 (14 tests): `Get-ExpectedTokenBaseURL`/`Test-TokenBaseURLStale` pure-logic coverage, `Invoke-ProfileConnect` discarding a stale-BaseURL token, and `Invoke-TokenRefresh`'s Username fallback with and without a `_RefreshContext` credential present. All 1216 unit tests pass (1202 + 14 new) |
| 2026-09-21 | Docs\Architecture.md | Added a Design Decision row for K04's fix, documenting the security tradeoff considered (no crypto binding between a token and its issuing server) and that forcing a fresh login was the user's explicit, informed choice over the alternatives |
| 2026-09-21 | Docs\Testing-Plan.md | Added Finding F59; marked K04/K07 resolved; added D44/D45; added a Component Test Matrix row; updated the Driver-level checklist; added a Revision Log entry. Also fixed a real documentation-structure bug found while doing this: Findings F57/F58 had been accidentally inserted into the Revision Log's 2-column table during the previous session instead of the Findings table - moved both to their correct location |
| 2026-09-21 | Docs\Testing-Plan.md | Per user direction ("Consider K10 closed. K11 make it functional"). Closed K10 - the remaining stuck safe is an environment-side/CyberArk-support matter outside this codebase's reach, not a defect |
| 2026-09-21 | Manage-Privilege.ps1 | Fixing K11: confirmed the entire Auth-layer `-WebView2AssemblyPath` plumbing was already complete (`Get-SelfHostedAuthToken`/`Get-ISPSSAuthToken` through the SAML/OIDC/SSO functions to `Invoke-WebView2Window`, including `_RefreshContext` persistence) - the only gap was the driver itself having no profile field and never passing one through. Added `WebView2AssemblyPath` as a new optional profile field (`New-BlankProfile`, `Show-ProfileDetail`, `Invoke-ProfileEditFlow`'s prompt after Ignore SSL Errors) and wired both `Invoke-ProfileConnect` and the Test Connection flow's `authParams` construction to forward it when set, using the standard `PSObject.Properties[...]` existence-guard so an older saved profile without the field doesn't throw |
| 2026-09-21 | Tests\Unit\Manage-Privilege.Automation.Tests.ps1 | Added AM66-AM68 confirming `WebView2AssemblyPath` reaches `Get-SelfHostedAuthToken`/`Get-ISPSSAuthToken` when set and is omitted when not. Extended the file's global auth-function stubs, which were missing several parameters these functions actually accept. All 1219 unit tests pass (1216 + 3 new) |
| 2026-09-21 | README.md, Docs\Interfaces.md, Docs\User-Guide.md | Corrected README's "not reachable through the driver" callout for `-WebView2AssemblyPath` (now a real profile field) and added it to README's Configuration table, `Interfaces.md`'s profile schema, and `User-Guide.md`'s Profile Settings Reference and SAML/OIDC Troubleshooting entry |
| 2026-09-21 | Docs\Testing-Plan.md | Added Finding F60; marked K11 resolved; added a Component Test Matrix row; added D46; updated the Driver-level checklist; added a Revision Log entry |
| 2026-09-21 | Modules\CyberArkCredentialStore.psm1 (new) | Per user request ("Start with extraction and then look at the PowerShell SecretManagement... Lets work on a design document first for this") - first step of a planned multi-source automation-credential helper script design. Extracted `Get-ProfileCredentialPath`/`Save-ProfileCredential`/`Get-ProfileCredential`/`Remove-ProfileCredential` (originally added for K06/F56) out of `Manage-Privilege.ps1` into their own module, so a future standalone helper script can reuse the same `.autocred` store without loading the whole interactive driver. Each function now takes an explicit `-ProfileDir` parameter rather than reading `$script:ProfileDir` implicitly, since `Import-Module` gives the module its own isolated scope. `Get-ProfileCredential`'s corrupt-file warning calls `Write-CyberArkLog` through a `Get-Command -ErrorAction SilentlyContinue` guard so the module works standalone without `CyberArkLogging.psm1` present |
| 2026-09-21 | Manage-Privilege.ps1 | Removed the 4 extracted functions; added a new unconditional `Import-Module` for `CyberArkCredentialStore.psm1` (a new `#region --- Core Module Imports ---`, before `Assert-Prerequisites` - not inside the dot-source-guarded Entry Point block used for Logging/Comms/Auth, since this module has no import-time side effects and Pester tests that dot-source the driver need the real functions available). Updated all 3 in-driver call sites (`Use-StoredCredentialIfMissing`, the profile-detail `[A]` menu action, `Remove-DriverProfile`) to pass `-ProfileDir $script:ProfileDir` explicitly |
| 2026-09-21 | Tests\Unit\CyberArkCredentialStore.Tests.ps1 (new), Tests\Unit\Manage-Privilege.Automation.Tests.ps1 | Added CS01-CS08 testing the new module standalone. Updated the existing AM41-50 tests' calling convention to pass `-ProfileDir $script:TempDir` explicitly (no test logic changed). All 1227 unit tests pass (1219 + 8 new), 5 skipped (unchanged) |
| 2026-09-21 | Docs\Architecture.md | Added `CyberArkCredentialStore.psm1` to the Build Status table, folder structure, component diagram's startup-import flow, and a new module description section |
| 2026-09-21 | Docs\Testing-Plan.md | Added Finding F61; added a Component Test Matrix row; added a `CyberArkCredentialStore.psm1 — Test Cases` section (CS01-CS08); updated the K06 driver row's cross-reference; added a Revision Log entry |
| 2026-09-21 | Docs\Automation-Credential-Sources-Design.md (new) | Per user request, design-only document (Status: Proposed - not started) for a planned standalone multi-source automation-credential helper script. Covers the existing DPAPI `.autocred` foundation (Section 3), a researched verdict on `Microsoft.PowerShell.SecretManagement`/`SecretStore` (Section 4 - both confirmed via their own manifests/Microsoft Learn docs/GitHub issues to nominally allow PS 5.1 but carry real documented 5.1 failures and an explicit "works on platforms that support PowerShell 7"/.NET Core crypto dependency for SecretStore specifically; both modules are now archived/feature-complete; recommendation is to NOT build on them), and a proposed `Get-AutomationCredential -Source <name>` dispatch design covering all 5 requested sources (DPAPI, Windows Credential Manager, CyberArk CP, CyberArk CCP, CyberArk Conjur). CP/CCP/Conjur sections are explicitly flagged as general CyberArk product architecture not confirmed against this project's local `C:\Code\References\` materials (none exist for these 3 products) or a live environment - each needs vendor documentation or environment access before implementation can proceed past this design |
| 2026-09-21 | (cross-project) New `C:\Code\aPeSecrets` project | Per user direction ("Create a new separate project calle aPeSecrets. In the current project we will have a simple wrapper to call this external project."), pivoted the credential-sources design from an in-aPePAS module to a new sibling project, per the user's stated reasoning that the capability is reusable beyond aPePAS. Discovered the sibling `aPeDiscovery` project already had a working `Modules\CredentialResolver.psm1` (`CurrentUser`/`PSCredential`/`CP`/`CCP`/`Conjur`, `CP` live-verified 2026-09-17 against a real Credential Provider) - per user choice, extracted it into aPeSecrets as the one canonical implementation (renamed `Get-DiscoveryCredential` to `Get-ResolvedCredential`) rather than writing a second, divergent copy, and repointed aPeDiscovery's 3 export scripts plus its 2 local-scanner modules at the new sibling path. Added `WindowsCredentialManager` as a genuinely new 5th source in aPeSecrets, via inline P/Invoke against the classic Win32 `CredRead`/`CredWrite`/`CredDelete` API - live-verified with a real round-trip against this machine's actual Windows Credential Manager store, cross-checked against `cmdkey /list`. Found and fixed a real bug while live-testing: adding `Set-StrictMode -Version Latest` (this project's own habit) to the new module broke the ported CP/CCP/Conjur code's `$Params.OptionalKey` pattern, since PowerShell's strict mode throws `PropertyNotFoundException` for a *missing hashtable key* accessed via dot notation under `-Version Latest`, not just a missing PSCustomObject property - removed strict mode from aPeSecrets's module to match the original, already-proven configuration rather than rewrite live-tested CP/CCP/Conjur logic. 17 new Pester tests in aPeSecrets (`Tests\Unit\CredentialResolver.Tests.ps1`), all passing |
| 2026-09-21 | Sync-AutomationCredential.ps1 (new) | The "simple wrapper" requested for aPePAS: takes `-ProfileName`/`-Source`/`-Params`/`-ProfileDir`/`-APeSecretsPath`, imports aPeSecrets (sibling path by default) and this project's own `Modules\CyberArkCredentialStore.psm1`, resolves a credential via aPeSecrets's `Get-ResolvedCredential`, and calls `Save-ProfileCredential` to seed/refresh a profile's `.autocred` file. `Manage-Privilege.ps1` itself was not modified - purely additive to the existing `Use-StoredCredentialIfMissing` mechanism. Live-tested end-to-end (not just unit-mocked): seeded a real Windows Credential Manager entry, ran the wrapper against a temp profile directory, confirmed the resulting `.autocred` file's username/password round-trip exactly via `CyberArkCredentialStore.psm1`'s own `Get-ProfileCredential`. No Pester test file added for this script - verified live instead, matching this project's existing convention for standalone/interactive-script code (e.g. profile backup/restore, Finding F51) |
| 2026-09-21 | Docs\Automation-Credential-Sources-Design.md | Rewrote the Status header and added Sections 9 (what actually shipped) documenting the aPeSecrets pivot; marked Section 5 (the original in-aPePAS dispatcher plan) as superseded but kept for its reasoning; updated Section 6's open questions with their resolutions; updated the Progress Tracker (Section 7); added a Revision Log entry |
| 2026-09-21 | (cross-project) aPeSecrets CCP live verification | Per user-supplied live test credentials (PVWA address, client certificate thumbprint, Safe `McWilliams Jesse`/Folder `root`/Object `CA_Automation_User`, AppID `APP_AIHost`), live-tested aPeSecrets's `CCP` source against a real PVWA/CCP host. First attempt returned CyberArk's structured `APPAP004E` error even though the account existed; per user direction, cross-checked the identical coordinates against the local `CP` source, which succeeded - isolating the failure to CCP-side provider authorization rather than a code or query-shape bug. Once the user granted that authorization, the identical CCP query succeeded, matching username and password length against the CP retrieval. Updated aPeSecrets's `README.md`/`Docs\Configuration.md` (a new diagnostic writeup for the `APPAP004E`-despite-CP-working symptom) and aPePAS's `Docs\Automation-Credential-Sources-Design.md` (Progress Tracker, Section 6, Section 9, Revision Log) to record `CCP` as live-verified. `Conjur` remains the one source not yet live-verified. Passwords were never printed to any output during this verification - only username and password length were surfaced |
| 2026-09-21 | Auth\CyberArk.Auth.ISPSS.psm1, Manage-Privilege.ps1 | Per user report: a new profile (`Bannermen_AutoUser`) using ISPSS `AuthMethod=ClientCredentials` prompted for OAuth2 Client ID/Secret and failed - the user's actual intent was plain CyberArk Identity username/password, no MFA, no OAuth (`AuthMethod=Interactive`). Investigating led to a real gap: `Interactive` could never refresh silently in automation mode even with the right `AuthMethod`, for two independent reasons confirmed directly by reading the code - a fresh login's typed password was never assembled into a `PSCredential` for `_RefreshContext`, and `Use-StoredCredentialIfMissing` (K06's `.autocred` fallback) explicitly excluded every ISPSS token regardless of `AuthMethod`. A third mechanism, `Invoke-ClearNonRefreshableContext` (previously untested), would have silently undone a fix to either - it actively stripped `Credential` from `_RefreshContext` for `Interactive` right after every connect. Fixed all three (K12/F62): `Invoke-IdentityChallengeLoop` gained a `-NoPrompt` switch and now captures a freshly-typed password for a single-factor `'UP'` mechanism; `Invoke-ISPSSInteractive`/`Update-ISPSSAuthToken` forward `-NoPrompt`; `Use-StoredCredentialIfMissing`'s guard and its 3 driver call sites now cover ISPSS Interactive; `Invoke-TokenRefresh`'s `$neverSilent` classification no longer includes `Interactive` (only `SSO` now); `Invoke-ClearNonRefreshableContext` no longer strips `Interactive`'s Credential |
| 2026-09-21 | Tests\Unit\CyberArk.Auth.ISPSS.Tests.ps1, Tests\Unit\Manage-Privilege.Automation.Tests.ps1 | Added ISPSS-INT01-05 (fresh-login credential capture, silent `-NoPrompt` refresh, clean failure with no stored credential, clean failure when a real second factor is required, unchanged prompting behavior without `-NoPrompt`) and AM69-76 (`Invoke-TokenRefresh`'s new ISPSS Interactive branches, `Use-StoredCredentialIfMissing` now covering ISPSS Interactive, a new `Invoke-ClearNonRefreshableContext` Describe block - previously untested). Fixed AM17 (was asserting Interactive was never-silent; now tests SSO) and retitled AM48 for accuracy. All 1240 unit tests pass (1227 + 13 net new), 5 skipped (unchanged) |
| 2026-09-21 | Docs\Testing-Plan.md | Added K12 and Finding F62; updated the ISPSS Auth and stored-automation-credential Component Test Matrix rows; added a Revision Log entry |
| 2026-09-21 | Docs\Interfaces.md, Docs\User-Guide.md | Updated `Update-ISPSSAuthToken`'s documented signature/behavior (added `-NoPrompt`, corrected the "Interactive/SSO: re-runs the full interactive flow" comment now that Interactive can be silent). Updated the Automation mode section's `[A]` Automation Credential description to cover ISPSS Interactive alongside Self-Hosted CyberArk/LDAP/RADIUS, including the caveat that a genuine second MFA factor still can't be satisfied non-interactively even with a stored credential |
| 2026-09-21 | Manage-Privilege.ps1, Tests\Unit\Manage-Privilege.Tests.ps1 | Per user report, live, immediately while applying the K12 profile fix: editing a pre-K11 profile crashed `Invoke-ProfileEditFlow` with `SetValueInvocationException` on `WebView2AssemblyPath`. Confirmed directly that `Get-AllDriverProfiles`'s existing field-backfill normalization (already proven for `CPM_List`, DP11a) never had `WebView2AssemblyPath` added when K11/F60 introduced it - every profile saved before K11 had no such property, and the edit flow's assignment to it (unlike its own already-guarded read) had no existence guard, so `Set-StrictMode -Version Latest` threw. One-line fix: added the field to the backfill list. Confirmed no other field has the same gap by diffing `New-BlankProfile`'s field list against the backfill list. Reproduced the crash directly before fixing it; added DP11b (mirroring the existing DP11a pattern) confirming both the backfill and that the exact assignment that crashed now succeeds. All 1241 unit tests pass (1240 + 1 new) |
| 2026-09-21 | Docs\Testing-Plan.md | Added K13 and Finding F63; added a Revision Log entry |
| 2026-09-21 | Docs\Testing-Plan.md | Per user report ("Authentication is now working for this user. Do an automated test for SaaS and run the Custom Export All."), live end-to-end verification of K12 and K13: `Bannermen_AutoUser` profile edited to `AuthMethod=Interactive` with no crash (K13 confirmed live), then `-Category Custom -Action ExportAll` completed successfully against real ISPSS/SaaS (`ExitCode=0`, 24/24 items succeeded) - K12 confirmed live for the automation-mode-with-Interactive-auth path, though the specific silent-refresh-on-token-expiry mechanism wasn't directly exercised in this run and remains flagged as still needing that observation. Updated Findings F62/F63 and the ISPSS Auth Component Test Matrix row; added a Revision Log entry |
| 2026-09-21 | Manage-Privilege.ps1 | Per user report: selecting exactly one profile to back up crashed `Invoke-ProfileBackupFlow` with `PropertyNotFoundException` on `.Count`. Confirmed directly via isolated repro: PowerShell unwraps a single-element array (even a strongly-typed `[string[]]`) to its bare scalar element when it's the output of an `if`/`else` expression captured by assignment. Found and fixed the identical pattern in both `Invoke-ProfileBackupFlow` and `Invoke-ProfileRestoreFlow` (the latter not yet reported) by wrapping the assignment in `@(...)`. Checked the rest of the file for the same shape; found one more instance with the same mechanism but no observable bug, left unchanged. Verified via a standalone, non-Pester repro (documented Pester/file-interaction hang risk for this class of function, per Finding F51) - reproduced the crash first, then confirmed the fix selects/restores exactly the right single profile |
| 2026-09-21 | Docs\Testing-Plan.md | Added K14 and Finding F64; updated the profile backup/restore Component Test Matrix row; added a Revision Log entry |
| 2026-09-21 | Manage-Privilege.ps1 | Per user direction to run a full self-hosted functional test pass, found live: a second automation-mode run against the same profile always failed with a 401, even though token validation reported it as good. Ruled out query-param shape and session churn directly before confirming the real cause: `Invoke-AutomatedAction` unconditionally called `Invoke-SessionLogoff` at the end of every run, and `POST /API/auth/Logoff` genuinely revokes the session server-side - killing the very session a subsequent run needed, defeating K06/K12's saved-session design for any multi-run scenario. Removed the call (the interactive session loop's own separate logoff is unaffected) |
| 2026-09-21 | Tests\Unit\Manage-Privilege.Automation.Tests.ps1 | Added AM77: a structural regression guard confirming `Invoke-AutomatedAction`'s source never calls `Invoke-SessionLogoff` (comment lines stripped first to avoid a false match against the fix's own explanatory comment) - `Invoke-AutomatedAction` has no full mock-based test coverage otherwise, per Testing Boundaries. Live-verified end-to-end instead: 3 consecutive real automation-mode runs against a real Self-Hosted PVWA, one saved session, zero re-authentication, all `ExitCode=0`. All 1242 unit tests pass (1241 + 1 new) |
| 2026-09-21 | Docs\Testing-Plan.md | Added K15 and Finding F65; updated the automation-mode Component Test Matrix row to record live verification; added a Revision Log entry |
| 2026-09-21 | Docs\Testing-Plan.md | Per user direction, ran a comprehensive write-action test batch via automation mode against the real Self-Hosted PVWA (two disposable test safes, one with an account) - Safes/Accounts/SafeMembers/Groups/Applications CRUD, Platforms lifecycle on a disposable copy, and `ResetImmediately` verification for ChangeImmediate/Reconcile/Verify via the account's `Activities` audit log (the property is a PVWA UI concept, not a REST field - confirmed by user clarification). All succeeded except `Platforms/Rename` (clean HTTP 405, consistent with its documented PVWA 15.0+ requirement) and `Reports/List` (clean HTTP 403, a permissions-scope gap for this test account) - neither a code defect. Updated ~20 Self-Hosted Functional Checklist line items to reflect what's now confirmed, explicitly skipped (`SetMasterPolicy`, per direct instruction), or still not attempted (items needing preconditions not naturally present: `CancelCpmTask`/`CheckIn`/`GetCredential`/`Unlock`/`ResumeAutoManagement`/template-role actions/`Platforms Import`/`SetPSMConfig`). Added a Revision Log entry. Documentation only - no code changes |
| 2026-09-21 | (no code changed) SaaS test setup | Per user direction ("Next is SaaS"), set up a real Privilege Cloud (ISPSS) test profile (`SaaS_Automation_Test`, subdomain `bannermen-nfr`) authenticated as `Automation_Admin_SaaS`, via aPeSecrets' CP source. Found and resolved two environment/configuration gotchas, neither a code defect: this tenant's CyberArk Identity resolves the bare username to a different, MFA-enrolled identity than the UPN-format `Automation_Admin_SaaS@company.com` (confirmed via a raw `StartAuthentication` call showing only a single-factor `UP` challenge for the UPN form - per user correction after an initial MFA-blocker report); and a new ISPSS profile needs `BaseURL` set explicitly to `https://<subdomain>.privilegecloud.cyberark.cloud/PasswordVault`, matching the regex `Invoke-ProfileConnect` uses to derive `PCloudSubdomain` - it is not inferred from any other field |
| 2026-09-21 | (no code changed) SaaS read-only test pass | With `SaaS_Automation_Test` authenticated, ran a full read-only pass via automation mode against real ISPSS, all under one reused session (confirming K15's session-reuse fix applies identically to ISPSS): `Safes/List` (29 safes), `Accounts/List` (21 accounts, second consecutive run with no re-auth), `Users/List` (40 users), `Groups/List` (70 groups), `Platforms/List` (39 platforms), `Applications/List` (2 applications), `Applications/ListAuthMethods` (2 checked, 0 methods), `Policies/GetMasterPolicy` (clean HTTP 404, re-confirming Finding F40), `Custom/ExportEntitlements` (29 safes, 114 members), `Custom/ExportPlatformDetails` (14/14 platforms), `Custom/ExportAll` (8/8 modules succeeded). No new code bugs found |
| 2026-09-21 | Docs\Testing-Plan.md | Updated the ISPSS Full Functional Checklist's Accounts, Safes, Platforms, Policies, Users, Groups, Applications, and Custom sections to record today's live SaaS confirmations; added a note to the "requires interactive human authentication" caveat documenting the direct-Auth-module-invocation test method and the UPN-format finding; added a Revision Log entry |
